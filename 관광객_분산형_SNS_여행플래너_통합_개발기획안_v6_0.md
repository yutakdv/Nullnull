# 관광객 분산형 SNS 여행플래너 통합 개발기획안 v6.0

> 브랜드명: **Nullnull**
>
> 공식 제출명: **Nullnull — 내 일정을 읽는 분산형 여행 SNS**
>
> 한 줄 정의: **여행 게시글에서 발견한 장소를 여행별 후보 보관함에 담고, 사용자가 직접 편집하거나 AI 최적화안을 승인해 혼잡·시간·동선을 함께 조정하는 일정 연동형 Social Travel Platform**
>
> 출품 분야: **2026 관광데이터 활용 공모전 ②-2 웹·앱 구현 부문**
>
> 선택 지정과제: **2번 - 유명 관광지 중심의 관광객 쏠림 현상으로 인한 특정 명소의 오버투어리즘(과밀화) 문제**
>
> 제출물: **실행 가능한 웹 URL + 공식 양식 기능설명서 PDF + 제출 시스템 입력정보**
> 문서 상태: Figma 초안 대조 + 여행 후보·수동 편집·AI 리밸런싱 구조 반영안 / 2026-09-02
> **v5.7 개정: 1차 타깃을 방한 외국인 자유여행객으로 변경. 구현은 한국어 우선 → 영어 순 / 2026-08-27**
> **v5.8 개정: S05·S08 삭제. 화면 수 축소, 정보 중복 제거 / 2026-08-27**
> **v5.9 개정: 서울 Live 4단계 서수·단일 Selector, 비교 UI 이분법, REPLAY P0 필수 경로, 다일 readiness, 시간축 수락 재검증을 데이터·UI·테스트·KPI까지 전파 / 2026-08-28**
> **v5.9 추가 반영: §6.8 UI 디자인 재량·계약 경계 신설, UI 시안 확정 시 기획안 동기화 절차 명문화 / 2026-08-28**
> **v5.9 디자인 틀 컨펌 반영: 일정 미등록 Home 안내, 일정 있음/없음 선분기, TXT·CSV·XLSX 일정 가져오기, 단일 일정 확인 단계, 전체 일정 생성 P1 분리, +Trip 상태, 하트=Must Visit, 사용자 시작 장소 교체를 확정 / 2026-08-30**
> **v6.0 개정: Feed·검색·Live의 `+`를 실제 일정 추가가 아닌 여행별 `담아둔 장소` 저장으로 변경하고, My Trip을 일정 보기·수동 편집·AI 최적화의 세 상태로 재구성. 독립 잠금, 유사 일정 안내, 전체 일정 리밸런싱, 지도·타임라인 비교·승인을 P0→P1 연속 구현 범위로 확정 / 2026-09-02**

> **v5.6 핵심 개정:** 집중률 예측 후보은행 10개 이상·실제 편차가 있는 `DEMO_READY` POI 5개 이상을 확보하고 `DemoScenarioFactory`가 세션마다 유효 후보를 선택하게 한다. Live 우회는 절대 혼잡·현재 상대 비교·명시적 과거 관측 재생의 세 모드로 분리해 심사 시점에도 정직하게 재현한다. P0 일정 입력은 한국어 지원 형식 8종, TXT·CSV·XLSX 가져오기와 직접 입력 보정으로 동결한다. **1차 타깃을 방한 외국인으로 두되 구현은 한국어 UI로 먼저 완성하고 안정화 후 영어를 추가한다.** EngService2 영문 관광정보와 한영 Canonical 매핑은 P1로 유지한다. 일정이 없는 사용자를 위한 최소 입력 경로를 P0 필수로 둔다. 국문 관광정보·집중률 API의 운영계정/쿼터 신청과 호출 예산을 제출 Gate에 추가한다.

---

# v6.0 개정 요약 (2026-09-02)

Figma 초안의 Home Feed, 게시물 상세, S02 계획 입력, S06 `일정에 담기`, S07 My Trip, S09 One Small Change·장소 교체, S11 Live·REPLAY 프레임을 대조했다. 기존 틀의 시각 방향과 화면 통합 원칙은 유지하되, 발견한 장소가 곧바로 확정 일정이 되는 모호함과 수동 편집·AI 변경의 역할 중복을 해소한다.

## v6.0 핵심 상태 모델

| 상태 | 의미 | 생성·변경 시점 |
|---|---|---|
| `SavedPost` | 여행과 무관하게 다시 볼 콘텐츠 북마크 | Feed의 저장 액션 |
| `TripCandidate` | 선택한 여행에 담아둔 방문 후보. 날짜·시간 미배치 상태 | Feed·검색·Live·장소 상세의 `+` |
| `TripItem` | 날짜·시간·순서가 확정된 일정 항목 | My Trip 수동 편집 저장 또는 승인된 AI 변경 적용 |
| `OptimizationRun` | 특정 Trip version과 화면 범위를 입력으로 계산한 최적화 실행 | My Trip의 `AI로 일정 최적화` |
| `OptimizationProposal` | 지도·타임라인·지표로 비교하는 적용 전 변경안 | 계산 완료 후 S09 Preview |

`TripCandidate` 저장만으로 `TripItem`을 생성하지 않는다. Feed 카드에는 북마크와 여행 후보 담기를 서로 다른 액션으로 표시한다. 기존 `+Trip` 명칭은 사용자 화면에서 `+` 또는 `내 여행에 담기`로 바꾸고, 실제 일정 배치는 `일정에 배치하기`로 구분한다.

## 확정 User Flow

```text
Home Feed·검색·Live·장소 상세
→ +
→ 활성 여행의 담아둔 장소 저장
→ 계속 둘러보기 또는 일정에 배치하기
→ My Trip
   ├─ 일정 편집: 직접 추가·교체·날짜·시간·잠금 수정
   └─ AI로 일정 최적화: 현재 보이는 일정 범위 계산
      → 변경 전·후 지도 + 타임라인 + 지표
      → 적용/유지 확인
      → 승인 시 원자적 반영 + 되돌리기
```

## My Trip의 두 변경 방식

### 일정 편집 — 사용자 주도

- 편집 진입 시 일정과 함께 `담아둔 장소` 패널을 연다.
- `Must Visit`, 날짜 고정, 시간 고정을 각각 독립적으로 수정한다.
- 예약·티켓·투어 잠금은 사용자 편의 잠금보다 강한 제약으로 표시하고, 해제에는 별도 경고와 확인을 요구한다.
- 담아둔 장소는 `일정에 추가 / 기존 장소와 교체 / 둘 다 유지` 중 하나를 선택한다.
- 교체 시 기존 항목의 날짜·시간·순서를 기본 승계하되 사용자가 저장 전에 수정할 수 있다.

### AI로 일정 최적화 — 제안 후 승인

- 날짜 화면에서는 해당 날짜, 전체 화면에서는 여행 전체를 기본 범위로 사용하며 실행 전에 범위를 표시한다.
- 기본값은 현재 일정에 배치된 `TripItem`만 사용한다. `담아둔 대안도 포함`은 명시적 opt-in이며 Must Visit 교체에는 사용할 수 없다.
- 우선순위는 `hard lock·예약 → 운영·충돌·접근성 → 혼잡 완화 → 이동시간·거리 → 선호·일정 안정성`이다.
- 변경 전에는 실제 일정을 수정하지 않는다. 적용 직전 최신 Trip version·예측·경로 Snapshot을 재검증한다.
- 사용자 화면에서는 AI 최적화라고 표현하되, 영업·혼잡·경로·도착시각은 서버의 결정적 검증과 optimizer가 계산하고 LLM은 선호 해석과 근거 설명만 담당한다.

## 유사 일정 안내

- Canonical POI가 같으면 `이미 일정에 있어요`로 처리하고 중복 후보를 만들지 않는다.
- 유사 장소가 있으면 담아둔 장소 카드에 `Day 2의 경복궁과 비슷한 역사·궁궐 경험이에요`처럼 대상과 reason code를 표시한다.
- 유사성 배지는 비차단 안내다. 사용자가 추가·교체를 선택할 때만 비교·확인 단계를 연다.
- Must Visit 대상 교체는 잠금 해제 확인, 비교 불가 혼잡값은 병렬 우위 표시 금지, 운영·이동·접근성 확인 불가는 `확인 필요`를 유지한다.

## 단계 정의

P0와 P1은 공모전 포함 여부가 아니라 구현 순서다. **공모전 최종 범위는 P0와 P1의 합**이며, P0 MVP의 Submission Gate를 먼저 닫은 뒤 같은 데이터 모델과 화면 위에서 P1을 이어서 구현한다.

| 단계 | 구현 범위 |
|---|---|
| P0 MVP | 여행별 TripCandidate, Feed `+`, My Trip 수동 편집, 독립 잠금, 중복·규칙 기반 유사 안내, 날짜 중심 작은 변경, 적용 전 비교·승인·되돌리기, 목록 기반 Live |
| P1 공모전 확장 | 다일 전체 일정 생성, 실제 route matrix 기반 공간·시간 리밸런싱 지도, 방문 순서·시간 변경, opt-in 후보 교체, Live Re-plan, Search·알림·UGC·영어 확장 |

## Figma 동기화 원칙

- 현재 Home Feed·게시물 상세의 `+`와 S11 후보 카드의 `+ 담기`는 모두 `TripCandidate 저장`으로 의미를 통일한다.
- 현재 S06 날짜·시간 선택 Bottom Sheet는 `담기 완료`가 아니라 `즉시 일정에 배치하기`의 선택적 2단계로 이동한다.
- 현재 S07은 기본 일정 보기로 유지하고 `일정 편집`, `AI로 일정 최적화`, `담아둔 장소 n` 진입점을 추가한다.
- 현재 S09 One Small Change의 전후 비교 틀은 `ITEM | DAY | TRIP` 범위의 Optimization Preview로 확장하고 지도·타임라인·변경 지표를 추가한다.
- S05와 S08은 계속 삭제 상태로 유지한다. 장소 상세는 S11, 날짜·경로 비교는 S09에 둔다.
- 상세 화면별 수정 명세는 [`docs/design/피그마_디자인_틀_수정.md`](./docs/design/피그마_디자인_틀_수정.md)를 기준으로 한다.

---

# v5.9 개정 요약 (2026-08-28, 이력)

서울 Live 121개 권역의 하루 13개 시점을 구조 검증한 결과, 4단계 서수값에서 `상위 25%`가 동률로 붕괴하고 카테고리 동일성을 측정 비교그룹에 넣으면 13개 시점 중 6개에서 후보쌍이 0이 됐다. v5.9는 이 결과를 반영해 비교 가능성·대체 가능성·주장·CTA를 각각 분리한다.

## 확정 계약

1. 교차 POI의 전후 상태는 같은 측정 비교그룹·같은 수집 배치에서만 병렬 표시한다. 비교 불가이면 화살표와 두 Level을 모두 제거한다.
2. `comparison_group_id`는 측정 동질성만 나타내고 카테고리·거리·중첩·운영·검수는 `CandidateRelation`이 맡는다.
3. 서울 Live 원천 `여유/보통/약간 붐빔/붐빔`을 `sourceOrdinal=1~4`로 보존하고 창작 5단계 값으로 공간 개선폭을 계산하지 않는다.
4. `LIVE_ABSOLUTE`와 `LIVE_RELATIVE`는 단일 `ORDINAL_LOWER` Selector를 공유한다. `claimMode`와 `ctaPolicy`를 분리해 일반 사용자 능동 CTA는 ABSOLUTE에만 허용한다.
5. RELATIVE는 `다른 관광지` 탭에서 사용자가 `지금 더 여유로운 곳` 정렬·필터를 직접 켠 경우에만 on-demand로 연다.
6. 공간 후보는 3km 상한, 0.5km 최소 중심점 이격, 권역 중첩률 3단계, 운영·민감지역·수용력·검수 Gate를 통과해야 한다.
7. 한 수집 사이클을 `snapshot_set_id`로 묶고 배치 내 원 관측시각 최대-최소 편차를 10분 이하로 제한한다.
8. REPLAY는 예외가 아니라 심사 시각 독립성을 위한 P0 필수 재현 경로다. 대표 세트는 7일 이내이며 최소 주 1회 갱신한다.
9. `spatialDemoReady`는 `sourceRegistryValid AND curatedRelationBankValid AND replayReady`로 판정한다. 현재 Live는 보너스 경로다.
10. 시간축 날짜 비교는 같은 예측 발행 회차에서만 허용하며, Suggestion 수락 직전 최신 회차로 개선 방향을 재검증한다.
11. 장기 구간에는 근거 없는 혼잡 숫자를 만들지 않고 `공식 혼잡 예측 범위 밖`과 출처 있는 주말·공휴일 정성 배지만 표시한다.
12. FeedScore는 혼잡 결측을 0으로 대체하지 않으며 선택적 혼잡 신호가 핵심 관련성 점수를 압도하지 못하게 한다.

## 운영 수집·readiness

- 검수 후보은행은 readiness 점검 창에 맞춰 조밀 수집한다.
- 신규 앵커 발굴용 121개 전수 스캔은 일 1~2회 저빈도로 분리한다.
- 최근 7일 운영 readiness와 rolling 28일 경향 보고를 분리한다.
- 검수 완료 앵커 3개 이상, CandidateRelation 5개 이상, 5개 관계가 최소 2개 앵커에 분산돼야 한다.
- 대표 REPLAY 만료 D-2에 경고하고 만료·연속 갱신 실패 시 `SUBMISSION_BLOCKED`로 전환한다.

## 주장 경계

> 방한 외국인 대상 설계, 현 단계 한국어 검증 버전.

> 서울 Live는 외국인 관광객 수가 아니라 권역 전체 인구 추정 기반의 혼잡 Context다.

> Nullnull은 새 코스를 대신 짜는 서비스가 아니라, 사용자가 이미 만든 일정에서 실행 부담이 작은 한 가지 변화를 제안한다.

## UI 디자인 병행 원칙 (추가 반영)

- UI 디자인 시안 작업이 기획과 병행 중이다. 기획안의 UI 관련 문장을 **디자인 재량 층 · UI 계약 층 · 개정 필요 층**으로 구분하며, 세부 기준과 절차는 §6.8에 둔다.
- **UI 시안이 확정되면 §6.8 동기화 절차에 따라 기획안을 시안에 맞춰 수정한다.** 스타일·레이아웃은 디자인이 주도하고, 데이터 정직성·심사 방어에서 나온 UI 계약은 어떤 시안에서도 유지한다.
- 2026-08-30 디자인 틀 컨펌 결과는 [`docs/design/피그마_디자인_틀_수정.md`](./docs/design/피그마_디자인_틀_수정.md)를 기준으로 한다. 날짜 변경의 충돌·재검증·되돌리기는 디자인 수정 항목이 아니라 본 기획안 S09의 기능 규칙으로 관리한다.

---

# v5.8 개정 요약 (2026-08-27)

와이어프레임 작업 중 **정보 중복이 확인되어 화면 2개를 삭제**한다. 기능은 유지하고 배치만 바꾼다.

## 삭제한 화면

| 화면 | 삭제 사유 | 어디로 갔나 |
|---|---|---|
| **S05 POI 상세** | S11 Live 상세와 정보가 완전 중복. 유지 지점이 둘로 갈라짐 | **S11 시트 전면 교체** |
| **S08 Crowd Calendar** | 사용자가 찾아가야 하는 화면인데, 정작 결정은 일정 화면에서 일어남 | **S11 혼잡 예보 3구간** + **S07 카드 인라인 제안** |

## 기능은 하나도 줄지 않았다

| 구 S05·S08 요구사항 | 새 위치 |
|---|---|
| 장소 상세 정보·출처·기준시각 | S11 상세 |
| 날짜별 혼잡 비교 | S11 상세 혼잡 예보 3구간 |
| `예측` vs `예측 범위 밖` 시각 구분 | 구간별 독립 카드 + 범위 밖은 출처 있는 정성 배지만 표시 |
| 날짜 변경 제안 진입 | **S07 카드 인라인** (`10/6이 더 여유로워요 →`) |
| 연관 관광지 | **S07 카드 뒤 레이어** (대체 관광지 1곳) |
| 후보은행 10개·`DEMO_READY` 5개 Gate | 그대로 유지 |

## 왜 이 방향이 맞나

**① 결정이 일어나는 자리에 제안을 둔다**
§4.4가 이미 `사용자가 My Trip과 Calendar를 차례로 찾아가지 않아도` 되도록 요구했다.
같은 논리를 확장해, 날짜 변경 제안을 일정 카드 안으로 옮겼다.

**② 화면이 적을수록 P0 완성도가 올라간다**
§3.5 Vertical Slice 원칙 — 범위를 넓히기 전에 하나의 흐름을 완성한다.
14개 화면 중 2개를 줄이면 나머지에 공수를 집중할 수 있다.

**③ 정보 중복은 표기 불일치를 낳는다**
같은 장소의 혼잡을 두 화면에서 각각 렌더하면 `데이터 상태`·`기준시각` 표기가
어긋날 위험이 있다. §6.7이 요구하는 일관성을 지키기 어려워진다.

## 유지되는 제약 (전부 승계)

- 동일 POI의 날짜끼리만 비교. 서로 다른 POI의 절대 혼잡 우열로 해석 금지
- **세 예보 구간을 하나의 연속 그래프로 잇지 않는다** (§9.2 교차 소스 비교 금지)
- `공식 혼잡 예측`과 `예측 범위 밖 정성 Context`를 시각·텍스트로 구분
- 원시 집중률을 대기시간·현장 인원처럼 오해할 정밀 숫자로 표시 금지
- 예측 범위 밖에서 임의 수치 생성 금지. `공식 혼잡 예측 범위 밖` 메시지와 검증된 주말·공휴일 배지로 빈 상태를 설명

## 제출물 주의

기능설명서 슬라이드 6(`#혼잡도분석`)의 캡처 대상이 바뀐다.
`Crowd Calendar` 화면이 아니라 **S11 상세 혼잡 예보 3구간**과
**My Trip 카드 인라인 제안**을 캡처한다.

---

# v5.7 개정 요약 (2026-08-27)

v5.6의 P0 범위 축소 판정은 대부분 유지한다. **타깃 정의 하나만 바꾸고, 그로 인해 성립하지 않게 된 전제들을 정리한다.**

## 무엇을 바꿨나

| # | 항목 | v5.6 | v5.7 | 사유 |
|---:|---|---|---|---|
| 1 | 1차 타깃 | 국내 자유여행객 | **방한 외국인 자유여행객** | 지정과제 2번의 쏠림 주체와 개입 대상을 일치시킴 |
| 2 | UI 언어 | P0 한국어만 | **한국어로 P0 완성 → 안정화 후 영어 추가** | 타깃은 외국인이지만 구현·검증은 한국어로 먼저 끝낸다. 두 언어를 동시에 만들면 레이아웃 회귀와 QA가 2배가 되어 핵심 E2E가 지연됨 |
| 3 | S02 진입 | 일정 보유 전제 | **일정 없는 최소 입력 경로를 동등한 P0 경로로 추가** | 여행 전 외국인은 한국어 일정을 보유하지 않는 경우가 많음 |
| 4 | 발전계획 | 영어·데이터 확대 | **UGC 경험 공유를 P1에 명시** | 외국인 간 경험 공유가 분산 효과를 재생산하는 장기 구조 |

## 무엇을 바꾸지 않았나

아래는 **의도적으로 v5.6 판정을 유지**한다. 커뮤니티 지향이라는 제품 방향과 별개로, 공모전 P0 기간에 안정적으로 구현·시연할 수 없기 때문이다.

- **사용자 게시물 작성(S13)은 P1 유지.** 저작권·스팸·신고 SLA·운영 인력이 선행돼야 한다. 심사 기간에 UGC가 비어 있으면 커뮤니티를 내세울수록 빈 피드가 치명적이다.
- **팔로우 그래프·Social 점수는 P1 이후 유지.**
- **P0 Feed 내용은 공식 Seed 카드 유지.** §1.3 비목표의 "완성된 SNS처럼 주장하지 않는다"를 그대로 지킨다.
- **EngService2 영문 관광정보·한영 Canonical 매핑은 P1 유지.** UI 문자열 번역과 관광정보 원문 번역은 공수·리스크가 다르다. 전자만 P0로 올린다.
- **일정 파서 범위는 한국어 지원 형식 8종 고정.** 영문 일정 파싱은 P1이다. 외국인 사용자는 파서가 아니라 최소 입력 경로로 진입한다.

## 커뮤니티 지향과 P0 범위의 관계

제품의 장기 방향은 **외국인 여행자끼리 대체 관광지 경험을 공유해 분산이 재생산되는 커뮤니티**다. 다만 P0에서 이를 기능으로 구현하지 않는다.

P0가 담당하는 것은 **그 커뮤니티가 성립할 수 있는 톤과 구조**다. 공식 Seed 카드를 쓰되 광고나 제안처럼 보이지 않게 하고, 사용자가 부담 없이 발견하는 경험을 만든다. 카드 구조에 작성자 자리(`공식 관광정보 기반` 라벨)를 미리 두어 P1에서 UGC가 들어와도 레이아웃을 다시 짜지 않는다.

즉 **P0는 커뮤니티의 그릇을 만들고, P1이 내용을 채운다.**

## 심사 표현 주의

- 사이트·기능설명서·제출 시스템에서 P0를 **사용자 커뮤니티가 작동하는 SNS로 표현하지 않는다.**
- 영어 지원은 **화면 문구 범위**임을 명확히 하고, 영문 관광정보를 제공하는 것처럼 오인시키지 않는다.
- 외국인 타깃은 기획 방향이며, P0 검증 시나리오는 서울 여행 흐름으로 동일하다.

---

# 0. 이 문서의 사용법

이 문서는 기획, 디자인, 프론트엔드, 백엔드, 데이터 작업과 공모전 제출 준비를 하나의 기준으로 맞추기 위한 실행 문서다.

- **1~4장:** 제품 목표, 범위, 핵심 사용자 흐름
- **5~7장:** 정보구조와 화면별 상세 명세
- **8~13장:** 기능 규칙, 데이터, API, 기술·운영 설계
- **14~17장:** 데모·심사 시연, 기능설명서, 제출 시스템 입력내용
- **18~21장:** 일정, 테스트, 리스크, 최종 완료 기준
- **22~25장:** 기능별 구현 감사, API 전수표, 추천 알고리즘, 오버투어리즘 효과 검증
- **26장:** 최종 서비스 정의

화면과 기능설명서의 역할은 반드시 구분한다.

| 구분 | 목적 | 포함할 내용 | 포함하지 않을 내용 |
|---|---|---|---|
| 개발 사이트 | 심사위원이 직접 기능을 실행하고 결과를 확인 | 실제 화면, 상호작용, 데이터 결과, 상태 변화, 오류·Fallback, 출처 | 장문의 문제 분석, API 키, 미구현 기능 |
| 기능설명서 PDF | 지정과제와 구현 기능·데이터의 연결을 설명 | 공식 양식 필수 항목, 실제 캡처, 흐름, 실제 사용 API, 차별성 | 상세 DB 스키마, 소스코드, 미사용 API |
| 콘텐츠랩 제출 시스템 | 접속과 OpenAPI 사용 검증 | 서비스 URL, 테스트 방식, 인증키, 실제 API 목록 | 서비스 홍보 콘텐츠, 기술 백서 |
| 내부 개발 명세 | 구현 충돌 방지와 운영 기준 | 알고리즘, 모델, API 계약, 캐시, 로그, 테스트 기준 | 심사용 장문 설명 |

## 0.1 최종 검토 8개 항목 판정

8개 수정 의견은 모두 타당하다. 다만 SNS·위치·혼잡 결합은 과장이나 규제 리스크를 피하도록 아래 조건을 붙여 반영한다.

| # | 검토 항목 | 판정 | 최종 반영 기준 |
|---:|---|---|---|
| 1 | 공식 출품 부문 명칭 | 타당·즉시 수정 | `②-2 웹·앱 구현 부문`으로 문서·PDF·제출 시스템 통일 |
| 2 | 여행 게시글이 메인인 SNS First 정의 | 타당·핵심 강화 | `/` 첫 화면은 일반 여행 게시글 Feed, 일정 등록 후 개인화, 여행 중 맥락 카드; P0는 공식 Seed임을 명시 |
| 3 | 고정된 데모 날짜·개선 수치 제거 | 타당·필수 | 실행일 기준 D+7~D+10, 실제 응답으로 POI·Level 선택; 일반 사용자는 개선 없으면 제안 생략, 데모는 `DEMO_READY` 후보 재선택 |
| 4 | P0 범위 축소와 Submission Gate 고정 | 타당·필수 | 날짜 변경 1종, 검수 연관 후보 1개, Live 기준 장소 선택, UGC·지도·시간/경로 변경은 P1 |
| 5 | GPS·위치정보 처리 재설계 | 타당·필수 | P0 서버는 좌표를 받지 않음; 장소 직접 선택·다음 일정 기준, 선택적 기기 내 계산만 허용 |
| 6 | 관광공사 OpenAPI 실제 호출·저장정책 | 타당·필수 | 실제 호출+단기 캐시, 무승인 전체 영속 미러 금지, 불가피한 저장은 제공기관 서면 확인 |
| 7 | 혼잡 소스의 비교 가능 범위 제한 | 타당·필수 | KTO는 동일 POI 날짜 비교, 서울 Live는 동일 source·scope 그룹 비교, 교차 소스 절대 순위 금지 |
| 8 | 일정 잠금·제약·제안 이력·소유권 모델 보강 | 타당·구현 전제 | 목적지/날짜/시간 잠금 분리, Trip version·snapshot 근거·append-only Decision·DemoSession 소유권 적용 |

## 0.2 공고문 교차검증 피드백 판정

검증 근거는 한국관광공사 [공식 투어라즈 공고 페이지](https://touraz.kr/announcementList/pssrpView?curPage=2&pssrpSeqEnc=wY3RgFjvo8lP8yzPYIomsg%3D%3D)와 첨부된 `2026 관광데이터 활용 공모전(웹·앱 구현 부문) 공고문.pdf`다. 공개 공고에 없는 개별 안내는 팀이 실제 수신한 원문을 근거대장에 등록하기 전까지 확정 사실로 쓰지 않는다.

| 피드백 | 전수 검토 판정 | 문서 반영 |
|---|---|---|
| `2026-09-21 16:00` 마감 근거 | **타당:** 공개 공고는 서비스 개발 `7월 말~9월`, 1차·최종심사 `10월 중`, 세부 일정은 대상자별 별도 안내라고만 명시 | 고정일 삭제, 미확정 `D_submit`으로 전환; 수신 공문·메일·공지 URL과 확인자를 기록하기 전 일정 확정 금지 |
| 테스트 계정의 특정 ID·비밀번호를 공식 형식으로 단정한 부분 | **타당:** 공개 공고에서 지정 형식을 확인할 수 없음 | 구체 자격증명 문자열 삭제; 제출 시스템의 최신 필드·별도 안내를 캡처한 뒤 테스트 방식을 확정 |
| 1차·최종 심사 공식 명칭 | **타당:** 기존 배점은 맞지만 명칭·세부 평가요소 보완 필요 | `서비스 구현성(과제 연계기능 완결성·구동 안정성·완성도)` 등 공식 용어와 PT 30·30·25·15 반영 |
| 지정과제 2 공간 분산 가시성 | **타당:** 예시 서비스는 의무가 아니지만 과제 부합 기획과 완결된 연계기능은 필수 | 기존 동일 서울 Live 그룹 비교를 화면의 `과밀지역 우회 보기`로 승격하고 `#과밀지역우회`를 기능설명서 핵심 해시태그로 사용 |
| 중복 출품·수혜 이력·팀원 가입 | **타당·필수:** 공고 유의사항과 참가신청 절차에 직접 명시 | 제출 전 Eligibility Gate, 팀원 서약·검색·회원가입 확인, SUB-00 티켓 추가 |
| `Nullnull` 오인 가능성 | **타당한 커뮤니케이션 리스크:** 명칭 변경 사유는 아님 | 팀 승인 후 표지·About에 네이밍 의도 1문장 추가 |
| Instagram·X 실명 제출물 사용 | **타당한 상표·오인 방지 조치:** 내부 비교 설명은 유지 가능 | 사이트·PDF·제출 시스템에는 `세로형 소셜 피드`로 통일하고 타사 로고·제휴 암시 금지 |
| `2026-08-24 기준` 문구 노후화 | **타당:** 검증일만 있고 갱신 책임·주기가 없음 | Source Registry에 owner·verified_at·next_review_at·evidence를 추가하고 주 1회 및 `D_submit-2영업일` 재검증 |
| 실행 리스크·영어 P0 강등 | **부분 재판정:** 파싱 범위 고정은 유지. 1차 타깃이 방한 외국인이므로 영어를 P1 발전계획으로 미루면 타깃이 서비스를 쓸 수 없으나, 두 언어 동시 개발은 QA 부담이 큼 | **한국어로 P0 완성 → 안정화 후 영어 추가**의 2단계. 설계 단계부터 영어 문자열 길이를 전제. EngService2·한영 매핑은 P1 유지 |

공간 분산 리스크의 **문제 진단은 전면 수용하되, S10 후보를 기계적으로 1곳에서 3곳으로 늘리는 안은 채택하지 않는다.** 관광공사의 연관 관광지 관계만으로는 후보 간 실시간 혼잡 비교나 실제 이동 가능성을 보장할 수 없기 때문이다. 대신 같은 서울 Live 소스·범위·정규화 버전에서 비교 가능한 S11을 과제 연계 주 흐름으로 삼고, S10의 검수 후보 1곳은 주변 체류를 보강하는 보조 기능으로 유지한다.

## 0.3 실행 병목 후속 판정

| 병목 | 판정 | v5.6 결정 |
|---|---|---|
| 집중률 예측의 누락·평평한 날짜 분포 | **최우선·타당:** 데이터가 있어도 날짜 차이가 없으면 시간 분산 E2E가 성립하지 않음 | 후보 POI 10개 이상을 매일 사전 점검하고, horizon 안에서 실제 Level 차이와 제약 Gate를 통과한 `DEMO_READY` 5개 이상을 Submission Gate로 설정 |
| Live 우회 CTA의 현재 혼잡 의존 | **최우선·타당:** 심사 시각의 우연한 상태가 공간 분산 E2E를 막음 | `LIVE_ABSOLUTE`·`LIVE_RELATIVE`·`REPLAY`를 구분한다. 상대 비교는 과밀로 부르지 않고, 재생은 `과거 관측·실시간 아님`을 고정 표시 |
| 한·영 자유 텍스트 파싱 범위 | **타당:** 공수·불확실성 대비 심사 증거 기여가 낮음 | P0는 한국어 지원 형식 8종, 규칙·장소사전, 직접 입력·확인 화면으로 동결; 상대 날짜·고급 NER/LLM·영문 파싱은 P1 |
| 영어 UI P0 | **부분 재판정:** E2E 안정성 우선은 타당. 다만 무거운 것은 EngService2 데이터 검수·한영 POI 매핑이지 UI 문자열 번역이 아니며, 1차 타깃이 외국인이므로 영어를 완전히 배제할 수 없음 | **한국어 우선 2단계.** 핵심 E2E를 한국어로 완성·검증한 뒤 영어 리소스 추가. 설계는 처음부터 영어 길이를 전제. EngService2·한영 매핑은 P1 유지 |
| 공공데이터포털 쿼터·운영계정 | **타당:** 실호출·짧은 캐시 구조에서 심사주간 가용성에 직접 영향 | API별 승인량·신청번호를 근거대장에 기록하고 즉시 운영계정/증설 신청; 정상 예상량은 승인량의 50% 이하, 30% 이상을 심사 예비량으로 보존 |

---

# 1. 제품 정의

## 1.1 문제 정의

오버투어리즘의 핵심은 관광객 총량만이 아니라, 많은 여행자가 비슷한 SNS 콘텐츠를 보고 같은 장소·날짜·시간·동선으로 선택을 동기화하는 데 있다.

`인기 콘텐츠 노출 → 방문 집중 → 게시물 증가 → 더 큰 노출 → 더 큰 집중`

Nullnull은 혼잡도 표나 일정 최적화 도구에서 시작하지 않는다. Home의 첫 화면은 Instagram·X처럼 이미지·짧은 문구·장소 태그가 이어지는 여행 게시글 Feed다. 특정 서비스의 화면·상표를 복제하거나 제휴를 암시하지 않고, 사용자가 익숙한 세로형 소셜 탐색 문법만 참고한다. 일정이 없는 사용자는 권역·카테고리 다양성을 갖춘 일반 여행 Feed를 보고, P0 지원 형식의 기존 일정을 붙여 넣거나 TXT·CSV·XLSX 파일로 가져오거나 직접 입력한 사용자는 장소·날짜·시간·관심사·Must Visit 후보·변경 가능한 일정·빈 시간을 확인한 뒤 해당 신호로 개인화된 Feed를 본다.

`여행 게시글 발견 → 여행별 후보에 담기 → 일정 편집 또는 AI 최적화 → 변경 전·후 확인 → 사용자 승인 → 확정 일정 반영`

여행 중에는 직접 선택한 장소 또는 현재 Trip의 다음 일정에 매핑된 권역의 `실시간 관측 혼잡`, 현재 날씨, 거리, 다음 일정까지 남은 시간, 사용자가 확정한 일정 제약을 한 화면에서 함께 보여준다. 날씨·거리·일정 Context는 관측된 혼잡값 자체를 임의로 바꾸지 않으며, 가능한 행동 대안을 고르는 근거로만 사용한다. 유명 관광지를 방문하지 말라고 요구하거나 Must Visit을 삭제하지 않는다. **P0 MVP는 후보 보관·수동 편집·같은 목적지 날짜 변경을 완결하고, P1 공모전 확장은 실제 경로와 시간창을 검증한 다일 리밸런싱·사용자 opt-in 대안 교체까지 같은 승인 흐름으로 확장한다.**

## 1.2 제품 목표

1. 기존 여행 일정의 텍스트·TXT·CSV·XLSX·직접 입력값에서 장소, 날짜, 시간, 관심사, Must Visit 후보와 빈 시간을 추출한다.
2. 여행 게시글 중심의 Home을 제공하고, 추출 결과를 사용자가 확인·수정한 뒤 해당 여행에 맞춘 Social For You Feed로 개인화한다.
3. SNS·검색·Live에서 발견한 장소를 `+` 한 번으로 활성 여행의 `담아둔 장소(TripCandidate)`에 저장하고, 확정 일정과 구분한다.
4. My Trip에서 일정 보기·일정 편집·AI 일정 최적화를 명확히 분리하고 Must Visit·날짜 고정·시간 고정을 독립 제약으로 관리한다.
5. 담아둔 장소와 현재 일정의 동일·유사 관계를 이유와 함께 보여주고 사용자가 추가·교체·유지를 선택하게 한다.
6. 보조 Live 탭에서 직접 선택한 장소 또는 현재 여행의 다음 일정 주변을 기준으로 혼잡·날씨·거리·남은 시간을 함께 확인하고, 검수된 더 여유로운 후보를 여행 후보로 담게 한다.
7. P0는 동일 POI 날짜 변경을, P1은 실제 route matrix·시간창을 사용한 날짜·시간·순서·경로·opt-in 목적지 교체를 계산한다.
8. 모든 AI 변경안은 지도·타임라인·전후 지표로 먼저 보여주고, 사용자가 승인했을 때만 원자적으로 실제 일정에 반영하며 되돌리기 이력을 보존한다.

## 1.3 비목표

- 모든 지역의 실시간 혼잡을 제공한다고 주장하지 않는다.
- 오버투어리즘을 완전히 해결한다고 주장하지 않는다.
- 근거가 없는 장기 날씨·시간별 혼잡을 정밀 예측처럼 표시하지 않는다.
- 날씨·거리·행사 신호를 실제 관측 혼잡값에 섞어 새로운 `실시간 혼잡도`인 것처럼 표시하지 않는다.
- P0의 공식 Seed Feed를 사용자 게시·팔로우 그래프가 완성된 SNS처럼 주장하지 않는다.
- 사용자가 원하지 않는 유명 관광지를 **임의로** 삭제하거나 교체하지 않는다. 사용자가 명시적으로 선택한 교체는 이에 해당하지 않는다.
- 공모전 기간 안에 대규모 ML 추천 시스템을 새로 구축하지 않는다.

## 1.4 핵심 가치 제안

### 사용자 가치

- 메인 여행 게시글 Feed를 넘기며 SNS처럼 여행 장소를 발견한다.
- 이미 만든 일정을 다시 작성하지 않고 붙여 넣거나 TXT·CSV·XLSX 파일로 가져와 시작한다.
- 일정에서 읽은 관심사와 추천 근거를 확인하고 직접 고친다.
- 별도 검색과 복사 없이 여행별 후보로 담고, 원할 때만 일정에 배치한다.
- 수동 편집과 AI 최적화를 구분해 자신이 직접 바꾼 값과 시스템 제안을 혼동하지 않는다.
- AI 변경 전·후의 지도·타임라인·이동·혼잡 지표를 확인한 뒤 적용 여부를 결정한다.
- 직접 선택한 현재 여행지 또는 다음 일정과 검수된 다른 관광지의 현재 상태를 같은 화면에서 확인한다.
- 가고 싶은 곳을 유지하면서 덜 붐비는 순간을 찾는다.
- 추천 이유와 데이터 신뢰도를 이해하고 직접 결정한다.

### 관광지·지역 가치

- 같은 관광지의 피크 날짜를 분산한다.
- 혼잡 권역과 같은 비교그룹·이동 범위의 더 여유로운 검수 관광지로 선택지를 넓혀 공간적 집중을 완화한다.
- 유명 관광지 주변의 연관 장소로 체류와 소비를 확장한다.
- 지역 분산 캠페인의 제안·수락·방문 전환을 측정할 수 있다.

## 1.5 핵심 타깃

| 구분 | 정의 | 핵심 요구 |
|---|---|---|
| **1차 사용자** | **방한 외국인 자유여행객** | 한국인이 실제로 가는 장소 발견, 부담 없는 탐색, 쉬운 일정 시작, 신뢰도 높은 혼잡 안내 |
| 2차 사용자 | 국내 자유여행객 | 빠른 탐색, 일정 최적화, 새로운 주변 장소 발견 |
| Creator | 관광지·카페·전시·축제·로컬 경험 게시자 | 장소 연결형 콘텐츠 작성, 발견·저장 전환 |
| 확장 고객 | RTO·DMO·지자체 | 분산 캠페인 운영 및 성과 확인 |

1차 타깃을 외국인으로 두는 이유는 지정과제 2번의 문제 정의와 일치시키기 위해서다. 경복궁·명동·북촌처럼 특정 명소에 쏠림이 발생하는 주된 주체는 방한 외국인 관광객이며, 이들이 참고하는 정보 대부분이 소수의 유명 콘텐츠에 집중돼 있다. 개입 지점과 문제 발생 지점을 같은 사용자에게 맞춘다.

기능설명서와 PT의 현 단계 표기는 `방한 외국인 대상 설계, 현 단계 한국어 검증 버전`으로 통일한다. 서울 Live는 외국인 관광객 수가 아니라 권역 전체 인구 추정 기반의 혼잡 Context이므로, 서비스 타깃과 데이터 측정 모집단을 같은 개념으로 쓰지 않는다.

타깃은 외국인이지만 **구현 순서는 한국어 우선이다.** P0 핵심 E2E를 한국어로 먼저 완성·검증하고, 그 위에 영어 리소스를 얹는다. 처음부터 두 언어를 동시에 만들면 레이아웃 회귀 테스트와 QA가 2배가 되어 구현성 30점의 핵심 흐름이 지연된다.

**단, 설계 단계에서는 처음부터 영어를 전제한다.** 모든 UI 문자열을 번역 키로 관리하고, 배지·버튼·탭은 영어 문자열이 들어와도 깨지지 않는 크기로 설계한다. 나중에 리소스만 추가하면 되도록 만든다. **관광정보 원문은 KorService2(국문)만 사용**한다. 영문 관광정보(EngService2)와 한영 Canonical POI 매핑은 P1에서 E2E 검증 후 공개하며, 일본어·중국어는 P2로 확장한다.

| 범위 | P0 (1단계) | P0 안정화 후 (2단계) | P1 |
|---|---|---|---|
| UI 문구·화면 라벨 | **한국어로 완성** | **영어 리소스 추가** | 일본어·중국어는 P2 |
| 관광정보 원문(POI 상세·소개) | KorService2 국문 | 동일 | EngService2 영문 |
| 일정 파싱 | 한국어 지원 형식 8종 + 언어 무관 직접 입력 | 동일 | 영문 일정 파싱 |

P0에서 영문 POI 상세가 없는 장소는 **잘못 매칭하지 않고** 국문 원문과 함께 `Official description available in Korean only`로 표시한다. 없는 정보를 만들어내지 않는다는 원칙(§1.3)이 언어에도 동일하게 적용된다.

## 1.6 제품 원칙

1. **SNS First:** Home의 기본 화면은 여행 게시글 Feed다. 일정 등록 전에는 다양성 중심 일반 Feed, 등록 후에는 확인된 일정 신호 기반 개인화 Feed를 제공한다. 혼잡도는 별도 목적지가 아니라 발견과 일정 실행을 돕는 Context다.
2. **추론 결과의 통제권:** 추출된 관심사와 Must Visit 후보는 근거와 함께 보여주고 사용자가 확정·수정한다.
3. **Must Visit 보호:** 사용자가 잠근 목적지는 **시스템이 자동으로 교체 제안하지 않는다.** 다만 사용자가 Feed에서 대체 관광지를 직접 발견하고 스스로 교체를 선택하는 것은 허용한다. 보호의 대상은 `사용자의 의사`이지 `장소 데이터`가 아니다.
   - **자동 제안(Push):** Must Visit 제외 — 시스템이 먼저 "여기 대신 저기" 라고 말하지 않는다
   - **Feed 노출(Pull):** Must Visit 포함 — 유사 스타일의 대체 관광지를 Feed에 노출한다
   - **사용자 교체(Choice):** Must Visit 포함 — 사용자가 직접 선택하면 잠금 해제 확인 후 교체한다
4. **후보와 일정 분리:** `+`는 TripCandidate만 생성하며 TripItem을 자동 생성하거나 날짜·시간을 임의 배치하지 않는다.
5. **독립 잠금:** Must Visit은 목적지, 날짜 고정은 날짜, 시간 고정은 시간 차원을 각각 보호한다. 예약·티켓 잠금은 별도 hard constraint다.
6. **작은 변경에서 전체 최적화까지:** P0는 동일 POI 날짜 변경을 ITEM 범위로 제공하고, P1은 `ITEM | DAY | TRIP` 범위에서 날짜 → 시간 → 주변·순서 → 경로 → opt-in 목적지 대체 순으로 확장한다.
7. **설명 가능한 추천:** 모든 추천에 일정에서 사용한 신호, 예상 개선, 데이터 시점과 신뢰도를 표시한다.
8. **사용자 승인:** 자동으로 일정을 바꾸지 않는다. OptimizationProposal은 적용 전 draft이며 승인·재검증 성공 후에만 반영한다.
9. **정직한 상태 표시:** `실시간 관측`, `과거 관측 재생·실시간 아님`, `공식 예측`, `공식 예측 범위 밖 정성 Context`를 명확히 구분한다.
10. **위치 최소 수집:** P0는 장소 직접 선택을 기본으로 하고 GPS 원시 좌표를 서버·지도 사업자에 전송하지 않는다. 기기 내 거리 계산이 검증된 경우에만 선택적으로 사용하며, 서버 전송형 GPS는 위치기반서비스 신고·약관 검토가 끝난 P1 Feature Flag 뒤에 둔다.
11. **실제 데이터 사용:** 심사 핵심 경로에서 한국관광공사 OpenAPI 호출과 화면 활용이 확인돼야 한다.
12. **결정적 사실 검증:** LLM이 영업·혼잡·경로·도착시각을 판단하지 않는다. 검증 가능한 서버 로직과 optimizer가 계산하고 LLM은 선호 해석과 근거 설명에만 사용한다.
13. **Context 분리:** 실시간 혼잡·공식 예측·예측 범위 밖 정성 Context·날씨·거리·일정 제약의 출처와 역할을 분리한다. 여러 신호는 제안 생성에 함께 사용할 수 있지만 서로 다른 원시값을 하나의 관측값처럼 합치지 않는다.

---

# 2. 공모전 관점의 핵심 전략

## 2.1 공식 심사기준별 대응

### 1차 심사 — 서면·기능심사

| 공식 심사항목 | 배점 | 공식 세부요소 | 구현·제출 대응 |
|---|---:|---|---|
| 서비스 구현성 | 30 | 과제 연계기능 완결성·구동 안정성·완성도 | `Feed·Live 발견 → 여행별 후보 저장 → My Trip 수동 편집/AI 최적화 → 지도·시간 비교 → 승인`의 완결 흐름, P0 ITEM 변경과 P1 DAY/TRIP 리밸런싱, 오류·Fallback, 모바일·데스크톱 검증 |
| 서비스 기획력 | 30 | 타당성·구체성·독창성 | SNS가 만든 집중 문제와 시간·공간 분산 개입의 인과관계, Must Visit 보호 원칙 |
| 데이터 활용 적절성 | 20 | 한국관광공사 OpenAPI 활용 필수 | OpenAPI 실제 호출, 화면별 출처, 데이터 시점·범위·신뢰도 구분 |
| 서비스 발전성 | 20 | 지속성·확장성 | 서울 P0에서 전국·다국어·RTO/DMO 협업으로 확장하는 단계별 계획 |

### 최종심사 — PT 발표심사

1차 심사 상위 3개 팀이 최종 PT 대상이다. 10월의 정확한 일시는 대상자별 별도 안내를 근거대장에 등록한다.

| 공식 심사항목 | 배점 | 공식 세부요소 | PT 대응 |
|---|---:|---|---|
| 서비스 적정성 | 30 | 명확성·논리성·시의적절성 | 문제→SNS 집중→데이터 근거→시간·공간 분산 행동의 논리를 1장으로 제시 |
| 서비스 완성도 | 30 | 기능 구현도·데이터 활용성·안정성 | 실제 URL 시연, 상태 변경, OpenAPI 호출·출처, 장애 Fallback 증거 |
| 서비스 실용성 | 25 | 활용성·이용 편의성·지속성 | 일정 재입력 최소화, 30초 행동 전환, 반복 사용·지역 확장 시나리오 |
| 발표 점수 | 15 | 발표 구성·전달 | 5분 이내 핵심 시연, 백업 영상·Q&A·발표자 리허설 |

심사명칭과 세부요소는 제출물에서 임의 축약하지 않는다. 내부 KPI나 개발 용어는 공식 항목 아래의 증거로만 연결한다.

## 2.2 가장 중요한 심사 증거

심사위원이 아래 장면을 직접 확인할 수 있어야 한다.

1. 사용자가 이미 만든 서울 일정을 입력하고 장소·관심사·Must Visit 후보·빈 시간을 추출받는다.
2. 사용자가 추출 결과를 수정·확정하면 해당 근거로 개인화된 SNS Feed가 만들어진다.
3. Feed에서 관광공사 기반 POI 카드를 발견하고 `+`로 특정 여행의 `담아둔 장소`에 저장한다. 이 시점에는 확정 일정이 바뀌지 않는다.
4. Live 탭에서 `LIVE|REPLAY` dataMode, `ABSOLUTE|RELATIVE|NONE` claimMode와 관측시각을 확인한다. 일반 사용자의 능동 우회 CTA는 ABSOLUTE에만 노출하고 RELATIVE는 사용자가 `지금 더 여유로운 곳` 정렬·필터를 직접 켠 경우에만 연다.
5. My Trip의 `일정 편집`에서 담아둔 후보를 열고, EXACT/SIMILAR 근거를 확인한 뒤 추가·교체·유지를 직접 결정한다.
6. Must Visit, 날짜 고정, 시간 고정을 서로 독립적으로 바꾸고 예약에서 온 잠금은 별도 확인 없이 해제되지 않는 것을 확인한다.
7. `DemoScenarioFactory`가 고른 `DEMO_READY` POI의 실제 날짜별 Level 차이를 바탕으로 ITEM Optimization Preview를 연다.
8. P1 시연에서는 `AI로 일정 최적화`를 눌러 보이는 DAY 또는 전체 TRIP의 변경 전·후 지도, 타임라인, 총 이동시간·거리·혼잡 지표를 확인한다. `담아둔 대안도 포함`은 사용자가 켠 경우에만 후보를 교체안에 넣는다.
9. `적용` 전에는 일정이 바뀌지 않으며, 승인 후 change set 전체가 원자적으로 반영되고 `되돌리기`가 동작한다.
10. 각 후보·추천·혼잡·최적화 화면에서 근거, 출처, 기준시각과 데이터 상태가 표시된다.

## 2.3 서비스 핵심 문구

- Hero: `내 일정을 읽고, 취향으로 이어지는 여행 SNS.`
- 보조 문구: `가지고 있는 일정을 붙여 넣으면 관심사와 빈 시간을 찾아, 나에게 맞는 장소와 지금 덜 붐비는 선택을 연결해드려요.`
- 영문 발전 문구(P1): `Turn your itinerary into a social travel feed.`
- Primary CTA: `내 일정 입력하기`
- Secondary CTA: `서울 SNS 데모 보기`

## 2.4 오버투어리즘 개입 논리와 주장 경계

Nullnull의 오버투어리즘 개입은 `혼잡한 곳을 보여준다`에서 끝나지 않고, 추천 노출이 실제 일정 선택으로 이어지는 아래 인과사슬을 제품 안에서 측정하는 것을 목표로 한다.

```text
동일 SNS 콘텐츠의 반복 노출
→ 특정 장소·날짜·시간으로 선택 동기화
→ 일정에서 사용자의 선호·Must Visit·변경 가능 구간 파악
→ 취향은 유지하되 시간·날짜·주변 지역이 다양한 후보 노출
→ 단일 서수 Selector로 더 낮은 후보를 찾고, 절대 주장·on-demand 상대 비교·명시적 과거 관측 재생을 구분해 검수 관광지 선택 제공
→ 발견한 장소를 여행별 후보로 보관하고 기존 일정과 동일·유사 여부를 설명
→ 사용자가 직접 편집하거나, 잠금을 지키는 AI 최적화안을 지도·시간 비교로 검토하고 승인
→ 사용자가 선택한 계획의 공간·시간 피크 노출 감소
→ 동의 기반 방문 확인이 가능해진 뒤 실제 방문 분산 검증
```

효과 주장은 측정 단계에 맞춰 제한한다.

| 단계 | 확인 가능한 것 | 허용 표현 | 금지 표현 |
|---|---|---|---|
| 공모전 P0+P1 | 추천 노출, TripCandidate 저장, 최적화안 승인, 변경된 계획의 CrowdLevel·검증된 이동시간 | `사용자가 승인한 계획의 혼잡 피크 노출과 검증 가능한 이동 부담을 줄였다` | `실제 관광객을 분산했다` |
| 공모전 이후 현장 검증 | 선택적 체크인·사용자 확인 또는 제휴 집계 기반 실제 방문 | `검증 표본에서 시간·지역 이동이 관찰됐다` | 모집단 전체 효과로 일반화 |
| 지역 파트너 검증 | 권역별 노출·계획·방문 분포와 대조군 비교 | `특정 캠페인·기간에서 분산 효과가 확인됐다` | 인과검증 없이 도시 혼잡 감소 주장 |

한 사용자의 일정만 덜 붐비게 만드는 것으로는 지역 전체 오버투어리즘 해결이 충분하지 않다. 따라서 다음 공급 측 안전장치를 함께 둔다.

- P0는 한 응답 안의 동일 POI·권역 슬롯 상한으로 반복 노출을 막고, P1은 시간창을 넘는 **ExposureBudget과 포화 중단**으로 확장한다.
- `숨은 명소`를 무제한 확산하지 않고 혼잡 악화, 주민 생활권, 수용력, 운영시간, 예약 가능성을 확인한다.
- 민감 주거지·보호구역·운영 중단 장소는 후보에서 제외하거나 DMO 검수 대상으로 둔다.
- POI·권역·작성자 다양성을 랭킹 단계와 최종 재정렬 단계에서 각각 보장한다.
- 혼잡이 낮다는 이유만으로 먼 이동을 유도하지 않는다. P0는 일정 시각 충돌·확인 가능한 운영 제약·직선거리 상한을 통과시키고, 실제 이동시간을 검증한 것처럼 표현하지 않는다. 경로 API가 있는 P1에서 이동시간·탄소를 추가한다.

---

# 3. 개발 범위와 우선순위

## 3.1 P0 MVP - 먼저 완성할 제출 Gate

1. 심사용 데모 진입 또는 테스트 계정 로그인
2. 한국어 지원 형식 일정 붙여넣기, `.txt`·`.csv`·`.xlsx` 파일 가져오기 또는 직접 입력 폼, **그리고 일정이 없는 사용자를 위한 지역·날짜·관심사 최소 입력 경로**
3. 지원 형식에서 장소·날짜·관심사·Must Visit 후보·Flexible 일정·빈 시간 초안 생성과 사용자 확인·보정; 최소 입력 경로는 사용자가 직접 고른 값으로 동일한 Trip Context를 구성
4. 여행 게시글 형태의 한국관광공사 공식 Seed 카드 20개 이상, 일정 미등록 일반 Feed와 일정 등록 후 Social For You Feed
5. **S11 장소 상세**(시트 전면 교체)와 Feed·상세·Live의 `+ → TripCandidate` 저장
6. My Trip 일정 보기·수동 편집, 담아둔 장소 패널, Must Visit·날짜 고정·시간 고정의 독립 수정
7. 보조 Live 탭: 장소 직접 선택 또는 다음 일정 기준의 서울 Live 권역 확인; 일반 사용자 능동 CTA는 ABSOLUTE 주장일 때만 노출하고, RELATIVE는 `다른 관광지` 탭의 사용자 직접 정렬·필터에서만 열며, REPLAY는 `과거 관측·실시간 아님`을 고정 표시해 같은 비교그룹·수집 배치의 검수 후보로 연결
8. **혼잡 예보**: S11 상세의 3구간 그래프(오늘 시간대별 / 이번 주 / 다음 달)로 동일 POI 날짜별 상대 혼잡 비교
9. 날짜 중심 S09 `Optimization Preview(scope=ITEM)` 제공
10. 최적화안 적용·유지·최신 Trip version 재검증·원자적 일정 반영·되돌리기
10-B. **담아둔 유사 장소 → 기존 일정 항목 교체**(사용자 선택). Must Visit은 잠금 해제 확인 후 허용
11. 핵심 POI에 검수된 주변 연관 관광지 최소 1개 제안; 별도 범용 탐색 화면은 만들지 않음
12. **한국어 핵심 경로 UI**와 공식 관광정보, 번역 키 기반 문자열 구조; **영어 리소스는 핵심 E2E 안정화 후 추가**하되 레이아웃은 처음부터 영어 길이를 견디게 설계
13. 데이터 상태·출처·기준시각·Fallback 표시
14. 핵심 분석 이벤트와 OpenAPI 호출 로그
15. 시간 예측 후보은행 10개 이상·`DEMO_READY` 5개 이상, 공간 CandidateRelation 검수 앵커 3개·후보 관계 5개 이상·최소 2개 앵커 분산, 7일 이내 완결 REPLAY와 `/api/demo/readiness`
16. API별 운영계정/증설 신청 근거, 승인량 대비 예상량 50% 이하, 심사 예비량 30% 이상과 quota Fallback

## 3.2 P1 공모전 확장 - P0 Gate 통과 후 이어서 구현

- `.xls`·`.xlsm`, 이미지·PDF·예약 URL 일정 불러오기와 OCR
- 지역·날짜·관심사·Must Visit만으로 여러 날의 전체 여행 일정을 자동 생성하는 기능
- 상대 날짜·비정형 문장·영문 일정 파싱과 고급 개체명·의도 추론
- EngService2 영문 관광정보·한영 Canonical POI 매핑·영문 관광정보 검수 (UI 문구 영어화는 P0)
- Live 악화 기반 자동 감지·알림·Re-plan 제안
- GPS 좌표를 서버 또는 지도 사업자에 전송하는 위치 기반 조회
- 범용 지도·경로·보행·대중교통 ETA와 경유 순서 자동 변경
- `OptimizationRun(scope=DAY|TRIP)`과 실제 route matrix 기반 전후 지도·타임라인·이동시간·거리 비교
- `담아둔 대안도 포함` opt-in과 Flexible 항목의 검증된 목적지 교체
- D-3 이내 시간 변경 제안
- 단기·중기 날씨 기반 실내·야외 Swap
- 독립 Search 화면과 범용 지도 탐색
- 기본 게시물 작성·신고·숨김
- 알림센터와 `혼잡 예측이 열렸습니다` 재계산 알림
- 트래픽 기반 전역 ExposureBudget 카운터; P0는 한 응답 안의 POI·권역 슬롯 상한으로 대체

P1은 발전계획 문구만 남기는 범위가 아니다. P0 회귀 Gate가 유지되는 동안 Feature Flag 뒤에서 순차 구현하고, 공모전 최종 배포·기능설명서에는 실제 E2E를 통과한 P1 기능까지 포함한다.

## 3.3 P2 - 공모전 사이트에서는 숨겨도 되는 범위

- 전국 실시간 혼잡
- 일본어·중국어 완성 UI
- 자동 UGC 번역
- 팔로우 그래프 기반 고급 추천
- ML 랭킹·임베딩 서비스
- Creator 수익화
- RTO·DMO 운영 대시보드
- 네이티브 앱과 푸시 고도화

## 3.4 기능 노출 원칙

- 구현이 끝나지 않은 메뉴는 `준비 중`으로 남기지 않고 숨긴다.
- 기능설명서에는 최종 URL에서 직접 확인되는 기능만 적는다.
- P1 기능이 불안정하면 P0 데모에서 접근되지 않도록 Feature Flag를 끈다.
- 모든 핵심 기능은 새 브라우저 세션에서도 재현돼야 한다.

## 3.5 P0 Submission Gate Vertical Slice와 중단 기준

P0 목록 전체를 동시에 넓히지 않는다. 먼저 아래 하나의 수직 흐름을 완성하고 통과한 뒤 범위를 확장한다.

1. 한국어 지원 Fixture 일정 1개를 분석하거나, 직접 입력 또는 **지역·날짜·관심사 최소 입력**으로 시작하고 사용자가 결과를 수정·확정한다.
2. 검수된 공식 Seed 카드 20개로 개인화 Feed를 만든다.
3. 핵심 POI 5개가 **S11 상세**·`+ → TripCandidate`·My Trip 담아둔 장소에 연결된다.
4. 검수된 서울 Live 앵커 3개와 CandidateRelation 5개 이상을 확보하고 관계가 최소 2개 앵커에 분산됐는지 확인한다. 같은 비교그룹·수집 배치·관측시각 편차 Gate에서 단일 서수 Selector로 더 낮은 후보를 열어 POI 상세·TripCandidate 저장까지 연결한다.
5. `DEMO_READY` POI의 실제 날짜별 Level 차이와 실행 가능한 날짜 변경안 1개를 보여준다.
6. 담아둔 장소를 S07 편집에서 배치하고, S09 변경안 적용·되돌리기 후 Trip·S11 혼잡 예보·S07 상태가 일관되게 갱신된다.

다음 중 하나라도 충족되지 않으면 P1 기능을 숨기고 위 흐름을 우선 복구한다.

- 고정된 제출 Gate 회귀 테스트 세트의 핵심 E2E 성공률 100%
- 외부 API 실패 시 Fallback 성공
- 공식 출처·상태·기준시각 표시
- Optimization 적용의 원자성·중복 방지·되돌리기
- 모바일 360px과 데스크톱 핵심 흐름 완료
- 시간 `DEMO_READY >= 5`, 앵커 분산 CandidateRelation, 7일 이내 완결 REPLAY, API quota Gate가 `/api/demo/readiness`에서 모두 통과

---

# 4. 핵심 사용자 흐름

## 4.1 기본 핵심 루프

`일정 입력·불러오기 → 관심사·Must Visit·빈 시간 추출 → 사용자 확인 → Social For You → POI 발견 → +로 담아두기 → My Trip 편집 또는 AI 최적화 → 변경 전후 Preview → 승인 → 변경된 일정`

당일 보조 루프는 `Live → 장소 직접 선택 또는 다음 일정 기준 → 현재 여행지 / 다른 관광지 확인 → POI 상세 → 후보로 담기 또는 오늘 일정 최적화 → Preview → 승인/유지`로 구성한다.

## 4.2 심사용 대표 시나리오

### 시나리오 A - 일정에서 취향을 읽어 SNS Feed 생성

1. 데모 세션이 실행일 `D0`를 기준으로 `D+7 경복궁, D+8 성수 카페, D+10 인사동 산책`이 포함된 기존 일정을 만든다. 실제 화면과 API에는 계산된 ISO 날짜를 사용한다.
2. 시스템이 `역사·문화·음식·산책`, `경복궁 Must Visit 후보`, `성수·인사동 Flexible`, `D+9 빈 시간`을 추출한다.
3. 사용자가 잘못 추출된 관심사를 수정하고 경복궁만 Must Visit으로 확정한다.
4. Feed가 이 신호를 근거로 역사·정원·로컬 음식 콘텐츠를 우선 노출한다.
5. 각 카드에서 `일정의 역사·산책 관심사와 잘 맞아요`처럼 추천 이유를 확인한다.

### 시나리오 B - Must Visit 날짜 분산

1. 사용자가 3박 4일 서울 여행을 생성한다.
2. Feed에서 유명 관광지 A를 발견하고 `+`로 해당 여행의 담아둔 장소에 저장한다.
3. My Trip 편집에서 A를 일정에 배치하고 `Must Visit`으로 잠근다.
4. `DemoScenarioFactory`가 후보은행에서 고른 `DEMO_READY` POI를 사용하며, 실행일 기준 여행 기간 안에서 현재 선택일과 더 여유로운 대체일의 실제 API 응답 Level이 표시된다.
5. 개선 가능한 대체일이 있을 때만 시스템이 목적지 교체가 아닌 날짜 변경을 제안한다.
6. 사용자가 수락하면 A가 해당 대체일로 이동한다.
7. 화면에 실제 응답으로 계산한 변경 전·후 Level과 변경 완료가 표시된다. 세션 생성 직전 편차가 사라지면 다른 `DEMO_READY` POI로 재선택하며, 후보가 5개 미만이면 데모 readiness 실패로 제출을 막는다. 일반 사용자에게만 개선일 없음 시 `현재 일정 유지`를 보여준다.

### 시나리오 C - 과밀지역 우회(P0 공간 분산)

1. 사용자가 Live 탭에서 현재 장소를 직접 선택하거나 현재 여행의 다음 일정을 기준으로 고른다.
2. `현재 여행지`에서 CrowdLevel, 현재 날씨, 거리, 다음 일정까지 남은 시간, 갱신시각이 포함된 목록을 확인한다.
3. 시스템은 같은 `comparison_group_id`·`snapshot_set_id`에서 `candidate.sourceOrdinal < anchor.sourceOrdinal`인 후보를 단일 `ORDINAL_LOWER` Selector로 찾는다.
4. 앵커가 `absolute_claim_min_ordinal` 이상이면 `과밀지역 우회 보기` 능동 CTA를 표시한다. RELATIVE는 능동 CTA를 만들지 않고, 사용자가 `다른 관광지` 탭에서 `지금 더 여유로운 곳` 정렬·필터를 직접 켰을 때만 결과를 보여준다.
5. 후보는 `candidate.sourceOrdinal <= 2`, 더 큰 서수 개선폭, 일정 적합성·거리·접근성, 검수 품질·최근성 순으로 정렬한다.
6. 현재 Live가 평평하거나 유효쌍이 없어도 데모는 Source Registry 검증·전체 E2E·신선도 Gate를 통과한 대표 REPLAY 세트를 사용하고 `과거 관측 재생 · 실시간 아님 · 관측시각`을 화면 상단에 고정한다.
7. 시스템은 3km 이내·중심점 0.5km 이상이며 권역 중첩·운영·접근성·민감지역·수용력·검수 Gate를 통과한 CandidateRelation만 보여준다.
8. 실시간 미지원 관광지는 Forecast·정성 Context 별도 그룹에 두고 우회 우열의 근거로 사용하지 않는다.
9. 사용자가 후보의 범위·출처·거리·일정 적합성을 확인한 뒤 POI 상세로 이동하거나 `+`로 담아둔 장소에 저장한다. 실제 일정 배치는 S07 편집 또는 승인된 S09 변경안에서만 수행한다.
10. 현재·REPLAY 모두에서 적합 후보가 없으면 억지 추천 없이 `비교 가능한 우회 후보 없음`으로 끝내고 Submission Gate를 통과시키지 않는다.

### 시나리오 D - 전체 일정 AI 리밸런싱(P1)

1. 사용자가 My Trip 전체 화면에서 `AI로 일정 최적화`를 선택한다.
2. 시스템은 현재 보이는 TripItem과 독립 잠금·예약·운영·접근성·혼잡·route matrix를 Trip version Snapshot으로 고정한다.
3. 기본안은 현재 장소만 날짜·시간·순서·경로를 조정한다. 사용자가 `담아둔 대안도 포함`을 켠 경우에만 Flexible 항목의 유사 후보 교체를 탐색한다.
4. S09에서 변경 전·후 지도, 타임라인, 이동시간·거리, 피크 혼잡 노출, 유지된 잠금을 함께 보여준다.
5. 사용자가 전체 변경을 승인하면 최신 Snapshot을 재검증해 원자적으로 반영하고, 유지하면 원래 일정을 보존한다.

### 시나리오 E - Live Re-plan(P1)

1. 사용자가 서울 실시간 지원 장소를 직접 선택한다.
2. 같은 서울 Live 권역·mapping version의 유효한 관측 시계열에서 혼잡 단계가 사전 검증한 임계값을 넘어 악화된다.
3. 시스템은 `30분 뒤 방문`, `주변 실내 POI 먼저 방문`, `담아둔 유사 후보 포함` 중 검증 가능한 당일 최적화안을 생성한다.
4. S09 지도·타임라인 Preview에서 수락한 경우에만 일정이 변경된다.

## 4.3 최초 진입부터 핵심 기능까지

- 신규 사용자(일정 있음): 일반 Seed Home Feed → `내 일정에 맞추기` → 일정 분석·확인 → 개인화 Feed
- 신규 사용자(일정 없음): 일반 Seed Home Feed → `내 일정에 맞추기` → 지역·날짜·관심사 선택 → 개인화 Feed
- 비로그인 데모: 일반 Seed Home Feed → `서울 SNS 데모 보기` → 롤링 예시 일정의 실제 분석·확인 → 개인화 Feed
- 테스트 계정: 로그인 → 복원 가능한 서울 데모 여행 → Feed 또는 My Trip
- Live 확인: 모든 핵심 화면 하단 `Live` 탭 → 장소 직접 선택 또는 다음 일정 기준
- 후보 저장: Feed·상세·Live의 `+` → 활성 여행 확인 → 담아둔 장소 저장 → 계속 보기 또는 일정에 배치
- 수동 수정: My Trip → `일정 편집` → 담아둔 장소 추가·교체와 독립 잠금 수정 → 저장
- AI 수정: My Trip → `AI로 일정 최적화` → ITEM·DAY·TRIP 범위 확인 → S09 Preview → 적용·유지
- 데모는 최대 2번 클릭, 신규 일정 분석은 확인 단계를 포함해 최대 3단계로 Feed에 도달

## 4.4 제출 Gate Happy Path

### 발견 경로 — 60초 이내

`S00 내 일정 입력 → S02 붙여넣기 → 추출 결과 확인·수정 → S03 개인화 Feed → 카드 + → S06 담아둔 장소 저장`

- S02에서 분석 결과를 전부 채우게 요구하지 않고 `확인 필요` 항목만 먼저 보여준다.
- Feed 첫 화면에는 일정 근거가 분명한 카드와 빈 시간에 맞는 카드가 최소 1개씩 있어야 한다.
- 로그인은 발견 전에 강제하지 않고 저장·동기화가 필요한 시점에 선택하게 한다.

### 수동 배치·시간 분산 경로 — 후보 저장 후 30초 이내

`후보 저장 → 일정에 배치하기 → S07 날짜·시간·잠금 확인 → 실행 가능한 ITEM 제안 → S09 비교 → 수락/유지`

- S06은 후보 저장만 완료하고, 사용자가 `일정에 배치하기`를 선택한 경우에만 날짜·시간 입력과 혼잡 인사이트를 연다.
- 사용자가 화면을 차례로 찾아가지 않아도 **배치 완료 인라인 인사이트** 또는 **My Trip 카드 인라인 제안**에서 S09에 바로 도달한다.
- 일반 사용자는 제안이 유효하지 않거나 개선폭이 없으면 `현재 일정 유지`로 끝낸다. DemoSession은 이 상태를 성공 시나리오로 사용하지 않고 다른 `DEMO_READY` 후보를 선택한다.

### 전체 일정 최적화 경로 — P1 공모전 확장

`S07 AI로 일정 최적화 → 현재 화면 범위·잠금·대안 포함 여부 확인 → OptimizationRun → S09 지도·타임라인·지표 비교 → 전체 적용/현재 일정 유지`

- 날짜 화면은 DAY, 전체 화면은 TRIP 범위를 기본값으로 사용하고 실행 전 scope를 표시한다.
- `담아둔 대안도 포함`은 기본 OFF이며 켠 경우에도 Must Visit·예약 잠금을 침범하지 않는다.
- 경로 제공자 데이터가 없거나 stale이면 이동시간 개선을 주장하지 않고 해당 최적화 차원을 제외하거나 `확인 필요`로 표시한다.
- 적용 직전 Trip version과 source snapshot이 달라지면 새 Preview를 생성한다.

### 공간 분산 경로 — Live 기준 장소 선택 후 30초 이내

`Live/REPLAY 선택 → 기준 장소 → ABSOLUTE CTA 또는 사용자 직접 RELATIVE 정렬 → 비교 가능한 후보 → POI 상세 → 담아두기/유지 → 필요 시 오늘 일정 최적화`

- `우회 후보`는 같은 서울 Live `comparison_group_id`·`snapshot_set_id`·유효 신선도와 CandidateRelation Gate를 모두 통과해야 한다.
- `dataMode`, `claimMode`, `ctaPolicy`를 분리한다.
  - `LIVE + ABSOLUTE + PROACTIVE`: 화면 `비슷한데 덜 붐비는 곳` / 제출물 `과밀지역 우회`
  - `LIVE + RELATIVE + ON_DEMAND_ONLY`: 일반 사용자 능동 CTA 숨김. `다른 관광지` 탭의 사용자 직접 정렬·필터에서 `지금 더 여유로운 곳`만 표시
  - `REPLAY + NONE + DEMO_ONLY`: `과거 관측·실시간 아님`
- 후보 카드의 병렬 전후 상태는 비교 가능할 때만 표시한다. 비교 불가이면 화살표·두 Level·우위 문구를 제거하고 각 장소의 자체 상태 칩과 출처만 표시한다.
- 비교 가능한 후보가 없으면 Forecast·정성 Context를 우회 순위에 섞지 않고 `현재 조건에서 추천할 우회 후보가 없어요`로 끝낸다.
- Live는 Trip이 없는 사용자도 쓸 수 있으며, 후보 저장 시점에만 여행 선택·생성을 요청한다.

---

# 5. 정보구조와 라우팅

## 5.1 모바일 내비게이션

- P0 하단 탭: `Home / My Trip / Live / Profile`
- Search가 안정화되면 `Home / Search / My Trip / Live / Profile`로 확장한다.
- 중앙 FAB: `Create Post` - P1 완료 시만 노출
- Live는 P0 핵심 탭이므로 숨기지 않는다. 실시간 데이터 장애 시에도 직접 장소 선택과 Forecast·정성 Context 별도 그룹으로 화면 골격을 유지한다.

## 5.2 데스크톱 내비게이션

- P0 좌측 사이드바: Home, My Trip, Live, Profile
- Search가 안정화되면 Search를 추가한다.
- `Create Post`는 독립 액션 버튼
- 주요 콘텐츠는 최대 너비를 제한하고 우측에 현재 여행 요약을 선택적으로 표시

## 5.3 화면·URL 목록

| ID | 화면 | 권장 URL | 우선순위 |
|---|---|---|---|
| S00 | Home Feed Shell·데모 진입 | `/` | P0 |
| S01 | 로그인 | `/login` | P0 또는 생략 |
| S02 | 일정 입력·분석·관심사 확인 | `/start` | P0 |
| S03 | Social For You Feed | `/` (`/feed`는 `/`로 redirect) | P0 |
| S04 | 검색·탐색 | `/search` | P1 |
| S05 | ~~POI 상세~~ → **S11 Live 상세로 통합** | — | **삭제 (v5.8)** |
| S06 | 내 여행에 담기·즉시 배치 | Bottom Sheet 또는 Modal | P0 |
| S07 | My Trip 보기·편집·AI 최적화 진입 | `/trip/:tripId` | P0/P1 |
| S08 | ~~Crowd Calendar~~ → **S11 혼잡 예보 + S07 인라인 제안으로 분산** | — | **삭제 (v5.8)** |
| S09 | AI 일정 최적화 Preview (`ITEM | DAY | TRIP`) | `/trip/:tripId/optimizations/:runId` | P0/P1 |
| S10 | 검수된 연관·유사 경험 | S07 담아둔 장소·S09 Preview 인라인, `/nearby/:poiId`는 P1 | P0 제한/P1 |
| S11 | 실시간 주변·관광지 혼잡도 | `/live` | P0 |
| S12 | 알림센터 | `/notifications` | P1 |
| S13 | 게시물 작성 | `/posts/new` | P1 |
| S14 | Profile·설정 | `/profile` | P0 최소 |
| S15 | 데이터 안내·출처 | `/about-data` | P0 |

---

# 6. 공통 화면·콘텐츠 규칙

## 6.1 데이터 상태 라벨

| 상태 | 표시 문구 | 사용 조건 | 금지 표현 |
|---|---|---|---|
| Live | `실시간 관측` | 실제 실시간 제공 지역·최신 응답 | 지원 밖에서 `실시간` 사용 |
| Forecast | `공식 혼잡 예측` | 관광공사 예측 범위 안 | 확정 혼잡처럼 표현 |
| Context | `공식 혼잡 예측 범위 밖` | 공식 예측 범위 밖이며 검증된 달력 소스로 주말·공휴일 여부만 설명 | CrowdLevel·숫자 막대·과거 패턴 기반 혼잡값 생성 |
| Delayed | `업데이트 지연` | 최신 데이터 호출 실패, 유효 캐시 사용 | 오류 숨김 |
| Unavailable | `현재 데이터 없음` | 캐시도 없고 대체 근거도 없음 | 임의 점수 생성 |

## 6.2 CrowdLevel 표현

| Level | 문구 | 기본 색상 의미 | 접근성 보조 |
|---:|---|---|---|
| 1 | 매우 여유 | 가장 낮은 혼잡 | 숫자와 문구 병기 |
| 2 | 여유 | 낮은 혼잡 | 숫자와 문구 병기 |
| 3 | 보통 | 중간 | 숫자와 문구 병기 |
| 4 | 혼잡 | 높은 혼잡 | 숫자와 문구 병기 |
| 5 | 매우 혼잡 | 가장 높은 혼잡 | 숫자와 문구 병기 |

색상만으로 상태를 전달하지 않는다. 모든 카드와 차트에는 숫자 또는 텍스트를 함께 표시한다.

서울 Live 공간 비교는 위 공통 5단계 숫자를 새로 만들지 않고 원천 4단계 `여유/보통/약간 붐빔/붐빔`과 `sourceOrdinal=1~4`를 사용한다. 공통 5단계는 호환 표시용일 뿐 공간 개선폭 계산에 사용하지 않는다.

## 6.3 출처 표시

한국관광공사 데이터를 사용한 화면에는 다음 중 하나를 텍스트로 표시한다.

- `출처: ⓒ한국관광공사`
- `출처: ⓒ한국관광콘텐츠랩`

`TourAPI`만 단독으로 쓰지 않으며, 공식 CI·BI 로고 이미지를 임의로 사용하지 않는다. 이미지별 별도 저작권·출처 조건이 있으면 해당 메타데이터도 함께 노출한다.

## 6.4 추천 카드 공통 요소

- 변경 종류: P0 `날짜`(자동 제안) · `장소 교체`(사용자 선택); P1 `시간 / 주변 POI / 경유`
- 비교 가능한 경우의 변경 전·후 값. 교차 POI 비교 불가이면 화살표·병렬 Level·우위 문구를 모두 제거
- 시간축은 같은 POI·같은 예측 발행 회차, 공간축은 같은 비교그룹·같은 수집 배치에서만 전후 상태 표시
- 추천 이유
- 데이터 상태와 기준시각
- 신뢰도: 높음 / 보통 / 낮음
- `수락` / `유지할래요`
- 적용 전에는 실제 일정에 반영하지 않음

## 6.5 로딩·빈 상태·오류 상태

- Skeleton을 사용하고, 무한 로딩은 8초 이내 오류 상태로 전환한다.
- 빈 Feed: 관심 카테고리 기반 Seed 카드 제공
- 빈 Trip: `Feed에서 장소를 담아보세요` CTA
- 예측 없음: `공식 혼잡 예측 범위 밖`과 출처 있는 주말·공휴일 정성 배지 또는 데이터 없음으로 구분
- API 실패: 마지막 업데이트 시각과 사용 중인 대체 상태 표시
- 저장 실패: 기존 값을 유지하고 재시도 제공

## 6.6 반응형·접근성

- 모바일 360px부터 데스크톱 1440px까지 핵심 흐름 지원
- 키보드만으로 로그인, 여행 후보 저장, 일정 편집, 최적화안 적용·유지 가능
- 이미지 대체 텍스트, 폼 Label, 포커스 표시 제공
- 차트는 표 형태의 대체 정보 제공
- P0 한국어 레이아웃이 깨지지 않고, 문자열이 화면 코드에 직접 박히지 않은 번역 키 구조로 관리됨
- **영어 문자열이 한국어보다 길어지는 경우(버튼·배지·탭)에도 줄바꿈·잘림이 발생하지 않도록 1단계부터 설계**한다. 영어 리소스는 나중에 추가하되 레이아웃은 미리 견디게 만든다

## 6.7 Live 화면 표시 원칙

- `실시간 관측`은 Live 지원 권역이며 최신성 기준을 통과한 응답에만 표시한다.
- `REPLAY`는 `과거 관측 재생 · 실시간 아님`과 원 관측시각을 Header·카드·상세에 반복 표시하고 `실시간 관측` 색상·배지를 사용하지 않는다.
- `claimMode=RELATIVE`는 `현재 비교군 내 상대 수준`으로 표시하며 `과밀·붐빔`이라고 부르지 않는다. 일반 사용자 능동 CTA는 숨기고 사용자가 `다른 관광지` 탭의 `지금 더 여유로운 곳` 정렬·필터를 직접 켠 경우에만 표시한다.
- Live Area가 개별 관광지가 아니라 권역 데이터이면 `경복궁 일대 실시간`처럼 범위를 함께 표기한다.
- `현재 여행지`·`다른 관광지` 결과에 Live 미지원 관광지가 섞일 수 있으며, 이 경우 별도 그룹에서 `공식 혼잡 예측` 또는 `공식 혼잡 예측 범위 밖`을 표시한다.
- `다른 관광지` 탭에서도 모든 장소가 실시간인 것처럼 보이지 않도록 카드마다 sourceState를 독립 표시한다.
- 모든 혼잡 카드에는 CrowdLevel 숫자·문구, 기준시각, 데이터 범위, 출처가 포함돼야 한다.
- 실시간 데이터가 오래되면 `업데이트 지연`으로 전환하고 최신 관측인 것처럼 색상만 유지하지 않는다.
- 교차 POI 병렬 상태는 `comparisonEligible=true`일 때만 렌더한다. 불가이면 새 장소 자체 상태 칩·sourceState·출처만 표시한다.
- P1 지도를 켜더라도 마커 색상만으로 혼잡을 전달하지 않고 마커 숫자 또는 목록의 텍스트를 함께 제공한다.

## 6.8 UI 디자인 재량·계약 경계와 시안 반영 절차

UI 디자인 시안 작업이 기획과 병행된다. 기획안의 UI 관련 문장은 아래 세 층으로 구분하며, 판별 기준은 하나다.

**`어떻게 보이는가`는 디자인 재량이고, `무엇을·언제 보여주는가`를 바꾸면 기획안 개정 대상이다.**

### 디자인 재량 층 — 기획안 수정 없이 변경 가능

- 색상 팔레트, 타이포그래피, 카드·칩·시트의 형태, 아이콘, 모션, 여백, 레이아웃 배치
- 동일한 정보·노출 조건을 유지하는 프레젠테이션 선택(Bottom Sheet vs Modal 등)
- 주장 경계(§2.4, §25.6) 안에서의 사용자 문구 톤 다듬기
- 재량 층 변경은 시안 확정 후 기획안의 해당 화면 구성 서술·문구를 사후 동기화한다. 기획안이 형태·배치를 세부 지시한 문장은 디자인 확정본을 우선하고 문서를 따라 고친다.

### UI 계약 층 — 어떤 시안에서도 깨지 않아야 하는 것

아래는 미학이 아니라 데이터 정직성·심사 방어에서 나온 계약이며 §19 테스트와 §21 DoD의 검증 대상이다. 시안이 이 목록과 충돌하지 않는 한 어떤 스타일을 입혀도 기획안 위반이 아니다.

1. 비교 불가 상태(`comparisonEligible=false`)에서 화살표·병렬 Level을 DOM에 생성하지 않는다. 숨김 처리가 아니라 미생성이다.
2. `claimMode=RELATIVE`에 일반 사용자 능동 CTA를 만들지 않는다. `RelativeProactiveCtaViolationCount` 목표 0.
3. REPLAY는 `과거 관측 재생 · 실시간 아님`과 원 관측시각을 Header·카드·상세에 반복 표시하고 `실시간 관측` 색상·배지를 사용하지 않는다.
4. `공식 혼잡 예측 범위 밖` 구간에 숫자 막대·CrowdLevel을 생성하지 않고 검증된 주말·공휴일 정성 배지만 표시한다.
5. 서울 Live는 원천 4단계 상태 문구를 우선하고, Forecast 5단계 Level과 하나의 순위처럼 읽히지 않게 시각적으로 분리한다.
6. 혼잡 예보 3구간을 하나의 연속 그래프로 잇지 않는다.
7. 색상만으로 혼잡·상태를 전달하지 않고 숫자 또는 문구를 병기한다.
8. 출처, 기준시각, 데이터 상태 표시 의무를 생략하거나 시각적으로 소거하지 않는다.
9. Feed·검색·Live의 `+`는 `담아둔 장소` 저장으로 표현하며, 확정 일정에 바로 배치된 것처럼 날짜·시간이나 성공 문구를 만들지 않는다.
10. My Trip의 보기·일정 편집·AI 최적화 모드를 제목과 주 액션으로 구분하고, 수동 저장과 AI 적용을 같은 버튼으로 합치지 않는다.
11. Must Visit·날짜 고정·시간 고정은 독립 조작으로 제공한다. 예약 잠금은 잠금 출처와 해제 확인을 별도로 표시한다.
12. AI Preview에는 변경 전·후 타임라인과 텍스트 변경 목록을 항상 제공한다. DAY/TRIP에서 경로가 검증됐을 때만 지도·거리·이동시간 개선을 표시한다.
13. `담아둔 대안도 포함`은 기본 OFF이며, 적용 전 일정 불변·전체 변경 수·적용/유지 선택·적용 후 되돌리기를 명시한다.

### 개정 필요 층 — 결정 기록 후 기획안 개정

- 화면 통합·삭제·신설(v5.8의 S05·S08 삭제와 같은 유형)
- 진입점 이동: 예) RELATIVE on-demand 트리거를 `다른 관광지` 탭의 `지금 더 여유로운 곳` 정렬·필터가 아닌 다른 UI로 변경
- 계약 표시 요소(관측시각·출처·데이터 상태 등)를 접힘 영역으로 이동하는 등 노출 방식 변경
- 인라인 제안·CTA의 노출 조건 변경

### UI 시안 확정 시 기획안 동기화 절차

**UI 시안이 확정되면 기획안을 시안에 맞춰 수정한다.** 문서가 시안을 제약하는 방향(계약 층)과 시안이 문서를 갱신하는 방향(재량 층)은 위 구분을 따른다.

1. 시안 검수 시 UI 계약 층 13개 항목을 화면별 체크리스트로 확인한다.
2. 반영 커밋은 `화면 구조 변경`과 `문구·스타일 동기화`로 분리한다.
3. 계약 층 또는 개정 필요 층을 건드리는 변경은 §7 화면 명세, §19 테스트, §21 DoD, §16 기능설명서 캡처 대상을 같은 개정에서 함께 갱신한다. v5.8 잔재형 불일치의 재발을 막기 위한 필수 전파 범위다.
4. 기능설명서 캡처(§16)는 최종 배포 UI 기준이므로 스타일 변경 자체는 제출물 제약이 아니다.

---

# 7. 화면별 상세 명세

## S00. Home Feed Shell·데모 진입

### 목적

첫 방문부터 여행 게시글을 보여줘 SNS 제품임을 즉시 이해시키고, 일정 등록·심사 데모로 자연스럽게 진입시킨다. 별도의 풀스크린 마케팅 랜딩이 Feed보다 먼저 나오지 않는다.

### 화면 구성

1. 상단 Header: 로고·`Nullnull`, 여행 선택기; 영어 전환은 P1 E2E가 통과한 뒤 노출
2. 첫 화면부터 보이는 공식 Seed 여행 게시글 카드와 세로 Home Feed
3. 일정 미등록 compact banner: `여행 일정이 아직 없어요`
4. 보조 설명: `일정을 등록하면 날짜와 관심사에 맞춰 Feed가 달라져요.`
5. Primary CTA: `일정 등록하기`
6. Secondary CTA: `일단 둘러보기`; 심사용 보조 CTA는 `예시 일정으로 체험하기`
7. 하단 탭: `Home / My Trip / Live / Profile`
8. 데이터 출처·서비스 안내 링크

### 동작

- Feed 카드 선택은 로그인·일정 없이 **S11 장소 상세**로 이동하고, `+` 시 담을 여행 선택 또는 일정 등록을 안내한다.
- Primary CTA는 로그인 여부와 무관하게 S02로 이동하며 저장 시점에만 로그인 또는 게스트 저장을 결정한다.
- `일단 둘러보기`는 배너를 접고 현재 일반 Feed 탐색을 유지한다.
- 데모 CTA는 로그인 없이 실제 parse·확인을 거쳐 개인화 Feed로 진입한다.

### 상태

- 일반 Feed는 일정·데모 API가 실패해도 유효 Seed 캐시로 유지한다.
- 데모 로딩 실패 시 `데모를 다시 불러오기` 제공
- 모바일·데스크톱 모두 핵심 CTA가 첫 화면 안에 보여야 함

### 완료 기준

- 새 익명 브라우저의 첫 viewport에 실제 여행 게시글 카드가 최소 1개 보임
- 새 익명 브라우저에서 2번 이하 클릭으로 데모 Feed 도달
- Primary CTA에서 일정 입력 화면으로 즉시 이동
- 한국어 핵심 문구와 번역 키 누락이 없음

## S01. 로그인

### 목적

일반 사용자의 일정과 제안 이력을 보존하고, 심사위원에게 안정적인 테스트 상태를 제공한다.

### 화면 구성

- 이메일 또는 ID
- 비밀번호
- 로그인
- `데모로 계속하기`
- 테스트 계정 안내는 제출 시스템에만 제공하고 화면에 비밀번호를 상시 노출하지 않음

### 동작·완료 기준

- 테스트 전용 계정으로 로그인 가능
- 로그인 성공 시 서울 데모 여행·저장 POI·제안 이력이 준비됨
- 여러 심사 사용자가 상태를 변경해도 `데모 초기화`로 복원 가능
- 인증 오류 문구가 한국어로 명확히 제공되고 번역 키로 관리됨

## S02. 일정 입력·분석·관심사 확인

### 목적

개인화 Feed와 혼잡 추천에 필요한 여행 Context를 확보한다. 이미 일정이 있는 사용자는 다시 만들게 하지 않고 추출하며, **일정이 아직 없는 사용자는 최소 입력만으로 시작하게 한다.** 어느 경로든 결과는 확정값이 아니라 편집 가능한 제안으로 보여준다.

1차 타깃인 방한 외국인 자유여행객은 **여행 전에 아직 일정을 만들지 않은 상태로 진입하는 비율이 높다.** 따라서 일정 보유를 전제로 하지 않으며, 일정 없는 경로를 예외가 아닌 **동등한 P0 경로**로 둔다.

### 화면 구성

#### Step 1. 시작 방식 선택

먼저 두 경로 중 하나를 고르게 한다. 어느 쪽도 기본값으로 강조하지 않는다.

**A. 일정이 있는 경우** — `텍스트 붙여넣기` / `파일 업로드` / `직접 입력` 탭

- 붙여넣기 Textarea: 아래 P0 지원 형식으로 작성한 한국어 일정; 지원 범위를 화면에 예시와 함께 표시
- 파일 업로드: `.txt`, `.csv`, `.xlsx`; 확장자와 실제 파일 형식을 함께 검사한다.
  - `.txt`: 붙여넣기와 같은 한국어 텍스트 파서로 전달한다.
  - `.csv`·`.xlsx`: 날짜·시작시간·종료시간·장소·메모·Must Visit 열을 자동 매핑하고, 불확실하면 사용자가 열을 직접 지정한다.
  - `.xlsx`에 시트가 여러 개면 사용자가 대상 시트를 선택한다.
  - 수식·매크로·외부 링크를 실행하지 않고 표시값만 읽는다.
  - 업로드 결과는 즉시 확정하지 않고 Step 2 미리보기로 보낸다.
- 데모 세션에서는 `예시 일정 채우기`를 제공하되 실제 분석 API를 거친다.
- 직접 입력: 지역, 시작일·종료일, 장소·날짜 행 추가 — **언어와 무관하게 동작한다**
- CTA: `일정 분석하기`

**B. 일정이 아직 없는 경우 — P0 필수 경로**

- 지역 선택: 서울(P0 고정), 이후 확장
- 여행 날짜: 시작일·종료일만 선택
- 관심사 Chips 직접 선택: 자연, 역사, 문화, 쇼핑, 음식, 축제, 산책 등
- Must Visit 직접 추가: `가고 싶은 곳이 있나요?` — 선택 입력이며 비워도 진행 가능
- CTA: `이 조건으로 Feed 만들기`
- 이 경로는 파서를 거치지 않고 Step 2의 확인 화면을 **간소화된 형태로** 통과한다. 추출한 값이 없으므로 근거 표시 대신 사용자가 직접 고른 값만 확인시킨다.
- P0에서는 이 입력만으로 여러 날의 전체 추천 일정 초안을 생성하지 않는다. 확인 후 개인화 Feed로 이동하며 전체 일정 생성·TRIP 최적화는 P0 Gate 통과 뒤 P1 공모전 확장으로 구현한다.

**공통 선택 입력** — 이동수단, 동행 인원, 최대 이동시간, 최대 도보시간, 휠체어·유아 동반 등 접근성 조건

**개인정보 안내:** `입력 원문은 분석 후 기본 저장하지 않으며, 확인한 일정 정보만 저장해요.`

경로 B는 입력이 적은 만큼 개인화 근거도 약하다. Feed 카드의 추천 이유는 이 차이를 정직하게 반영해 `선택한 역사 관심사와 맞아요`처럼 **사용자가 직접 고른 값만 근거로** 제시하고, 일정에서 읽었다고 표현하지 않는다.

P0 파서는 범용 자연어 이해를 주장하지 않고 아래 8개 검증 Fixture가 사용하는 형식까지만 지원한다.

1. `YYYY-MM-DD / 시간 / 장소 / 메모` 한 줄 형식
2. `M/D / 오전·오후·저녁 / 장소` 한 줄 형식
3. 날짜 Header 아래 여러 장소 Bullet 형식
4. `10:30` 같은 명시 시각이 포함된 일정
5. `오전·오후·저녁` 시간대만 포함된 일정
6. `꼭·반드시`가 포함된 Must Visit 후보
7. `예약 완료·티켓 구매`가 포함된 잠금 후보
8. 날짜 또는 장소가 빠져 `확인 필요`로 보내야 하는 일정

`내일·다음 주` 같은 상대 날짜, 한 문장에 여러 날짜가 섞인 서술, 영문 일정은 P1이다. 붙여넣기 결과가 불완전해도 사용자는 같은 화면의 직접 입력 행에서 즉시 보정할 수 있다.

CSV·XLSX 파일은 범용 문장 파서를 거치지 않고 표 데이터를 공통 TripItemDraft 형식으로 정규화한다. 헤더 자동 인식이 실패해도 파일 전체를 거부하지 않고 열 매핑 화면에서 날짜·시간·장소·메모를 직접 연결하게 한다.

#### Step 2. 추출 결과 확인

- 여행 요약: 지역, 시작일·종료일, 인식한 일정 수
- 관심사 Chips: 자연, 역사, 문화, 쇼핑, 음식, 축제, 산책 등
- 각 관심사 근거: `경복궁·인사동에서 역사 관심을 찾았어요`처럼 원문 장소 또는 문구 연결
- Must Visit 후보: 명시적 표현은 목적지 잠금 후보, 추론만 한 항목은 미선택 상태
- 예약·티켓 잠금: 목적지 잠금과 날짜·시간 잠금을 분리해 확인
- Flexible 후보: 날짜·시간 변경 가능성이 있는 일정
- 빈 시간: 날짜와 비어 있는 시간대, 추천을 넣을 수 있는 슬롯
- 인식 실패·중복·날짜 불명 항목: 별도 `확인 필요` 영역
- 관심사·Must Visit·Flexible·날짜를 직접 추가·삭제·수정
- 보조 조건: 이동수단, 동행 인원, 최대 이동·도보시간, 접근성 조건
- CTA: `이 관심사로 Feed 만들기`
- Secondary CTA: `일정 다시 분석하기`

### 동작

- `일정 분석하기`는 원문을 바로 저장하지 않고 분석 결과 초안을 반환한다.
- 사용자가 Step 2에서 확정하기 전에는 User.preference_profile과 TripItem을 생성하지 않는다.
- `꼭 가기`, `예약 완료`, `반드시` 등 명시적 표현만 Must Visit 선택 상태로 제안한다.
- 장소가 포함됐다는 이유만으로 Must Visit을 자동 확정하지 않는다.
- 날짜·장소가 모호하면 임의 보정하지 않고 `확인 필요`로 분리한다.
- P0 계약 밖 문장은 오류로 단정하지 않고 인식한 토큰만 보존한 채 직접 입력·수정으로 전환한다.
- 시작일은 종료일보다 늦을 수 없다.
- 여행 기간이 너무 길면 지원 범위를 안내한다.
- 확인 완료 후 Trip·TripItem·InterestSignal을 저장하고 개인화 Feed로 이동한다.
- 공식 예측 범위 밖 날짜는 숫자 없이 `공식 혼잡 예측 범위 밖`과 출처 있는 주말·공휴일 배지만 표시한다.

### 데이터

- 입력 초안: ItineraryImportDraft(input_type, parse_status, source_file_name, source_sheet, column_mapping, expires_at) - raw_text와 원본 파일은 기본 비영속
- 추출 결과: TripDraft, TripItemDraft, InterestSignal, EmptySlot
- 확정 후 생성: Trip, TripItem, TripInterest
- 선호도: 해당 Trip Context를 우선 사용하고, 사용자가 동의한 경우에만 User.preference_profile에 누적
- 분석 이벤트: `itinerary_import_started`, `itinerary_column_mapping_confirmed`, `itinerary_parse_completed`, `itinerary_signal_edited`, `trip_confirmed`

### 완료 기준

- 고정한 한국어 Fixture 8종에서 날짜·장소·관심사 초안을 반환하고 회귀 테스트가 통과
- `.txt`는 텍스트 Fixture와 동일한 결과를 만들고, `.csv`·`.xlsx` 대표 Fixture는 자동 매핑과 수동 열 매핑을 거쳐 같은 TripItemDraft를 만든다.
- **일정 없는 사용자가 지역·날짜·관심사만으로 Trip을 생성하고 개인화 Feed에 도달 가능**
- 상대 날짜·영문·복합 서술을 지원한다고 표시하지 않음
- 파싱 실패 시 원문을 잃지 않고 직접 입력·수정으로 핵심 흐름 완료
- 관심사마다 최소 한 개의 추출 근거 확인 가능
- 추출 관심사·Must Visit·Flexible·빈 시간을 수정 가능
- 확정 전 새로고침·이탈 시 원문이 사용자 계정에 영구 저장되지 않음
- 필수값 누락·역전 날짜 검증
- 확정 후 새로고침해도 Trip과 확인된 신호가 보존
- 예측 범위 밖에서도 빈 화면이 발생하지 않음

## S03. Social For You Feed

### 목적

관광지 탐색과 실제 여행 계획을 하나의 흐름으로 연결한다.

### 화면 구성

- 상단 여행 선택기: 일정 없음이면 `일정 없음 · 일반 여행 Feed`, 등록 후에는 `서울 · D+7~D+10`; 실제 UI는 데모 실행일에 계산한 ISO 날짜 표시
- 일정 기반 개인화 바: 일정 등록 후에만 `일정에서 읽은 관심사 · 역사 · 문화 · 음식 · 산책` + `수정`
- P0 탭: `For You / Saved`; `Nearby / Following`은 P1
- 세로 스크롤 여행 게시글 Feed; P0는 페이지네이션·유한 Seed 세트로 안정적으로 제공
- Feed 게시글 카드
  - 화면의 중심이 되는 이미지·영상 썸네일
  - 작성 주체 또는 `공식 관광정보 기반` 라벨
  - 장소명·지역·카테고리
  - 짧은 게시물 문구
  - 공식 Seed / UGC 구분 배지
  - 혼잡 배지
  - 여행 중 추천 날짜가 있으면 표시
  - 출처
  - 좋아요·저장·공유 - 구현 완료 항목만. 저장은 여행과 무관한 `SavedPost`
  - `+` — 활성 여행의 `TripCandidate`로 저장. 실제 일정 배치·교체는 하지 않음
- `왜 이 장소가 보이나요?` 설명

추천 이유는 `관심사와 일치` 같은 일반 문구로 끝내지 않고 가능한 경우 아래 중 1~2개를 노출한다.

- `경복궁·인사동 일정에서 읽은 역사 관심사와 잘 맞아요.`
- `D+9 오후 빈 시간에 이동 가능한 거리예요.`
- `저장한 정원·산책 콘텐츠와 비슷해요.`
- `여행 기간 중 다른 날짜가 상대적으로 여유로워요.`

Feed Context는 세 상태로 구분한다.

1. 일정 없음: 권역·카테고리 다양성을 우선한 일반 여행 게시글 Feed
2. 일정 등록: 확인된 관심사·방문 예정지·빈 시간으로 개인화한 Feed
3. 여행 중: Trip timezone의 현재시각이 start_at~end_at에 있고 기준 장소를 선택했거나 다음 일정이 있을 때, 개인화 Feed를 유지하면서 오늘 방문 가능한 카드에 현재 권역 혼잡·날씨·거리·다음 일정까지 남은 시간을 별도 Context로 추가

현재 날씨는 게시글 추천과 대안 선택의 근거로 사용할 수 있지만 `실시간 혼잡 관측값` 자체를 보정하거나 새 혼잡값을 만드는 데 사용하지 않는다.

### 동작

- 카드 선택 → POI 상세
- `+` → S06 `내 여행에 담기`
- 후보 저장 완료 시 카드 CTA가 `담아둠`으로 바뀌고 `trip_candidate_added`를 기록한다.
- 저장 완료 Snackbar는 `내 여행에 담았어요 · 일정에 배치하기`를 제공하며 후자는 S07 편집으로 이동한다.
- 일정이 여러 개면 상단 활성 여행을 기본 대상으로 사용하고, 활성 여행이 없으면 S06에서 여행 선택·최소 생성을 요구한다.
- 개인화 바의 `수정`에서 S02 Step 2를 다시 열고 관심사·Must Visit·Flexible을 수정
- 관심사 수정 후 Feed 캐시를 무효화하고 랭킹을 갱신
- 같은 장소·지역·작성자의 과도한 반복 노출 억제

### 데이터·API

- P0 KorService2 기반 POI와 이미지; EngService2는 P1
- 확인된 TripInterest·EmptySlot·TripItem과 TripFit·DateFit·CrowdPenalty가 적용된 룰 기반 FeedScore
- FeedImpression, 클릭, 체류, SavedPost, TripCandidate 저장, 실제 일정 배치, 관심 없음, 숨김을 서로 다른 이벤트로 기록
- P0는 `여행 게시글 형태의 공식 Seed 발견 Feed`이며, 실제 사용자 게시·팔로우 그래프·Trending은 P1 이후에만 사용
- P0는 한 응답 안에서 POI·권역·작성자 슬롯 상한을 적용한다. 트래픽 기반 전역 ExposureBudget과 포화 카운터는 P1에서 검증한다.

### 상태

- 여행 없음: 일반 Seed Feed와 `일정을 입력하면 취향에 맞춰드려요` CTA
- 일정 분석 미확정: Feed를 생성하지 않고 추출 결과 확인으로 복귀
- 관심사 없음: 일정 지역·기간 기반 Feed를 제공하고 관심사 선택 CTA 표시
- 혼잡 데이터 없음: 혼잡 배지를 숨기거나 `데이터 없음`
- API 지연: Seed 캐시와 마지막 동기화 시각 표시

### 완료 기준

- 최소 20개 Seed 카드
- 최소 5개 카드가 POI 상세와 `+ → TripCandidate`에 정상 연결
- 실제 관광공사 데이터 카드에 출처 표시
- 일정 기반 추천 카드에 추천 근거와 해당 신호 수정 진입 제공
- Feed → 후보 저장 → My Trip 배치 전환 이벤트를 서로 구분해 확인
- 같은 POI·권역·작성자가 인접 카드와 상위 노출을 독점하지 않음
- 관심 없음·숨김한 후보가 같은 세션에서 다시 노출되지 않음

## S04. 검색·탐색(P1)

### 목적

키워드·카테고리·지역으로 관광지를 직접 찾게 한다.

### 화면 구성

- 검색창
- 최근 검색어
- 카테고리 Chips
- 리스트·지도 전환
- 결과 카드: POI, 거리, 혼잡 상태, `+`(선택한 여행의 담아둔 장소)

### 동작·데이터

- 키워드 검색은 관광공사 검색 API 또는 제공기관 저장정책 확인을 마친 승인 인덱스 사용
- 위치 권한 없이도 지역 선택 검색 가능
- 검색 결과가 없으면 오탈자·인근 지역 안내
- `+`는 TripCandidate만 생성하며, 일정 날짜·시간 배치는 My Trip 편집에서 수행

### 완료 기준

- 서울 대표 POI 검색 성공
- 한국어·영어 검색 결과의 Canonical POI 연결 검증

## ~~S05. POI 상세~~ → S11 Live 상세로 통합 (v5.8 삭제)

### 삭제 사유

별도 POI 상세 화면을 두면 S11 Live 상세와 **정보가 완전히 중복**된다.
장소명·카테고리·거리·혼잡·데이터 상태·출처·액션이 같은 내용이며,
유지 지점이 둘로 갈라져 표기 불일치 위험이 커진다.

### 대체 구조

**S11 시트가 선택 시 장소 상세로 전면 교체된다.** 목록이 사라지고 상세 페이지가 된다.

진입 경로 3가지 — 모두 같은 상세로 이어진다.
- Feed 카드 선택
- Live 지도 마커 선택
- Live 목록 카드 선택

### S11 상세가 반드시 포함할 요소 (구 S05 요구사항 승계)

1. 대표 이미지·장소명·지역·카테고리
2. 공식 소개와 운영 정보
3. 현재 혼잡 요약 + **혼잡 예보 3구간** (오늘 시간대별 / 이번 주 / 다음 달)
4. 내 일정에 있으면 `day N · 날짜 시간대 방문 예정`과 **그날 예상 혼잡**
5. `내 여행에 담기` 또는 `비슷한데 덜 붐비는 곳` — 혼잡도에 따라 CTA가 바뀐다
6. 담은 뒤 My Trip 편집에서 `Must Visit`으로 배치하는 선택
7. 연관 관광지·주변 경험 — **S07 인라인 제안으로 이동**
8. P0 직선거리 또는 `이동시간 확인 불가`
9. 데이터 상태, 기준시각, 출처

### 유지되는 제약

- 이미지 없을 때 브랜드 Placeholder
- 변동 정보(운영시간·행사)는 기준시각 표시, stale 처리
- P1 영어 기능을 켤 때 공식 영문 상세가 없으면 `영문 상세 없음` 처리
- 이미지별 저작권·출처 metadata 보존


## S06. 내 여행에 담기·즉시 배치 Bottom Sheet

### 목적

Feed·검색·Live·장소 상세에서 발견한 POI를 선택한 여행의 `TripCandidate`로 저장한다. 후보 저장과 실제 일정 배치를 분리해 `+`만으로 날짜·시간이나 기존 일정이 바뀌지 않게 한다.

### 화면 구성

- 선택 POI 요약과 공식 Seed·UGC·출처 표시
- 활성 여행 선택기. 활성 여행이 있으면 한 번의 확인으로 저장
- Primary CTA: `내 여행에 담기`
- Secondary: `취소`
- 저장 완료 상태: `내 여행에 담았어요`
- 완료 액션: `계속 둘러보기` / `일정에 배치하기`
- 일정이 없으면 `아직 등록된 여행이 없어요`와 `여행 만들기`·`일정 등록하기`를 표시하고, 생성 후 원래 후보 저장으로 복귀한다.
- 같은 Canonical POI가 이미 TripCandidate이면 `이미 담아둔 장소예요`와 `담아둔 장소 보기`를 표시한다.
- 같은 Canonical POI가 TripItem이면 `이미 일정에 있어요`와 `기존 일정 보기`를 표시하고 후보를 중복 생성하지 않는다.

#### 선택적 2단계 — `일정에 배치하기`

- S07 편집 상태를 열고 해당 TripCandidate에 포커스를 맞춘다.
- 빠른 배치가 필요하면 같은 Bottom Sheet 변형에서 날짜·시작시간·체류시간, `Must Visit`, `날짜 고정`, `시간 고정`을 설정할 수 있다. 날짜·시간을 아직 정하지 않으면 배치하지 않고 TripCandidate로 유지한다.
- 저장 전 시간 충돌·운영·예약 제약을 검사하고 충돌이면 `시간 미정으로 배치` 또는 `다른 날짜 선택`을 제공한다.
- 유효한 날짜별 혼잡 차이가 있으면 배치 완료 후 `더 여유로운 일정 보기`로 S09 ITEM Preview에 연결한다.

### 동작

- `TripCandidate(trip_id, canonical_poi_id, source_post_id, added_at, status=ACTIVE)`를 멱등 생성한다.
- 후보 저장은 `Trip.version`을 올리지 않으며 `TripItem`·`OptimizationRun`을 만들지 않는다.
- SavedPost와 TripCandidate를 같은 이벤트로 기록하지 않는다.
- `일정에 배치하기`를 완료한 경우에만 TripItem을 만들고 TripCandidate를 `SCHEDULED`로 전환한다.
- 뒤로 가기·취소 시 Feed 스크롤과 원래 카드 상태를 복원한다.

### 완료 기준

- 후보 저장·취소·중복·일정 없음 상태가 같은 컴포넌트 변형으로 동작
- Feed·검색·Live·상세에서 담은 후보가 같은 My Trip 보관함에 즉시 표시
- 후보 저장만으로 날짜·시간·Must Visit·Trip version이 바뀌지 않음
- `일정에 배치하기` 선택 시 정확한 TripCandidate가 열린 S07 편집으로 이동
- 중복 요청과 빠른 연타에도 TripCandidate가 하나만 생성됨

## S07. My Trip

### 목적

여행별 `TripCandidate`와 확정 `TripItem`을 한 화면에서 관리하되, 기본 보기·사용자 수동 편집·AI 최적화를 명확히 분리한다.

### 화면 구성

- 여행명·지역·기간·언어
- 범위 전환: `전체 일정 / 날짜별`
- 상단 Primary actions: `일정 편집` / `AI로 일정 최적화`
- `담아둔 장소 n` 진입점과 후보 패널
- 날짜별 일정 목록과 날짜 미정 TripItem
- TripItem 카드
  - 장소명
  - 예정일·시간
  - Must Visit 하트(`♥ 꼭 가기`)
  - 날짜 고정·시간 고정·예약 잠금 상태 문구
  - CrowdLevel·데이터 상태
  - 제안 존재 배지
- **카드 인라인 제안** — 혼잡한 항목에만 표시. 장소당 1개, 하루 최대 2개
  - 시간 분산: 카드 안 `<날짜>가 더 여유로워요 →`
  - 공간 분산: `claimMode=ABSOLUTE`의 검수 후보 1곳만 능동 노출. RELATIVE는 인라인에 노출하지 않고 S11 사용자 직접 정렬·필터에서만 진입
- CTA: `장소 더 찾기`

### 상태 A — 일정 보기

- 일정 카드와 인라인 제안은 읽기 상태이며 필드 탭만으로 값이 즉시 바뀌지 않는다.
- `담아둔 장소 n`은 Bottom Sheet 또는 데스크톱 우측 패널로 연다.
- 인라인 제안 선택 → S09
- `AI로 일정 최적화`는 현재 화면이 날짜면 DAY, 전체면 TRIP scope 확인 Sheet를 연다.

### 상태 B — 일정 편집

- 편집 진입 시 담아둔 장소 패널을 함께 보인다.
- TripItem별로 장소·날짜·시작·종료 또는 시간대·체류시간을 수정한다.
- `♥ 꼭 가기`, `날짜 고정`, `시간 고정`을 각각 독립 Toggle로 제공한다.
- 예약·티켓·투어 잠금은 별도 배지와 이유를 표시하며 일반 Toggle과 같은 모양으로 오인시키지 않는다.
- TripCandidate는 `일정에 추가 / 기존 장소와 교체 / 둘 다 유지`를 제공한다.
- 교체 시 기존 TripItem의 날짜·시간·정렬 순서를 기본 승계하고 저장 전 Preview를 보여준다.
- Must Visit 교체는 `<장소명>은 꼭 가려던 곳이에요. 잠금을 해제하고 바꿀까요?` 확인 없이는 진행하지 않는다.
- 항목 삭제·여행 기간 밖 이동·충돌·저장 취소를 각각 확인하며 `저장 / 변경 취소`를 제공한다.
- 드래그는 보조 수단이며 키보드·스크린리더 사용자를 위해 `이 날짜에 추가`, `위/아래로 이동`, `교체` 액션을 제공한다.

### 담아둔 장소 유사성 상태

- `EXACT`: `이미 일정에 있어요`; 추가·교체 비활성, 기존 일정 보기
- `SIMILAR`: `Day N의 <장소>와 비슷한 <reason> 경험이에요`; 추가·교체·둘 다 유지
- `NONE`: 일반 후보; 일정에 추가
- SIMILAR은 카테고리·공식 relation·검수 tag·거리·선택적 embedding을 후보 근거로 사용할 수 있지만 LLM 문장만으로 확정하지 않는다.
- 비교 가능한 공간 데이터가 아니면 두 CrowdLevel과 우위 화살표를 만들지 않는다.

### 상태 C — AI 최적화 진입

- 계산 범위: `이 일정 항목 / 이 날짜 / 전체 여행`
- 기본 `현재 일정만 최적화`; 선택 `담아둔 대안도 포함`은 OFF
- 보호 조건 요약: Must Visit 수, 날짜 고정 수, 시간 고정 수, 예약 잠금 수
- 최적화 목표: 혼잡 완화·동선·총 이동시간·일정 안정성. 사용자가 우선순위를 수정할 수 있으면 soft preference로만 반영
- CTA: `최적화 시작` → OptimizationRun 생성 → S09

### 상태

- 빈 여행: `담아둔 장소로 일정 만들기`와 Feed 추천 카드·탐색 CTA
- 후보 없음: `Feed·Live에서 +로 장소를 담아보세요`
- 일부 데이터 없음: 해당 카드만 상태 표시, 전체 일정은 유지
- 저장 충돌: 현재 Trip version을 다시 불러오고 사용자 수정 초안은 가능한 필드까지 보존

### 완료 기준

- 보기 상태에서 의도하지 않은 일정 mutation이 발생하지 않음
- 후보 저장과 실제 일정 배치 이벤트가 분리됨
- Must Visit·날짜 고정·시간 고정이 독립 저장되고 각 차원의 수동·AI 변경을 정확히 막음
- 일정 편집 저장·취소·충돌 복구와 키보드 조작이 동작
- EXACT·SIMILAR·NONE 상태와 추가·교체·유지 흐름이 완결
- Optimization 적용 후 변경된 날짜·시간·순서가 즉시 반영
- 새로고침 후 동일 상태 유지
- Must Visit은 **자동 생성되는** 목적지 대체 후보에서 제외 (사용자가 직접 선택한 교체는 잠금 해제 확인 후 허용)
- RELATIVE 공간 비교는 일반 사용자 카드에 능동 노출되지 않음

## ~~S08. Trip Crowd Calendar~~ → S11 예보 + S07 인라인으로 분산 (v5.8 삭제)

### 삭제 사유

별도 달력 화면은 **사용자가 찾아가야 하는 화면**이다.
정작 날짜 변경 결정은 일정을 보고 있을 때 일어나는데,
Calendar는 그 흐름에서 한 단계 떨어져 있어 도달률이 낮다.

기획안 §4.4도 이미 `사용자가 My Trip과 Calendar를 차례로 찾아가지 않아도` 되도록
S06에서 S09로 바로 연결하라고 요구하고 있다. 같은 논리를 확장한다.

### 대체 구조 — 두 곳으로 나눈다

**① 혼잡 예보 → S11 장소 상세**

날짜별 혼잡 비교를 **3구간 막대 그래프**로 보여준다.

| 구간 | 소스 | 라벨 |
|---|---|---|
| 오늘 시간대별 | 서울 Live + 당일 예측 | `실시간 관측 + 당일 예측` |
| 이번 주 | 관광공사 집중률 예측 | `공식 혼잡 예측` |
| 다음 달 | 공식 공휴일 달력 | `공식 혼잡 예측 범위 밖` + `주말·공휴일` 정성 배지 |

**② 날짜 변경 제안 → S07 카드 인라인**

일정 카드 안에 `10/6이 더 여유로워요 →`를 직접 표시한다.
사용자가 일정을 보는 그 자리에서 제안이 보인다.

### 유지되는 제약 (전부 승계)

- **동일 POI의 날짜끼리만 비교**한다. 서로 다른 POI의 절대 혼잡 우열로 해석하지 않는다
- `공식 혼잡 예측`과 `공식 혼잡 예측 범위 밖`을 **시각·텍스트로 구분**한다
  → 범위 밖에는 CrowdLevel·숫자 막대를 만들지 않고 검증된 주말·공휴일 배지만 표시
- **세 구간을 하나의 연속 그래프로 잇지 않는다.** 소스가 다르므로 각각 독립 카드
- 원시 집중률을 실제 대기시간·현장 인원처럼 오해할 정밀 숫자로 표시하지 않는다
- 예측 범위 밖에서는 `공식 혼잡 예측 범위 밖`을 표시하고 출처 있는 정성 Context만 제공한다
- 후보은행 10개 이상·`DEMO_READY` 5개 이상 Submission Gate는 **그대로 유지**

### 사용자 문구 (승계)

- 공식 범위 안: `한국관광공사 혼잡 예측을 바탕으로 비교했어요.`
- 범위 밖: `아직 공식 혼잡 예측 범위 밖이에요.` + 공식 달력으로 확인된 경우에만 `주말`·`공휴일` 배지

### 완료 기준 (재정의)

- S11 상세에서 최소 한 개 POI의 날짜별 차이와 근거를 확인 가능
- `예측`과 `예측 범위 밖 정성 Context`가 시각·텍스트로 구분되고 범위 밖 숫자 막대가 생성되지 않음
- S07 카드에서 날짜 변경 제안을 바로 확인하고 S09로 진입 가능


## S09. AI 일정 최적화 Preview

### 목적

P0의 한 항목 날짜 변경과 P1의 하루·전체 일정 리밸런싱을 동일한 승인 화면으로 제공한다. 현재 일정과 변경안을 지도·타임라인·지표로 비교한 뒤 사용자가 적용 여부를 결정한다.

### 화면 구성

- Header: `AI 일정 최적화`와 scope `항목 1개 / 이 날짜 / 전체 여행`
- 보호 문구: `꼭 가는 곳과 고정한 일정은 유지했어요.`
- 계산 진행: 조건 확인 → 공식 관광정보 후보·운영 검증 → 혼잡·날씨 검증 → 이동 동선 최적화 → 근거 설명 생성
- 변경 전·후 전환 또는 모바일 Tab: `현재 일정 / 최적화안`
- 지도: 번호 마커·경로선·추가·이동·교체 항목 구분. 지도만으로 정보를 전달하지 않음
- 타임라인 Diff: 날짜·시간·순서·장소의 from/to와 변경 이유
- 지표: 예상 총 이동시간·거리, 피크 혼잡 노출, 빈 시간·충돌, 변경 항목 수
- 보호 요약: Must Visit·날짜 고정·시간 고정·예약 잠금 보존 수
- 데이터 상태·신뢰도·기준시각·route provider·forecast issue
- Primary: `이 변경 적용하기`
- Secondary: `현재 일정 유지`
- 선택: `다시 최적화`
- 적용 직후 Snackbar: `일정을 변경했어요 · 되돌리기`

P0 ITEM Preview는 현재 Figma의 `한 가지만 바꿔볼까요?` 비교 카드 구조를 유지한다. P1 DAY·TRIP Preview에서만 지도·전체 타임라인·집계 지표를 확장한다.

### 상태 처리 예시 — 기획안 규칙

아래는 디자인 틀 수정사항이 아니라 S09 구현·API 상태 계약이다.

- `PENDING`: 계산 단계를 표시하고 취소를 제공한다. 검은 상자 Spinner 하나로 숨기지 않는다.
- `AVAILABLE`: 유효한 변경안이 있고 일정·예약·운영·경로 제약을 통과하면 변경 전·후를 보여준다.
- `CONFLICT`: 대체일에 기존 일정 또는 예약 잠금이 있으면 제안을 노출하지 않거나 `일정과 겹쳐 변경할 수 없어요`로 종료한다.
- `DATA_CHANGED`: 사용자가 적용을 누른 시점의 최신 예측에서 개선 방향이 사라졌으면 TripItem을 변경하지 않고 `혼잡 예측이 갱신되어 일정을 변경하지 않았어요`를 표시한다.
- `TRIP_CHANGED`: 생성 당시 Trip version과 달라졌으면 적용하지 않고 `일정이 수정되어 최적화안을 다시 만들게요`로 재계산한다.
- `ROUTE_UNAVAILABLE`: 경로 데이터가 없으면 이동시간 개선 지표·경로 우위 표현을 제거하고 날짜·혼잡처럼 검증 가능한 차원만 남긴다.
- `APPLIED`: 적용 후 My Trip과 혼잡 예보를 함께 갱신하고 제한 시간 안에 되돌리기를 제공한다.
- `REVERTED`: 되돌리기가 성공하면 원래 날짜와 관련 상태를 복구한다. 다른 탭에서 일정이 바뀌어 충돌하면 안전하게 중단한다.

### 탐색 규칙

P0 제출 Gate는 **같은 POI의 여행 기간 안 날짜 변경 1종**을 `scope=ITEM`으로 생성한다. 아래 2~5단계는 P1 Feature Flag가 켜지고 해당 데이터·Constraint가 검증된 경우에만 `scope=DAY|TRIP`에서 탐색한다.

1. 여행 기간 안의 날짜 변경
2. 시간 단위 근거가 있는 경우 시간 변경
3. 주변·연관 POI를 먼저 또는 이후 방문
4. 이동 경로·경유 순서 조정
5. 사용자가 `담아둔 대안도 포함`을 켠 경우에만 Flexible 항목의 목적지 대체

앞 단계에서 의미 있는 개선이 확인되면 뒤 단계는 탐색하지 않는다.

### 실행 가능성 Gate

점수가 더 좋더라도 아래 조건 중 하나라도 실패하면 제안으로 노출하지 않는다.

- 여행 기간 안이며 기존 일정과 겹치지 않는다.
- 운영일·운영시간·행사기간·휴무 정보를 확인했다. 정보가 없으면 `확인 필요`로 낮추고 자동 최적안으로 채택하지 않는다.
- 예약·티켓·투어 시간이 잠겨 있지 않다. 잠긴 항목은 날짜·시간 변경에서 제외한다.
- destination_locked·date_locked·time_locked를 변경 차원별로 검사한다.
- P0는 기존 일정과 시각 충돌이 없고 최소 체류시간을 침범하지 않는다. 경로 API가 없으면 예상 이동시간·이동 버퍼 통과를 주장하지 않는다.
- P1 이동시간·도착시각·경유 순서는 target date·mode·departure bucket이 맞는 신선한 route matrix에서 계산한다.
- 사용자가 입력한 휠체어·유아·동행 인원·도보 한계 등 접근성·그룹 제약을 위반하지 않는다.
- 날씨·공사·행사 통제처럼 알려진 운영 리스크를 위반하지 않는다.
- 주변 후보는 직선거리 보수 상한·민감 생활권·수용력 Gate를 통과한다. 직선거리는 터무니없는 제안을 거르는 참고 필터이며 실제 이동 가능성 검증으로 표현하지 않는다.

### 동작

- OptimizationRun은 `scope`, `input_trip_version`, lock summary, candidate inclusion, objective weights, source snapshot refs를 저장한다.
- OptimizationProposal change_set은 여러 TripItem 변경을 포함할 수 있지만 적용 전 일정은 변경되지 않는다.
- MVP는 전체 변경안 단위 `적용/유지`만 제공한다. 부분 선택 적용은 P2로 미룬다.
- 적용 시 OptimizationDecision 저장 후 모든 TripItem 변경을 하나의 트랜잭션으로 처리한다.
- 유지 시 원래 일정 보존, 동일 변경안 과다 반복 억제
- 만료된 제안은 재계산
- 수락 직전 현재 Trip version과 예약 잠금을 다시 검증해 다른 탭의 수정과 충돌하면 적용하지 않는다.
- 시간축 변경 생성 시 `generatedForecastIssueId`와 전후 Level·근거 Snapshot을 저장한다. 적용 시 최신 발행 회차가 달라졌다면 최신 회차 하나 안에서 전후 날짜를 다시 계산한다.
- 최신 회차에서 개선이 뒤집히면 `DATA_CHANGED`로 만료하고 TripItem을 변경하지 않으며 제안을 재계산한다. 개선은 유지되지만 Level이 달라졌으면 최신 Preview를 다시 보여준 뒤 확인을 받는다.
- 적용 후 제한 시간 안에 `되돌리기`를 제공하고 원래 TripItem 값과 Decision 이력을 보존한다.

### 완료 기준

- ITEM·DAY·TRIP scope별 변경 전·후와 이유 확인 가능
- P1 Preview에서 지도와 동일 정보를 텍스트 타임라인·표로 확인 가능
- route matrix가 없으면 이동시간·거리 개선 주장과 경로 우위 시각이 생성되지 않음
- 적용 후 My Trip과 혼잡 예보가 동일하게 갱신
- Must Visit 목적지는 교체되지 않음
- 날짜·시간 고정은 해당 차원에서 이동하지 않으며 잠금 보존 요약과 실제 결과가 일치
- 수락·거절·오류 상태가 모두 기록됨
- 일정 시각 충돌·예약 잠금·확인 가능한 운영 제약을 통과하지 못한 제안은 생성되지 않음
- 최신 예측 발행 회차에서 개선이 뒤집힌 제안은 적용되지 않고 `DATA_CHANGED`로 기록됨
- 적용 직후 되돌리기가 Trip과 혼잡 예보에 일관되게 반영됨

## S10. 주변·숨은 명소 연결(P0 제한·P1 확장)

### 목적

유명 관광지 주변의 연관 경험으로 시간과 소비를 분산하고, 담아둔 장소와 현재 TripItem의 동일·유사 관계를 설명한다. P0의 **공간 분산 과제 연계기능**은 S11 `과밀지역 우회 보기`가 주 흐름이고, S10은 후보의 맥락과 사용자가 시작한 추가·교체 결정을 보강한다.

### 화면 구성

- 기준 관광지
- 연관 관광지·음식·숙박·행사 후보
- 이동 가능 거리 또는 시간
- 상대 혼잡 상태
- 연관 이유: P0 `함께 방문하기 좋은 장소`, 검증 시 `직선거리`; P1 `비 오는 날 실내 대안`·실제 이동시간
- `내 여행에 담기`, S07 편집의 `일정에 추가 / 기존 장소와 교체 / 둘 다 유지`, P1 `먼저 들르기`
- 출처

담아둔 장소 카드에는 `EXACT | SIMILAR | NONE`과 reason code를 제공한다. SIMILAR 예시는 `같은 경험 유형`, `공식 연관 관광지`, `같은 관심사`, `일정 동선 안`, `비슷하지만 더 여유로운 검수 후보`다. 유사성이 혼잡 우위를 의미하지 않으며 비교 계약을 별도로 통과해야만 전후 Level을 표시한다.

### 후보 순서

1. **P0:** 관광지별 연관 관광지 정보에서 후보를 받고, 선택 POI 좌표 기준 직선거리·운영·민감 지역을 수동 검수해 1개 고정
2. **P1:** 관광공사 위치기반 관광정보
3. **P1:** 여행일에 운영되는 행사
4. **P1:** 구현된 UGC 신호
5. **P1:** 지도 API의 실제 거리·이동시간 검증

`연관 관광지` 응답은 의미적·행동적 연관성이지 도보권 보장이 아니다. P0는 좌표 기반 직선거리와 운영 검수 결과를 표시하고, 이동시간을 보장하지 않는다. P1은 지도 경로로 이동 가능성을 추가 검증한다. P0 응답별 노출 상한·민감 지역·시설 수용력 검수를 통과한 장소만 `분산 추천`으로 표현하고, 트래픽 기반 혼잡 포화 제어는 P1 ExposureBudget에서 수행한다.

### P0 완료 기준

- 핵심 유명 관광지 1곳 이상에 검수된 연관 후보 1개 제공
- 연관성이 곧 도보거리라고 가정하지 않음
- 지도·경로 API를 P0 전제로 두지 않고 좌표 기반 직선거리 또는 `이동시간 확인 불가` 표시
- 혼잡해진 대안·민감 지역·운영 종료 장소가 자동 추천에서 제외됨

대표 유명 관광지 3곳 이상, 실제 이동시간 검증, `먼저 들르기`에 따른 순서 변경, 고급 유사 후보 검색은 P1 확장 기준이다.

## S11. 실시간 주변·관광지 혼잡도

### 목적

사용자가 여행 중 현재 또는 다음 일정 주변의 혼잡·날씨·거리·남은 시간을 빠르게 판단하도록 직접 선택한 장소와 다른 관광지의 상태를 한 화면에서 확인하게 한다. Live 탐색은 P0, GPS 서버 전송·혼잡 악화 자동 감지·Re-plan은 P1로 분리한다.

### P0 범위와 진입 방식

- 하단 내비게이션의 `Live` 탭에서 항상 진입 가능하다.
- 첫 진입 기본값은 `장소 직접 선택` 또는 현재 여행의 `다음 일정 기준으로 보기`다.
- P0 서버 API에는 GPS 원시 좌표를 보내지 않는다.
- 검증된 선택 기능으로 제공하더라도 GPS는 브라우저 안에서 이미 내려받은 후보와의 직선거리를 계산하는 용도로만 사용하고 서버·지도 사업자·분석 로그로 전송하지 않는다.
- GPS 서버 전송이 필요한 경로는 위치기반서비스 신고·약관·동의·보존정책 검토를 완료한 P1 Feature Flag에서만 활성화한다.

### 화면 구성

- Header: `지금 어디가 여유로울까요?`
- 현재 기준 위치: 직접 선택 장소 또는 현재 여행의 다음 일정; 선택적 기기 내 GPS는 별도 배지
- 데이터 요약: 현재 권역 원천 상태, 현재 날씨, 기준시각, 다음 일정까지 남은 시간, `실시간 관측/과거 관측 재생/공식 예측/공식 혼잡 예측 범위 밖`
- 데모가 아닌 일반 사용자는 **절대 혼잡 기준을 넘을 때만** Primary CTA를 노출한다.
  기준 미만이면 이 CTA를 표시하지 않는다. 붐비지 않는 장소에서 분산을 제안하는 것은
  과제 목적과 무관하며 사용자에게 불필요한 개입으로 느껴진다.
- **화면 문구는 생활어를 쓴다.** `과밀지역 우회`는 데이터 용어이며 1차 타깃(외국인)에게 어렵다.
  - 화면: `비슷한데 덜 붐비는 곳` (영문: `Similar places, less crowded`)
  - 기능설명서·제출물 해시태그: `#과밀지역우회` 유지 (지정과제 2 대응)
  - 이 분리는 §16.3의 대외 문구 통일 원칙과 같은 방식이다
- 데모 세션은 현재 Live 유효쌍이 있으면 우선 사용하되, 심사 시각 독립성은 7일 이내 대표 REPLAY로 보장한다. dataMode·claimMode·ctaPolicy 라벨을 고정 표시한다.
- Segment: `현재 여행지` / `다른 관광지`
- P0 기본은 목록; 지도는 P1 Feature Flag
- 마지막 업데이트 시각, 새로고침, 데이터 안내 링크

#### `현재 여행지` 탭

- 반경 선택: `1km / 3km / 5km`
- 선택 장소 또는 다음 일정 기준점
- 주변 관광지·행사·문화시설 카드
  - 장소명·카테고리·썸네일
  - 선택 장소 기준 직선거리; P1 경로 API가 유효할 때만 이동시간
  - 서울 Live는 원천 4단계 상태와 문구, Forecast는 자체 Level. 서로의 숫자 순위를 섞지 않음
  - 추세: `30분 전보다 혼잡`, `비슷`, `여유` - 실제 비교 데이터가 있을 때만
  - 데이터 상태·범위·기준시각
  - `상세 보기`, `+ 내 여행에 담기`, 선택적으로 `길찾기`
- `지금 여유로운 곳` 필터와 `가까운 순 / 일정 적합 순` 정렬
- `여유로운 순`은 동일한 서울 Live 소스·scope 그룹 안에서만 제공
- 현재 Trip이 있으면 `다음 일정까지 1시간 20분`과 들어갈 수 있는 후보를 우선 표시

#### `다른 관광지` 탭

- 지역 선택: 서울 전체 또는 구·권역
- 관광지명 검색
- 카테고리 필터: 관광지, 문화시설, 행사, 쇼핑, 음식 등
- 혼잡 필터: `여유 / 보통 / 혼잡`
- 데이터 필터: `실시간만` 토글
- on-demand 정렬·필터: `지금 더 여유로운 곳`. 사용자가 직접 켠 행위를 `ctaPolicy=ON_DEMAND_ONLY` 진입 트리거로 기록
- 대표 관광지 목록; P1 지도 Feature Flag가 켜진 경우에만 지도 마커
  - 장소 또는 권역명
  - CrowdLevel과 상태
  - 사용자가 고른 기준 장소로부터 거리
  - 기준시각과 출처
  - 일정에 포함된 장소에는 `내 일정` 배지
- Live 미지원 장소도 숨기지 않되 Forecast·정성 Context 그룹으로 분리한다. 혼합 목록에서 Live와 Forecast·Context를 하나의 혼잡 순위로 정렬하지 않는다.

### 기본 정렬 규칙

1. 사용자가 선택한 정렬값
2. 동일 sourceState·source·scope 그룹
3. 선택 장소 기준 거리 적합도
4. 사용자가 `지금 더 여유로운 곳`을 직접 켠 경우에만 같은 서울 Live 비교그룹·수집 배치 안의 낮은 `sourceOrdinal`
5. 현재 일정의 관심사·빈 시간 적합도

`지금 더 여유로운 곳`은 사용자가 직접 켠 on-demand 상태에서만 같은 서울 Live 비교그룹·수집 배치와 CandidateRelation Gate 안에서 적용한다. KTO Forecast의 관광지별 Level이나 정성 Context와 섞어 먼 장소를 상단에 올리지 않는다.

### P0 공간 우회 재현 계약

이 기능은 지도 경로를 바꾸는 내비게이션이 아니라, **동일한 원천 서수 상태를 비교할 수 있을 때 더 여유로운 검수 관광지를 선택**하도록 돕는 공간 분산 기능이다. 후보 선택은 하나의 Selector를 쓰고 주장과 CTA 노출을 별도 계약으로 관리한다.

```text
data_mode = LIVE | REPLAY
selector_mode = ORDINAL_LOWER
claim_mode = ABSOLUTE | RELATIVE | NONE
cta_policy = PROACTIVE | ON_DEMAND_ONLY | DEMO_ONLY

absolute_claim_min_ordinal = 3  # 설정값, 정책 검토로 4 상향 가능
relative_claim_min_ordinal = 2
```

공통 hard 조건:

```text
valid_group_member_count >= 3
AND anchor.source_ordinal >= relative_claim_min_ordinal
AND candidate.source_ordinal < anchor.source_ordinal
AND same comparison_group_id
AND same snapshot_set_id
AND observed_at_skew <= 10 minutes
AND CandidateRelation gates passed
```

| dataMode | claimMode | ctaPolicy | 일반 사용자 진입 | 주장 경계 |
|---|---|---|---|---|
| LIVE | ABSOLUTE | `PROACTIVE` | `비슷한데 덜 붐비는 곳` 능동 CTA | `absolute_claim_min_ordinal` 이상일 때만 제출물의 `과밀지역 우회` 근거로 사용 |
| LIVE | RELATIVE | `ON_DEMAND_ONLY` | 능동 CTA 없음. `다른 관광지` 탭에서 사용자가 `지금 더 여유로운 곳` 정렬·필터를 직접 켠 경우만 | 상대 비교만 허용, `과밀·붐빔`·percentile 표현 금지 |
| REPLAY | NONE | `DEMO_ONLY` | DemoSession에서만 | `과거 관측 재생 · 실시간 아님`과 원 관측시각 고정, 현재 효과 주장 금지 |

후보 랭킹은 `candidate.sourceOrdinal <= 2`, 더 큰 ordinal 개선폭, 일정 적합성·거리·접근성, 검수 품질·최근성 순으로 적용한다. `붐빔(4)→약간 붐빔(3)`도 비교상 참이면 hard 탈락시키지 않지만 여유·보통 후보보다 뒤에 둔다.

CandidateRelation 공간 Gate 초기값:

```text
default_radius_km = 3.0
min_center_separation_km = 0.5

overlap_ratio > 0.30         => AUTO_REJECT
0.10 < overlap_ratio <= 0.30 => MANUAL_REVIEW
overlap_ratio <= 0.10        => CONTINUE_OTHER_GATES
```

1. `comparison_group_id`는 같은 source·지표·scope 종류·normalization/mapping version이라는 측정 동질성만 표현한다. 카테고리·거리·검수는 넣지 않는다.
2. 0.5km 최소 이격은 중첩률이 놓칠 수 있는 사실상 같은 장소를 보수적으로 거른다. 10~30% 중첩은 자동 승인이 아니며 수동 검수를 통과해야 한다. 초기값 변경은 `comparison_policy_version`을 올린다.
3. 운영 여부·접근성·일정 충돌·민감 생활권·수용력 또는 확인 불가 상태·교차 검수를 통과한 CandidateRelation만 노출한다.
4. 교차 POI의 병렬 상태는 `comparisonEligible=true`일 때만 표시한다. 불가이면 화살표·두 Level·우위 문구를 제거하고 새 장소의 자체 상태·sourceState·출처만 표시한다.
5. 사용자가 POI 상세를 확인한 뒤 `+`로 TripCandidate를 저장해야 공간 분산 관심 전환으로 기록한다. 실제 계획 전환은 이후 S07 배치 또는 S09 승인 이벤트로 별도 기록하며 자동 일정 변경은 하지 않는다.
6. REPLAY는 Source Registry·보존기간·전체 E2E·7일 신선도 Gate를 통과해야 하며 현재 Live KPI에 합산하지 않는다.
7. 적합 후보가 없으면 Forecast·정성 Context를 섞어 만들지 않고 `비교 가능한 우회 후보 없음`을 표시한다.

### 동작

- 카드 선택 → **시트가 장소 상세로 전면 교체** (별도 화면 없음)
- `+` → S06 TripCandidate 저장, 현재 여행이 없으면 여행 선택 또는 S02로 연결
- ABSOLUTE CTA → 같은 비교그룹·수집 배치의 검수 후보만 필터링한 `다른 관광지` 상태
- RELATIVE on-demand → 사용자가 `다른 관광지` 탭의 `지금 더 여유로운 곳` 정렬·필터를 직접 켠 시점부터 같은 비교그룹·수집 배치 후보를 표시하고 `relative_filter_enabled`를 기록
- P1 지도 마커 선택 → 동일 장소 카드가 펼쳐짐
- 기준 장소·반경·필터 변경 → URL 또는 화면 상태에 반영되어 뒤로 가기 시 복원; GPS 좌표는 URL에 넣지 않음
- 새로고침은 외부 Live API를 무제한 직접 호출하지 않고 서버 캐시 정책을 따른다.
- 현재 일정 장소의 Live Level이 계획보다 악화됐으면 P1 Feature Flag가 켜진 경우에만 `오늘 일정 최적화`를 노출하고 `scope=DAY` S09로 연결한다.

### 상태·Fallback

- 기본: `장소 직접 선택`과 `다음 일정 기준으로 보기`
- 선택적 기기 내 위치 권한 거절: 오류로 막지 않고 직접 장소 선택 유지
- 기기 내 위치 확인 중: 목록 Skeleton과 `기기에서 현재 위치를 확인하고 있어요`
- 지원 권역 밖: `이 지역은 실시간 미지원` + Forecast·정성 Context 주변 결과
- 주변 결과 없음: 반경 확대와 다른 관광지 탭 CTA
- Live 지연: 캐시가 유효하면 `업데이트 지연`, 아니면 실시간 배지를 제거하고 Planning Crowd 사용
- 현재 Live 값이 평평함: 일반 사용자에게는 능동 CTA 없음. 사용자가 직접 RELATIVE 필터를 켜도 더 낮은 유효쌍이 없으면 후보 없음 표시. DemoSession은 대표 REPLAY 사용
- 재생 Snapshot D-2: `REPLAY_EXPIRING` 경보를 보내고 운영자가 최근 7일 후보 세트를 검토·교체
- 재생 Snapshot 만료·검증 실패 또는 연속 갱신 실패: `replayReady=false`, `spatialDemoReady=false`, `SUBMISSION_BLOCKED`로 전환하고 수동 교체·전체 E2E 재검증 전까지 해제 금지
- 전체 혼잡 데이터 없음: 장소 탐색은 유지하고 `현재 혼잡 데이터 없음` 표시
- P1 지도 실패: 목록 화면 유지, 거리 대신 `이동시간 확인 불가`

### P1 Live Re-plan Trigger

- 같은 서울 Live 권역·mapping version의 이전 관측 대비 현재 관측이 검증된 악화 임계값을 넘음
- 관측 신선도가 유효함
- 도착 예상 시점에도 악화가 지속될 가능성이 있음
- 사용자에게 적용 가능한 시간·주변 POI 제안이 있음

조건 충족 시에도 자동 변경하지 않고 `30분 뒤 방문`, `주변 실내 POI 먼저 방문`, `다른 여유 관광지 담기`, `오늘 일정 최적화` 중 가능한 제안만 제공한다. 실제 일정 반영은 S09 승인 이후다.

### 데이터·분석 이벤트

- LiveArea, SeoulLiveAreaMap, CrowdSnapshot, CandidateRelation, CanonicalPOI, TripInterest, EmptySlot
- `live_opened`, `live_anchor_selected`, `location_permission_result`, `live_scope_changed`, `live_filter_changed`, `relative_filter_enabled`, `live_place_clicked`, `live_to_trip_added`, `live_refresh_requested`, `detour_opened`, `detour_candidate_clicked`, `detour_to_trip_added`, `detour_no_candidate`
- 모든 공간 분산 이벤트에 `comparisonAxis`, `comparisonEligible`, `comparisonReasonCode`, `dataMode`, `selectorMode`, `claimMode`, `ctaPolicy`, `comparisonGroupId`, `collectorRunId`, `snapshotSetId`, 전후 `sourceOrdinal`, `observedAtSkewSeconds`, `candidateRelationId`, `curationVersion`, `distanceKm`, `overlapRatio`, `overlapGateResult`, `isCurrent`를 기록한다.
- `claimMode=RELATIVE`인데 일반 사용자 능동 CTA가 노출된 이벤트는 `RelativeProactiveCtaViolationCount`에 집계하며 목표는 0이다.
- P0 서버는 원시 GPS 좌표를 수신하지 않는다. 분석 이벤트에는 위치 모드와 사용자가 선택한 공개 권역 식별자만 기록한다.
- 서울 실시간 도시데이터는 장소명 1개 단위 호출이므로 브라우저가 여러 장소를 직접 fan-out하지 않는다. 수집기는 쿼터 확인 전 5분 전수 수집을 전제하지 않고 이원화한다.
  - readiness 조밀 수집: 검수 후보은행의 앵커·후보만 일 2회 이상 지정 점검 창과 심사 운영 창에서 수집
  - 신규 앵커 발굴: 공식 121개 전수 스캔을 일 1~2회 저빈도로 수행
- 승인량·실측 호출량·실패율을 보고 점검 창 빈도를 조정하되 예상량 50% 이하와 심사 예비량 30% 이상을 유지한다.
- 지원 장소 수를 코드에 고정하지 않고 공식 장소 코드 목록을 주기적으로 동기화한다.

### 완료 기준

- `현재 여행지`와 `다른 관광지` 전환이 모바일·데스크톱에서 동작
- 장소 직접 선택과 다음 일정 기준 경로가 모두 동작
- 선택적 기기 내 GPS를 켠 경우 허용·거절 모두 서버 좌표 전송 없이 동작
- 검수 앵커 3개 이상·CandidateRelation 5개 이상이며 관계가 최소 2개 앵커에 분산
- 일반 사용자 ABSOLUTE CTA와 사용자 직접 RELATIVE 정렬·필터의 두 진입 경로가 각각 동작하고 RELATIVE 능동 CTA는 생성되지 않음
- 완결 REPLAY가 없어도 현재 Live 화면은 사용할 수 있지만 `/api/demo/readiness`의 `spatialDemoReady`는 실패함
- `claimMode=RELATIVE`를 과밀로 표현하지 않고 REPLAY에는 `실시간 아님`·관측시각이 모든 관련 화면에 유지
- 다른 sourceState·scope·normalizationVersion 후보가 우회 순위에 섞이지 않고 후보 없음 상태가 구현됨
- 다른 관광지에서 Live·Replay·Forecast·정성 Context 카드가 혼동 없이 구분
- 카드 → POI 상세 → 여행별 후보 저장 연결이 정상 동작
- 기준시각, 범위, 출처가 모든 혼잡 카드에 표시
- 지원 장소 외에는 실시간 배지를 표시하지 않음
- 서울 Live 호출 실패 시 Planning Crowd로 전환하고 `실시간` 제거
- 위치 권한 거절이나 지도 장애가 화면 전체 실패로 이어지지 않음
- P1 Re-plan을 켜도 적용 전 자동 일정 변경 없음

## S12. 알림센터(P1)

### 주요 알림

- `혼잡 예측이 열렸습니다. 일정을 다시 확인해보세요.`
- `여행일이 가까워져 시간별 날씨를 반영했어요.`
- `현재 혼잡이 예상보다 높아요. 일정 최적화안을 확인하세요.`

### 완료 기준

- 알림 선택 시 정확한 Trip·OptimizationRun Preview로 이동
- 동일 이벤트 중복 알림 제한
- 읽음 상태 저장

## S13. 게시물 작성·모더레이션(P1)

### 화면 구성

- 이미지 업로드
- 문구
- POI 또는 지역 연결 - 필수
- 공개 범위
- 게시
- 신고: 스팸, 괴롭힘·혐오, 불법·위험, 허위 장소정보, 저작권

### 완료 기준

- 공식 Seed와 UGC 시각적 구분
- 숨김·삭제 콘텐츠가 Feed 후보에서 즉시 제외
- 이미지 업로드 실패·용량 오류 처리

## S14. Profile·설정

### 화면 구성

- 언어
- 장기 관심 카테고리와 `여행에서 자동 누적` 동의 상태
- Trip별 `일정에서 읽은 관심사` 조회·수정·삭제
- 내 여행 목록
- 저장·작성 게시물 - 구현된 경우
- 데이터·출처 안내
- 위치 권한 상태와 위치정보 사용 안내
- 로그아웃
- 데모 계정일 때 `데모 상태 초기화`

### 완료 기준

- 언어 전환이 주요 화면에 유지
- Trip별 관심사 수정 시 해당 Feed 추천이 갱신
- 장기 관심사 누적 동의 철회 가능
- 데모 초기화 후 기준 여행 상태 복원

## S15. 데이터 안내·출처

### 목적

예측의 의미와 한계를 투명하게 설명한다.

### 화면 구성

- `실시간 관측`, `공식 혼잡 예측`, `공식 혼잡 예측 범위 밖 정성 Context`의 차이
- CrowdLevel 1~5 설명
- 데이터 기준시각과 갱신 원칙
- 한국관광공사·서울시·기상청 등 실제 사용 출처
- Live 권역 데이터와 개별 관광지 데이터의 범위 차이
- 일정 원문 비영속, 구조화 정보 저장, P0 외부 AI 미사용과 P1 도입 시 고지·삭제 원칙
- `AI 일정 최적화`에서 LLM은 선호 해석·근거 설명만 담당하고, 실제 운영·혼잡·경로·시간 가능성은 서버 규칙과 출처가 있는 데이터로 검증한다는 역할 경계
- TripCandidate와 확정 TripItem의 차이, 독립 잠금, 최적화안 승인 전에는 일정이 바뀌지 않는다는 원칙
- 추천은 참고정보이며 사용자가 최종 결정한다는 안내
- 위치정보 처리 정책 - 위치 기능 활성화 시

### 완료 기준

- 사이트에서 실제 사용하지 않는 API·데이터는 나열하지 않음
- 모든 출처 링크와 명칭 검수
- Live 카드의 데이터 상태·범위 설명과 화면 표기가 일치
- 개인정보처리방침의 일정 원문·관심사·위치 처리 설명과 실제 구현이 일치

---

# 8. 핵심 기능 규칙

## 8.0 일정 이해와 관심사 추출 규칙

### 처리 순서

1. 입력 형식 정규화: 줄바꿈, 날짜 표현, 시간 표현, 장소 구분
2. 여행 지역·기간 추출
3. 일정 항목 추출: 날짜, 시간, 장소명, 메모, 예약·확정 표현
4. Canonical POI 매핑: 명칭, 좌표, 지역, 관광공사 contentId 후보 비교
5. 관심사 신호 생성: POI 카테고리, 사용자의 명시 문구, 반복 장소 유형, 활동 표현
6. Must Visit 후보·Flexible 후보·빈 시간 계산
7. confidence와 근거 문구를 포함한 초안 반환
8. 사용자 확인·수정 후에만 Trip과 개인화 신호 저장

### 추출 결과 스키마

```json
{
  "trip": { "area": "서울", "startDate": "<D+7의 ISO 날짜>", "endDate": "<D+10의 ISO 날짜>" },
  "items": [
    {
      "rawText": "D+7 오전 경복궁 꼭 가기",
      "date": "<D+7의 ISO 날짜>",
      "timeRange": "AM",
      "poiCandidateId": "...",
      "mustVisitCandidate": true,
      "destinationLockedCandidate": true,
      "dateLockedCandidate": false,
      "timeLockedCandidate": false,
      "flexibility": "DATE_FLEXIBLE",
      "confidence": "HIGH",
      "needsReview": false
    }
  ],
  "interests": [
    { "code": "HISTORY", "score": 0.86, "evidence": ["경복궁", "인사동"] }
  ],
  "emptySlots": [
    { "date": "<D+9의 ISO 날짜>", "timeRange": "AFTERNOON", "durationMinutes": 180 }
  ]
}
```

위 코드는 롤링 데모 규칙을 설명하기 위한 예시다. 실제 API 응답은 `DemoScenarioFactory`가 `D0`에서 계산한 유효한 ISO 8601 날짜를 반환한다.

### 판정 규칙

- 관심사는 `명시적 선호 문구 > Must Visit 확정 항목 > 반복된 장소 카테고리 > 단일 장소 카테고리` 순으로 신뢰도를 높인다.
- `꼭`, `반드시`는 목적지 잠금 후보로, `예약 완료`, `티켓 구매`, 정해진 투어 시간은 목적지·날짜·시간 잠금 후보로 분리한다.
- 단순히 일정에 포함된 장소는 Must Visit이 아니라 일반 또는 Flexible 후보로 시작한다.
- 날짜·장소 매핑 confidence가 낮으면 원문과 후보를 나란히 보여주고 사용자가 선택하게 한다.
- 빈 시간은 Trip 기간 안에서 확정 일정과 최소 이동 버퍼를 제외해 계산한다.
- 추출 결과 수정은 해당 Trip의 Feed 랭킹·빈 슬롯·이후 생성되는 OptimizationRun 입력에 즉시 반영한다.
- 사용자 계정의 장기 선호에는 매 Trip 신호를 자동 누적하지 않고 별도 동의를 받는다.

### 구현 안전장치

- P0는 한국어 지원 Fixture 8종의 텍스트·TXT 붙여넣기, CSV·XLSX 표 가져오기와 직접 입력을 지원한다. `.xls`·`.xlsm`, 이미지·PDF·예약 URL·상대 날짜·영문·범용 자유문장 해석은 P1이다.
- 업로드 파일은 크기·행 수를 제한하고 MIME과 확장자를 함께 검사한다. 원본 파일·수식·외부 링크를 실행하거나 영속 저장하지 않으며, 정규화 결과를 사용자가 Step 2에서 확정한 뒤에만 Trip을 만든다.
- P0 구현은 결정적 규칙과 장소 사전으로 고정하고 NER·LLM을 호출하지 않는다. 계약 밖 문장은 인식한 값만 보존해 Step 2 직접 보정으로 넘긴다.
- P1에서 외부 AI API를 검토할 때만 전송 데이터·보존·학습 사용·비용·DPA를 확인하고 이름·연락처·예약번호를 제거한다.
- 어떤 분석 결과도 일정에 직접 반영하지 않고 반드시 Step 2 확인 화면을 거친다.

## 8.1 Feed 후보 생성과 랭킹

### 후보 생성 소스

- **사용자 일정 항목의 대체 관광지 후보** — Must Visit 포함 **전체 TripItem**을 기준으로,
  같은 관심사·카테고리 스타일이면서 상대적으로 여유로운 검수 관광지를 Feed 후보에 포함한다.
  이는 자동 제안(Push)이 아니라 Feed 노출(Pull)이므로 Must Visit을 제외하지 않는다.
  사용자가 스크롤 중 자연스럽게 마주치고, 끌릴 때만 교체를 선택한다.
  다만 Home Feed 자체를 `대체 관광지 전용`으로 필터링하지 않는다. My Trip의 `바꾸기` 또는 Feed 카드의 `기존 장소와 바꾸기` 이후에만 해당 일정에 맞춘 집중 후보 목록을 연다.
- 사용자가 확인한 TripInterest와 각 InterestSignal 근거
- Follow·소셜 그래프 - P1 이후
- 현재 Trip의 지역·기간·빈 일정 슬롯
- 한국관광공사 연관 관광지
- 선택한 POI 좌표 기준 관광공사 위치기반 관광정보; 사용자 GPS 기준이 아님
- 행사·축제
- 공식 Seed 콘텐츠
- UGC Trending - 표본이 있을 때만

### 초기 룰 기반 점수

모든 component는 계산 전 `[0,1]`로 정규화한다. 핵심 관련성 점수와 선택적 조정값을 분리한다.

```text
BaseRelevance =
  w_interest * Interest
  + w_trip * TripFit
  + w_date * DateFit
  + w_freshness * Freshness

OptionalAdjustment = clamp(
  Social + DiscoveryBonus + DistributionBonus
  - CrowdPenalty - RepetitionPenalty,
  -0.15,
  +0.15
)

FeedScore = clamp(BaseRelevance + OptionalAdjustment, 0, 1)
```

| 신호 | 정의 | P0 처리 |
|---|---|---|
| Interest | 확인된 TripInterest·근거와 후보 카테고리 적합도 | 일정 추출 결과를 우선하고 직접 선택값으로 보정 |
| Social | 팔로우·좋아요 등 소셜 신호 | Seed 단계에서는 0 또는 낮은 가중치 |
| Freshness | 행사·게시물 최신성 | 데이터 기준시각 사용 |
| TripFit | 여행 지역·빈 슬롯·동선 적합도 | 지역·기간 일치 중심 |
| DateFit | 여행 기간 안에 상대적으로 유리한 날짜 존재 | 집중률 예측 있을 때 활성화 |
| WeatherFit | 날씨와 실내·야외 적합 | P0 점수에는 사용하지 않고 현재 날씨 Context만 표시; P1에서 유효 예보 구간에 활성화 |
| DiscoveryBonus | 사용자가 아직 보지 않은 지역·장소 | 반복 노출 억제와 함께 적용 |
| DistributionBonus | 취향을 유지하며 과노출이 낮은 후보 | 같은 소스·scope에서 비교 가능한 근거와 P0 슬롯 상한이 있을 때 적용 |
| CrowdPenalty | 혼잡이 높은 후보 감점 | 같은 POI의 날짜 적합 또는 동일 서울 Live 그룹 안에서만 사용; Must Visit 제거에는 사용하지 않음 |
| RepetitionPenalty | 동일 지역·작성자·일정 POI 반복 억제 | 항상 적용 |

P0에서는 점수와 가중치를 설정값으로 관리하고, 사용자에게는 숫자 공식이 아니라 `여행 일정에 잘 맞아요`, `조금 더 여유로운 지역이에요`처럼 설명 가능한 이유만 보여준다.

혼잡 결측은 관측값 0이나 `여유`로 대체하지 않는다.

```text
if crowd evidence unavailable:
    CrowdPenalty = UNAVAILABLE
    DistributionBonus = UNAVAILABLE
    crowd reason = DISABLED
    spatial detour eligibility = false
```

- 결측 component는 가용 component의 값을 바꾸지 않고 해당 선택적 조정만 비활성화한다.
- 혼잡 미지원 후보도 관심사·일정 관련성으로 Feed에 노출할 수 있지만 `덜 붐빔`·`여유` 근거를 붙이지 않는다.
- 실제로 붐비는 신흥 장소가 커버리지 밖에서 무감점 상위 노출될 위험을 줄이기 위해 P0 CrowdPenalty 가중치는 작게 시작하고 `CrowdUnknownExposureShare`를 감시한다.
- 각 component 원값·정규화값·가중치·가용성, score version, 최종 rank, reason code를 로그에 남긴다.

점수 계산 전 `닫힘·중복·이미 본 항목·숨김·신고·여행 범위 밖·민감 지역` 후보를 먼저 제거한다. P0에서는 한 응답 안의 POI·권역 슬롯 상한을 적용하고, 트래픽 기반 포화 상한은 P1에서 운영 데이터가 있을 때 활성화한다. 점수 계산 후에는 상위 점수 순서를 그대로 내보내지 않고, 인접 카드의 POI·권역·작성자 유사도를 낮추는 다양성 재정렬을 한 번 더 수행한다.

행동 신호는 원시 좋아요 수를 그대로 점수에 더하지 않는다. P0에서는 아래 이벤트를 정규화한 설정값으로 쓰고, 데이터가 충분해지는 P2에서만 여러 행동의 발생확률을 예측하는 모델로 교체한다.

| 방향 | 이벤트 | 해석 주의 |
|---|---|---|
| 강한 긍정 | 실제 일정 배치·교체, 최적화안 수락 | 인기 장소의 자기강화가 생기므로 노출·권역 상한과 함께 사용 |
| 중간 긍정 | TripCandidate 저장 | 발견 관심이지 방문 계획 확정은 아니므로 실제 일정 배치와 분리 |
| 약한 긍정 | 상세 클릭, 유효 체류, 공유 | 실수 클릭·백그라운드 체류 제거 |
| 약한 부정 | 빠른 넘김, 반복 노출 무반응 | 단일 이벤트로 장기 비선호를 확정하지 않음 |
| 강한 부정 | 관심 없음, 숨김, 신고 | 같은 세션 즉시 제외, 신고는 안전 필터와 분리 처리 |

P0 다양성은 구현·설명이 쉬운 greedy MMR 또는 카테고리·권역 슬롯 믹싱으로 처리한다. DPP 기반 재정렬과 임베딩 기반 후보 탐색은 데이터와 성능 검증이 끝난 P2 선택사항이다.

## 8.2 TripCandidate 저장·배치 규칙

- Feed·검색·Live·상세의 `+`는 `TripCandidate`만 멱등 생성한다.
- SavedPost와 TripCandidate는 다른 목적·이벤트·UI 상태다.
- Feed에서 담았다면 `source_post_id`, 모든 경로에서 `source_surface`와 추천·노출 context를 보존한다.
- 동일 Trip·동일 Canonical POI의 ACTIVE TripCandidate는 하나만 허용한다.
- 동일 POI가 이미 TripItem이면 후보를 만들지 않고 `이미 일정에 있어요`로 안내한다.
- 후보 저장만으로 날짜·시간·Must Visit·Flexible·Trip version·OptimizationProposal을 변경하지 않는다.
- S07에서 배치를 확정할 때만 TripItem을 만들고 candidate status를 `SCHEDULED`로 전환한다.
- 일정에서 제거한 장소를 보관함으로 되돌릴지는 삭제 확인에서 사용자가 선택한다.
- 새로운 예측이 들어와도 자동으로 배치·날짜 변경하지 않고 해당 scope의 OptimizationProposal만 재계산한다.

## 8.3 S11 혼잡 예보 시간 구간 규칙

공공데이터포털에서 **2026-08-24에 확인한** 공개 설명은 관광지 집중률 예측을 `향후 30일`로 안내한다. 이 날짜는 초기 확인일일 뿐이며 Source Registry의 owner가 주 1회와 `D_submit-2영업일`에 공식 문서·실응답을 재검증한다. 실제 운영 Endpoint의 대상일·갱신·누락 규칙은 샘플 응답과 계약 테스트로 확인해 `H` 설정값으로 관리하고, 화면 문구와 코드가 숫자 30에 직접 의존하지 않게 한다.

| 여행일까지 | 혼잡 로직 | 날씨 로직(P1; P0는 유효한 현재 날씨만 별도 표시) | UI 상태 |
|---|---|---|---|
| D-31 이상 또는 H 밖 | 혼잡 수치 생성 없음. 공식 달력의 주말·공휴일 정성 Context만 선택적으로 표시 | 반영하지 않음 | `공식 혼잡 예측 범위 밖` |
| 공식 H 안 | 관광지 집중률 예측 | D-10 전에는 미반영 | `공식 혼잡 예측` |
| D-10~D-4 | 집중률 예측 | 중기예보의 광역 강수·기온 위험 | P1 날짜 주의만 표시 |
| D-3~D-1 | 집중률 예측 | 단기예보의 시간별 강수·기온·하늘 | P1 시간·실내외 제안 가능 |
| 당일 | Planning과 지원 지역 Live 관측을 별도 표시 | 단기·초단기 또는 Live 날씨 | P1 Live Re-plan 가능 |

공식 H 밖에서는 `공식 혼잡 예측 범위 밖` 메시지와 출처가 확인된 주말·공휴일 정성 배지만 제공한다. 숫자 막대·CrowdLevel·과거 패턴 유사값을 만들지 않는다. H 진입 시 재계산·알림은 P1 Feature Flag에서 제공한다.

## 8.4 Weather Context(P0)·Weather-aware Scheduling(P1)

- P0는 서울 Live 또는 유효한 현재 날씨를 혼잡과 나란히 표시하되 FeedScore·CrowdLevel을 변경하지 않는다.
- 날씨를 이용한 시간 변경·실내외 Swap·대안 생성은 P1 Feature Flag다.
- 아래 예보 구간 규칙과 WeatherFit은 P1 Feature Flag가 켜진 경우에만 적용한다.
- D-10 이전에는 WeatherFit을 0으로 둔다.
- 중기예보 구간에서는 야외 일정에 주의만 표시하고 시간 최적화 근거로 쓰지 않는다.
- D-3부터 실내·야외 Swap, 시간 변경, 비 회피 POI 제안을 허용한다.
- 예보 변경은 자동 일정 수정이 아니라 새 OptimizationRun 생성으로 처리한다.
- 날씨 API가 실패하면 혼잡 분석은 정상 유지하고 WeatherFit만 제거한다.

## 8.5 AI 일정 최적화 생성 규칙

P0 `scope=ITEM` 엔진은 동일 POI의 날짜 변경만 생성한다. P1 `scope=DAY|TRIP`은 실제 운영·시간창·route matrix가 준비된 경우에만 시간·주변·경로를 확장한다. S11 Live의 후보 탐색과 TripCandidate 저장은 최적화와 분리하고, 후보가 변경안에 들어가려면 사용자의 `담아둔 대안도 포함` opt-in이 필요하다.

### 고정 탐색 순서

1. **날짜:** 같은 여행 기간 안에서 더 낮은 CrowdLevel 날짜
2. **시간:** 시간 단위 근거가 유효한 D-3 이후 또는 당일
3. **주변 POI:** 연관 관광지·위치기반 후보 중 상대적으로 여유로운 장소
4. **경로·경유:** 목적지를 유지하면서 피크 시간 체류를 줄이는 순서
5. **목적지 대체:** `include_trip_candidates=true`일 때 Flexible 항목만 최후 수단. Must Visit은 자동 탐색 대상이 아니며 사용자가 S07 편집에서 잠금 해제 후 직접 교체하는 경로로만 변경된다

### 변경안 생성 조건

- 변경 후 CrowdLevel이 변경 전보다 낮다.
- 변경 전·후가 같은 POI·source·normalizationVersion·`forecastIssueId`에서 계산됐다.
- 여행 기간·영업일·이동 가능 조건을 만족한다.
- Must Visit·date_locked·time_locked·예약 잠금을 변경 차원별로 위반하지 않는다. **자동 목적지 대체는 Must Visit을 대상으로 하지 않는다.**
- DAY·TRIP 경로 개선은 신선한 route matrix가 있을 때만 계산하며 직선거리를 이동시간처럼 표현하지 않는다.
- 데이터 신뢰도가 최소 노출 기준 이상이다.
- 같은 사용자가 최근 유지한 동일 변경안이 아니다.

### OptimizationRun·Proposal 데이터

- scope: `ITEM | DAY | TRIP`
- input_trip_version
- include_trip_candidates
- objective_weights / lock_summary / route_matrix_version
- type
- change_set: 변경 대상 1개 이상의 필드와 from/to 값
- reason_code
- predicted_before_level / predicted_after_level
- confidence
- source_state
- constraint_check_summary
- generated_from_trip_version
- normalization_version
- source_snapshot_ids
- generated_forecast_issue_id
- generated_before_level / generated_after_level
- revalidated_forecast_issue_id / revalidated_at
- revalidated_before_level / revalidated_after_level
- revalidation_result: `UNCHANGED | REFRESH_REQUIRED | DATA_CHANGED`
- rollback_payload 또는 원래 값 참조
- generated_at / expires_at

### 수락 처리

1. 제안 만료·Trip version·예약 잠금·일정 충돌 재검증
2. 최신 예측 발행 회차를 조회한다. 생성 회차와 다르면 최신 회차 하나 안에서 전후 날짜를 다시 계산한다.
3. 개선이 뒤집혔으면 `DATA_CHANGED`로 만료하고 TripItem을 변경하지 않으며 제안을 재계산한다.
4. 개선은 유지되지만 표시 Level이 바뀌었으면 `REFRESH_REQUIRED` 최신 Preview를 반환하고 사용자 확인 전 적용하지 않는다.
5. 동일하거나 재확인된 변경안에만 OptimizationDecision `ACCEPTED` 기록
6. change_set 전체 TripItem 변경을 원자적으로 적용
7. 변경 후 S11 혼잡 예보·S07 일정·후보 유사성·인라인 제안 재계산
8. 성공 응답과 발행 회차·재검증 결과 분석 이벤트 저장

되돌리기는 새 Decision(`REVERTED`)으로 기록하고, 적용 당시의 trip_version과 현재 상태가 충돌하지 않을 때만 원래 값을 복구한다.

2~4는 일관성을 유지하도록 하나의 트랜잭션 또는 보상 가능한 작업으로 처리한다.

## 8.6 Live 탐색·Re-plan 규칙

- Live 탭 조회는 일정이 없어도 사용할 수 있다.
- P0 서버 조회는 사용자가 선택한 공개 장소·권역 ID 또는 현재 Trip의 다음 일정 POI로만 실행한다.
- 선택적 GPS는 브라우저 안에서 거리 계산에만 사용하고 서버·지도 사업자에 전송하지 않는다.
- `현재 여행지`는 선택 장소 또는 다음 일정 POI를 기준으로 기본 반경 3km에서 시작하고 1km·5km를 사용자가 변경할 수 있다.
- `다른 관광지`는 서울 전체를 기본으로 하며 지역·카테고리·원천 상태·실시간만 필터와 사용자 직접 `지금 더 여유로운 곳` on-demand 정렬을 제공한다.
- 실시간 지원 여부, mapping_type, freshness 기준을 통과한 CrowdSnapshot만 `실시간 관측`으로 노출한다.
- AREA_CONTAINS_POI 매핑은 개별 POI의 정확한 내부 혼잡이 아니라 권역 상태임을 카드에 표시한다.
- Live 미지원 장소는 Planning Crowd가 있으면 `공식 혼잡 예측`, 없으면 `공식 혼잡 예측 범위 밖` 정성 Context 또는 `현재 데이터 없음`으로 표시한다.
- 정규화된 CrowdLevel은 일관된 표시 계약이지 서로 다른 제공처·scope의 비교 가능성을 보장하지 않는다.
- Live 후보는 같은 `comparisonGroupId`·`snapshotSetId`에서 `candidate.sourceOrdinal < anchor.sourceOrdinal`인 단일 `ORDINAL_LOWER` Selector를 사용한다.
- 일반 사용자 능동 CTA는 `anchor.sourceOrdinal >= absoluteClaimMinOrdinal`인 ABSOLUTE에만 활성화한다. 초기 설정값은 3이며 코드 변경 없이 4로 올릴 수 있다.
- RELATIVE는 일반 사용자 능동 CTA를 만들지 않는다. 사용자가 `다른 관광지` 탭의 `지금 더 여유로운 곳`을 직접 켠 경우에만 `ON_DEMAND_ONLY`로 결과를 연다.
- 후보는 3km 이내, 중심점 0.5km 이상, 관측시각 편차 10분 이하, 중첩 3단계·운영·접근성·일정·민감지역·수용력·수동검수 Gate를 통과해야 한다.
- REPLAY는 보관·표시조건·완결성·전체 E2E를 검증한 최근 7일 대표 세트만 DemoSession에서 사용하고 `과거 관측·실시간 아님`과 관측시각을 고정 표시한다.
- 우회 기능은 후보 POI 선택·TripCandidate 저장까지를 발견 흐름으로 삼고, 실제 계획 전환은 S07 배치 또는 S09 승인으로 별도 측정한다.
- 우회 후보가 없을 때 다른 소스·scope의 값을 끌어와 순위를 만들지 않는다.
- Planning 원시값과 Live 원시값을 직접 비교하지 않는다.
- P1 Re-plan의 악화 판단은 같은 대상에 대해 교차 소스 보정·검증 계약이 마련된 경우에만 허용한다. 그 전에는 Planning과 Live를 별도 Context로만 표시하고 `악화 폭`을 계산하지 않는다.
- Live 데이터가 지원되는 서울 장소와 최신성 기준을 모두 만족해야 한다.
- Live 미지원 장소에는 Planning 예측이 있을 때만 `공식 혼잡 예측`을 표시하고, 범위 밖에서는 정성 Context만 표시한다.
- 위치정보 법적·운영 검토가 완료되지 않은 P0에서는 서버 전송형 GPS를 제공하지 않는다.
- Re-plan은 P1 Feature Flag가 켜진 경우에만 생성하며 Live 탐색 화면 자체는 P0로 유지한다.

## 8.7 다국어 확장 규칙

- P0는 **한국어 리소스를 먼저 완성**하고 핵심 E2E가 안정화된 뒤 영어 리소스를 추가한다. 모든 UI 문자열을 번역 키로 관리하며 미번역 키 검사·레이아웃 회귀 테스트를 둔다.
- **레이아웃은 1단계부터 영어 문자열 길이를 전제로 설계한다.** 한국어에만 맞춘 고정폭 배지·버튼을 만들지 않는다.
- 언어 전환은 영어 리소스가 추가된 시점에 노출하며 기본 언어는 브라우저 로케일을 따르되 사용자가 변경할 수 있다.
- **UI 문구 영어화와 영문 관광정보 제공은 별개다.** P0 영어 UI에서도 POI 상세 원문은 국문이며, 영문 상세가 없으면 `Official description available in Korean only`로 표시하고 임의 번역하지 않는다.
- EngService2 기반 영문 관광정보는 P1 데이터 검수가 통과하기 전에는 노출하지 않는다.
- P1에서도 KorService2와 EngService2 contentId가 같다고 가정하지 않는다.
- 공식 cross-language key가 없으면 Canonical POI를 중심으로 연결하고 High confidence만 자동 연결한다.
- Medium은 운영 검수, Low는 해당 언어 상세를 제공하지 않는다.
- UGC 원문을 기본으로 하고 자동 번역은 P2로 둔다.

## 8.8 SNS 안전 규칙

- 신고·숨김·차단이 구현된 경우에만 UI를 노출한다.
- 숨김·삭제·위험 콘텐츠는 Feed 후보에서 즉시 제외한다.
- 공식 Seed는 UGC처럼 위장하지 않는다.
- 이미지 저작권·출처 메타데이터를 보존한다.

---

# 9. 데이터 설계

## 9.1 Planning Crowd

```text
P0 PlanningLevel =
  NormalizeWithinPoi(PredictedConcentration, normalization_version)

PlanningContext =
  EventContext + WeatherContext + CalendarContext + InternalDemandContext
```

- PredictedConcentration: 한국관광공사의 관광지별 상대 집중률 예측; 같은 POI의 날짜 비교에만 사용
- EventContext: 여행일에 실제 운영되는 행사 정보; P0에서는 별도 주의 표시
- WeatherContext: 현재·예보 날씨; 관측 혼잡값을 변경하지 않고 P1 행동 제안에 사용
- InternalDemandContext: 서비스 내부 TripCandidate 저장 수요; 최소 표본 N_MIN 이상인 P1에서만 사용
- CalendarContext: 공식 달력으로 확인한 요일·공휴일 정성 Context. 공식 혼잡 예측과 별도 상태로 표시하며 CrowdLevel을 만들지 않음

P0에서는 서로 단위가 다른 값을 더해 하나의 PlanningCrowd 원시점수를 만들지 않는다. P1에서 복합 점수가 필요하면 각 성분을 정규화하고 가중치·학습기간·`normalization_version`을 별도 검증한 뒤 도입한다. N_MIN 미만에서는 InternalDemandContext를 사용하지 않는다.

## 9.2 CrowdLevel·원천 서수 정규화

Planning Crowd의 내부 5단계는 같은 POI의 날짜 비교에만 사용한다.

| 내부 Level | 의미 | Planning 사상 예시 |
|---:|---|---|
| 1 | 매우 여유 | 관광지별 예측 분포 하위 구간 |
| 2 | 여유 | 낮은 집중 구간 |
| 3 | 보통 | 중간 집중 구간 |
| 4 | 혼잡 | 높은 집중 구간 |
| 5 | 매우 혼잡 | 최상위 집중 구간 |

서울 Live는 원천 4단계를 보존한다.

| sourceState | sourceOrdinal | 호환용 내부 Level |
|---|---:|---:|
| 여유 | 1 | 1 |
| 보통 | 2 | 3 |
| 약간 붐빔 | 3 | 4 |
| 붐빔 | 4 | 5 |

- 서울 Live에는 내부 Level 2를 창작하지 않는다.
- UI는 숫자 Level보다 원천 상태 문구를 우선한다.
- 서울 공간 개선폭은 `sourceOrdinal` 차이로 계산한다. 호환용 내부 Level 차이는 `보통→여유`를 2단계로 과장할 수 있어 사용하지 않는다.
- Planning cut-off는 관광지별 예측 분포로 캘리브레이션하되 같은 POI·같은 `forecastIssueId`의 날짜 비교에만 사용한다.
- 서로 다른 출처·scope의 원시값 또는 숫자 Level 차이는 계산하지 않는다.
- 모든 CrowdSnapshot에 target_type, scope, source, source_state, source_value, source_ordinal, target_at, observed_or_predicted_at, confidence, normalization_version, comparison_group_id를 저장한다.

## 9.3 서울 Live 장소와 TourAPI POI 매핑

`SeoulLiveAreaMap`을 별도로 둔다.

| mapping_type | 의미 |
|---|---|
| EXACT_POI | 실시간 장소와 POI가 직접 대응 |
| AREA_CONTAINS_POI | 실시간 권역 안에 POI 포함 |
| NEARBY | 인접하지만 동일 장소는 아님 |
| NO_MAPPING | 연결하지 않음 |

- 서울시 장소 수를 상수로 하드코딩하지 않는다.
- 최신 코드 목록을 정기 동기화하고 대표 POI를 수작업 검수한다.
- 한 Live Area가 여러 TourAPI POI를 포함할 수 있다.
- 매핑되지 않은 POI에는 실시간 배지를 제공하지 않는다.
- Live 화면은 area_code 단위 CrowdSnapshot과 Canonical POI를 함께 반환하되, `mapping_type`과 `area_name`을 프론트에 전달한다.
- AREA_CONTAINS_POI·NEARBY는 카드에 `○○ 일대 기준`을 표시하고 개별 시설 내부의 정확한 혼잡처럼 표현하지 않는다.
- `다른 관광지` 목록은 Live 매핑 관광지와 Planning Crowd 관광지를 함께 조회할 수 있지만 sourceState별 그룹으로 반환하고, 혼합 혼잡 순위를 만들지 않는다.

측정 비교그룹과 실제 대체 관계를 분리한다.

```text
comparison_group_id =
  source + metric + scope_type + normalization_or_mapping_version
```

카테고리·거리·대체 가능성·검수 여부는 `comparison_group_id`에 넣지 않고 `CandidateRelation`으로 관리한다.

```text
CandidateRelation
- anchor_target_id / candidate_target_id / substitution_set_id
- category_similarity_reason / interest_similarity_reason
- distance_km / min_separation_passed
- overlap_ratio / overlap_gate_result / overlap_policy_version
- geometry_version / geometry_source_hash / overlap_calculation_version
- mapping_type
- operating_constraint_status / sensitivity_constraint_status
- capacity_constraint_status / curation_status
- reviewed_by / cross_checked_by / reviewed_at / version
```

권역 중첩률은 서울시 공식 hotspot polygon을 입력으로 PostGIS `ST_Intersection`·`ST_Area`로 계산한다. 연구용 45×45 격자 근사값은 구조 진단에만 사용하고 운영 승인값으로 사용하지 않는다.

## 9.4 Canonical POI

내부 UUID를 중심으로 P0의 관광공사 POI·서울 Live 권역 등 외부 장소 ID를 연결한다. 언어별 관광정보 연결은 P1이다.

1. P0는 공식 ID·좌표·정규화 명칭·주소로 KTO POI와 Live 권역 후보를 연결
2. P1 다국어는 공식 cross-language key를 먼저 확인하고, 없으면 좌표 근접 + 정규화 명칭 유사도 + 주소·전화 보조 특성 사용
3. match_confidence 저장
4. High 자동, Medium 검수, Low 미연결

P1에서는 잘못된 영문 정보를 보여주는 것보다 `영문 상세 없음`을 우선한다.

## 9.5 데이터 신선도와 캐시 원칙

공모전 심사 핵심 경로는 한국관광공사 OpenAPI를 서버에서 실제 호출하고 호출 이력을 남긴다. P0는 관광공사 응답 전체를 장기 로컬 DB 미러로 운영하지 않으며, 내부 UUID·외부 ID 매핑·검수 상태·사용자 생성 도메인 데이터만 영속화한다. P1 언어 매핑도 원문 복제가 아닌 연결 metadata로 관리한다. 외부 응답은 이용조건과 쿼터를 지키는 단기 cache-aside를 기본으로 한다.

공식 운영 안내상 부득이한 로컬 DB 저장은 별도 문의 대상이므로, 관광공사 원문·이미지 메타를 장기 영속화해야 한다면 `tourapi@knto.or.kr`에 저장 범위·갱신·호출 검증 방식을 서면 확인한 뒤 Source Registry에 승인 근거를 기록한다.

| 데이터 | 초기 캐시 정책 | 실패 시 |
|---|---|---|
| 관광정보·이미지 메타 | 실제 OpenAPI 호출 + 설정형 단기·중기 캐시; 영속 미러는 사전 확인 후 | 유효 캐시와 마지막 호출시각 표시, 없으면 카드 비노출 |
| 집중률 예측 | 짧은 TTL, stale 여부 기록 | 유효 캐시 → `공식 혼잡 예측 범위 밖`; 수치 참고값 생성 금지 |
| 서울 Live | 점검 창 기반 수집 배치와 매우 짧은 조회 TTL | 실시간 배지 제거; 대표 REPLAY는 별도 검증 경로로만 사용 |
| 기상청 단기·중기 | 발표·갱신주기 기반 TTL | 날씨 Context·P1 WeatherFit 제거, 혼잡 분석 유지 |
| 연관 관광지 | 설정형 단기·중기 TTL | 검수된 P0 후보 또는 카테고리 후보 |
| 지도 거리·경로 | 약관·쿼터 범위 내 캐시 | 직선거리 또는 이동시간 확인 불가 |

정확한 TTL은 각 API의 최신 이용조건·갱신주기 확인 후 설정 파일로 확정한다.

P0 Live 서버 조회는 공개 `anchorId`만 받으며 사용자 GPS 좌표를 캐시 키·URL·로그·DB에 남기지 않는다. 관광지별 CrowdSnapshot 캐시는 사용자 위치와 분리한다. 서버 전송형 GPS를 P1에서 검토할 때는 위치기반서비스 신고와 전체 프록시·CDN·APM 로그 경로를 먼저 검증한다.

서울 Live Snapshot은 한 수집 사이클을 하나의 배치로 정의한다.

```text
collector_run_id = UUID generated at collection-cycle start
snapshot_set_id = collector_run_id
observed_at_min = min(records.observed_at)
observed_at_max = max(records.observed_at)
observed_at_skew = observed_at_max - observed_at_min
```

- 같은 수집 사이클의 레코드만 같은 SnapshotSet에 포함한다.
- 초기 `max_observed_at_skew`는 배치 내 최대-최소 10분이다. 공식 API 7일 p95로 축소만 검토하고 자동 확대하지 않는다.
- 편차가 넘으면 지연 권역을 제외하고 후보은행 완결성을 다시 계산하며 관측시각을 수집시각으로 덮어쓰지 않는다.
- freshness는 `fetched_at`이 아닌 원 `observed_at` 기준으로 판정하고 provider lag를 별도 저장한다.
- rolling 7일은 운영 readiness 판정에, rolling 28일은 요일·계절 경향 보고에 사용한다.

REPLAY 대표 세트는 Source Registry 검증, CandidateRelation 전체 Gate, 관측시각 편차, Header→후보→상세→`담아둔 장소` E2E를 통과해야 한다. 최근 7일 안의 11:00~20:00 관측을 우선하고 서로 다른 유효 앵커 수·검수 후보쌍 수·후보 `sourceOrdinal<=2`·최근성 순으로 선택한다. 최소 주 1회 교체하고 7일을 넘으면 만료한다.

## 9.6 OpenAPI 호출 예산·운영계정 Gate

공공데이터포털의 **API별 현재 승인량**을 source of truth로 사용한다. 2026-08-24 공식 페이지에서 국문 관광정보 서비스와 관광지 집중률 예측 정보의 개발계정은 각각 일 1,000건, 운영계정은 활용사례 등록 후 증설 신청 가능으로 확인했지만, 이 값을 다른 API나 향후 승인량에 일반화하지 않는다.

| 관리값 | 기준 |
|---|---|
| `Q_approved` | My Page에 표시된 API·키별 일 승인량; 캡처·확인일·신청번호를 `EVID-KTO-QUOTA`에 기록 |
| `Q_projected_p95` | Seed 동기화 + 예측 후보은행 점검 + 캐시 갱신 + 심사 동시접속을 합친 일 p95 예상 호출량 |
| 정상 운영 Gate | `Q_projected_p95 <= 0.5 × Q_approved` |
| 심사 예비량 | 매일 `Q_approved`의 최소 30%를 심사 세션용으로 예약하고 배치가 소진하지 못하게 함 |
| 경보 | 60% 주의, 80% 배치 중단·TTL 연장, 90% 신규 비핵심 호출 차단 |

- 국문 관광정보·집중률 예측 API는 즉시 운영계정/증설을 신청하고 활용사례, 예상 트래픽, 서비스 URL, 캐시 전략, 신청번호·상태를 근거대장에 기록한다.
- 후보은행 점검은 사용자 요청과 분리된 Scheduler에서 batch·request coalescing으로 실행하고, 같은 POI·날짜 요청을 카드별로 반복하지 않는다.
- 서울 Live의 실제 키 승인량을 확인하기 전에는 121개 권역 5분 전수 수집을 전제하지 않는다. `121 × 12 × 24 = 34,848 calls/day`이므로 다음 두 프로필로 분리한다.
  - 검수 후보은행: readiness 점검 창 중심의 조밀 수집
  - 신규 앵커 발굴: 121개 전수 스캔 일 1~2회
- 운영계정 승인이 늦어져도 개발계정 한도 안에서 심사 예비량이 유지되는 축소 프로필을 준비한다. 축소 프로필은 Seed 갱신 빈도를 낮추되 심사 핵심 POI와 실제 호출 증거를 유지한다.
- `429`, 한도 초과 응답, 잔여량 계산 실패를 계약 테스트하고 Circuit Breaker가 비핵심 갱신부터 중단하게 한다.
- 승인량·운영계정 상태·최근 7일 peak·예상 심사 동시접속 중 하나라도 미확인하면 Submission Gate를 통과시키지 않는다.

---

# 10. 외부 데이터·API 활용 계획

## 10.1 한국관광공사 OpenAPI

| API | 사용처 | 화면 | P0 여부 |
|---|---|---|---|
| 국문 관광정보 서비스_GW / KorService2 | Canonical POI 매핑, 검색, 상세, 이미지, 선택 POI 기준 위치기반 후보, 행사 | Feed, POI, S10 인라인 | 필수 |
| 영문 관광정보서비스_GW / EngService2 | 외국인용 POI·상세·이미지 | P1 영문 Feed, POI | P1; P0 제출 목록에서 제외 |
| 관광지 집중률 방문자 추이 예측 정보 | 같은 POI·같은 발행 회차의 날짜별 상대 혼잡 | S11 혼잡 예보, S07, ITEM Optimization Preview | 필수 |
| 관광지별 연관 관광지 정보 / TarRlteTarService1 | P0 검수 연관 후보 1개, P1 주변·경유 후보 | POI 상세, S10 | 필수 권장 |
| 빅데이터 지역별 방문자수_GW | 지역 단위 연구 보조 | P1 분석 | 실제 구현 시만 기재; CrowdLevel 생성 근거로 사용 금지 |
| 일문·중문 관광정보 | 다국어 확장 | P2 | 제출 설명서에는 미구현 시 제외 |

실제 Endpoint 명칭, 요청 파라미터, 응답 필드는 공공데이터포털의 최신 Swagger·명세와 샘플 응답으로 확정하고 계약 테스트를 작성한다.

데이터 의미의 한계도 화면과 S15에 명시한다.

- 관광지 집중률 예측은 KT 이동통신 기반의 관광지별 상대값으로, 외국인 관광객만의 수요나 실제 현장 인원·대기시간을 뜻하지 않는다.
- 서울 Live 인구 혼잡은 권역의 생활·체류 인구 Context이며 관광객만의 혼잡이나 개별 시설 내부 혼잡이 아니다.
- 따라서 두 데이터는 일정 선택을 돕는 참고정보로 사용하고 타깃 사용자의 실제 방문 결과처럼 일반화하지 않는다.

## 10.2 기타 API

| 제공처 | 데이터 | 사용처 | 노출 조건 |
|---|---|---|---|
| 서울특별시 | 서울 실시간 도시·인구·날씨 Context | Live 탭 P0, Live Re-plan P1 | 장소 매핑·최신성·범위 표시 검증 후 |
| 기상청 | 단기예보 | P1 D-3~당일 시간·실내외 제안 | P1 실제 구현 시 |
| 기상청 | 중기예보 | P1 D-10~D-4 날짜 주의 | P1 실제 구현 시 |
| 지도 사업자 | 거리, 이동시간, 지오코딩, 경로 | P1 일반 주변·경유 검증 | P1 사업자·쿼터·라이선스 확정 후 |

지도 사업자는 외국인 UX, PWA SDK, 경로 API, 가격·쿼터, 라이선스를 비교해 하나를 확정한다.

## 10.3 출처·키·호출 로그

- 인증키는 서버 환경변수 또는 Secret Manager에만 저장한다.
- 브라우저 번들, Git, 로그 원문, 사이트 화면, 기능설명서에 인증키를 넣지 않는다.
- 실제 호출 API명, 호출 시각, 성공·실패, 캐시 hit 여부를 운영 로그에 남긴다.
- API·키별 일 호출량, 60/80/90% 경보, 배치 중단 여부와 운영계정 신청 상태를 운영 대시보드·근거대장에 남긴다.
- 기능설명서와 제출 시스템에는 최종 서비스에서 실제 사용한 API만 작성한다.
- 파일데이터만 사용하는 경로는 OpenAPI 활용 실적으로 인정되지 않을 수 있으므로 심사 핵심 경로에서 실제 OpenAPI 호출을 유지한다.
- 한국관광공사 데이터가 사용된 화면에는 텍스트 출처를 표시한다.

---

# 11. 도메인 모델

| 엔티티 | 핵심 필드 | 용도 |
|---|---|---|
| User | id, locale, home_country, preference_profile | 사용자와 선호 |
| DemoSession | id, anonymous_token_hash, baseline_version, forecast_scenario_id, data_mode, claim_mode, cta_policy, snapshot_set_id, expires_at | 심사 사용자별 독립 데모와 시간·공간 분산 Snapshot 고정 |
| ItineraryImportDraft | request_id, input_type(PASTE/TXT/CSV/XLSX/MANUAL), parse_status, source_file_name, source_sheet, column_mapping, expires_at | 확정 전 일정 분석 작업; raw_text와 원본 파일은 기본 비영속 |
| InterestSignal | trip_draft_id 또는 trip_id, interest_code, score, evidence, confidence, confirmed | 일정에서 추출한 관심사와 근거 |
| TripInterest | trip_id, interest_code, weight, evidence, source_signal_ids, confirmed_by_user | 사용자가 확정·수정한 Trip별 Feed 개인화 입력 |
| EmptySlot | trip_id, date, start_time, end_time, duration_minutes, confirmed | Feed·추천에 사용할 일정 여유 시간 |
| Post | id, author_id(nullable), source_type(SEED/UGC), canonical_poi_id, caption, media, visibility, moderation_status, report_count, created_at | 여행 게시글과 공식 Seed·UGC 구분 |
| SavedPost | user_id 또는 demo_session_id, post_id, saved_at | 여행과 무관한 콘텐츠 북마크 |
| CanonicalPOI | id(UUID), ko_content_id, lat, lng, category, mapping_status | 언어·출처 통합 기준 |
| LocalizedPOI | canonical_poi_id, locale, external_content_id, name, description, match_confidence | 언어별 관광정보 |
| SeoulLiveAreaMap | area_code, area_name, canonical_poi_id, mapping_type, reviewed_at | Live 권역 연결 |
| LiveArea | area_code, area_name, centroid, support_status, source, synced_at | Live 탭의 지원 권역·직접 선택 목록 |
| Trip | id, user_id 또는 demo_session_id, start_date, end_date, timezone, base_area, locale, status, version | 여행 단위와 낙관적 잠금 |
| TripConstraint | trip_id, transport_mode, party_size, max_travel_minutes, max_walk_minutes, wheelchair, stroller, value, confirmed | 사용자가 확인한 이동·그룹·접근성 제약 |
| TripCandidate | id, trip_id, canonical_poi_id, source_post_id, source_surface, status(ACTIVE/SCHEDULED/REMOVED), added_at | Feed·검색·Live에서 담은 날짜·시간 미배치 여행 후보 |
| CandidateTripMatch | candidate_id, trip_item_id, match_type(EXACT/SIMILAR/NONE), reason_codes, score, relation_version, generated_from_trip_version | 담아둔 장소와 현재 일정의 동일·유사 관계와 설명 근거 |
| TripItem | id, trip_id, canonical_poi_id, start_at, end_at, timezone, destination_locked, date_locked, time_locked, lock_sources, source_post_id, source_candidate_id, sort_order | 확정 일정 항목과 목적지·날짜·시간·예약 잠금 분리 |
| OptimizationRun | id, trip_id, scope(ITEM/DAY/TRIP), primary_trip_item_id, input_trip_version, include_trip_candidates, objective_weights, lock_summary, status, generated_at, expires_at | 현재 화면 범위와 조건을 고정한 최적화 실행 |
| OptimizationProposal | id, optimization_run_id, change_set, reason_codes, metric_before, metric_after, confidence, source_snapshot_ids, route_matrix_version, normalization_version, generated_forecast_issue_id, revalidated_forecast_issue_id, revalidation_result, rollback_payload | 지도·타임라인 전후 비교와 최신 데이터 재검증을 포함한 적용 전 변경안 |
| OptimizationDecision | id, optimization_proposal_id, actor_user_id 또는 demo_session_id, decision, sequence, unlock_confirmed, decided_at, idempotency_key | 적용·유지·되돌리기 append-only 이력 |
| RouteMatrixSnapshot | id, provider, origin_poi_id, destination_poi_id, mode, departure_bucket, duration_seconds, distance_meters, observed_at, expires_at, quality | P1 도착시각·순서·경로 최적화의 검증 입력 |
| CrowdSnapshot | target_type, target_id, scope, source_state, source, source_value, source_ordinal, normalized_level, normalization_version, comparison_group_id, comparison_policy_version, collector_run_id, snapshot_set_id, observed_or_predicted_at, observed_at_min, observed_at_max, observed_at_skew_seconds, forecast_issue_id, target_at, fetched_at, provider_lag_seconds, confidence, replay_eligible_until | 현재·예측·검증된 재생 관측과 시간·공간 비교 계약 |
| CandidateRelation | anchor_target_id, candidate_target_id, substitution_set_id, distance_km, overlap_ratio, overlap_gate_result, geometry_version, mapping_type, constraint_statuses, curation_status, reviewed_by, cross_checked_by, version | 측정 비교그룹과 분리된 실제 대체 가능성·공간 Gate·검수 관계 |
| ReplaySetSelection | snapshot_set_id, selected_at, selected_reason, observed_at, expires_at, selection_policy_version, full_e2e_verified, status | 대표 REPLAY 선택·주 1회 갱신·D-2 경보·만료 이력 |
| DemoForecastCandidate | canonical_poi_id, before_date, after_date, before_level, after_level, source_snapshot_ids, constraint_status, readiness, checked_at, expires_at | 후보은행과 `DEMO_READY` 판정 |
| OpenApiQuotaEvidence | api_id, key_alias, account_stage, approved_daily_calls, application_id, status, checked_at, evidence_ref | API별 승인량·운영계정 신청 근거 |
| WeatherSnapshot | area_id, source, issued_at, target_at, rain_prob, condition, confidence | 날씨 이력 |
| FeedImpression | user_id 또는 demo_session_id, post_id, rank, score, context, shown_at, dwell_ms | 피드 평가 |
| UserActionEvent | user_id 또는 demo_session_id, event_type, post_id, poi_id, trip_id, metadata, occurred_at | 행동 분석 |
| Report | reporter_id, post_id, reason, status, created_at, reviewed_at | 모더레이션 |
| ExposureBudget | target_type, target_id, time_window, max_impressions, current_impressions, saturation_state | P1 트래픽 기반 POI·권역·시간대 추천 집중 방지 |
| PlaceConstraint | canonical_poi_id 또는 area_code, constraint_type, value, valid_from, valid_to, source, reviewed_at | 운영·예약·접근성·민감 지역·수용력 Gate |

## 11.1 필수 무결성 규칙

- TripItem의 날짜는 Trip 기간 안이어야 한다.
- 동일 Trip·CanonicalPOI의 ACTIVE TripCandidate와 확정 TripItem 중복 정책을 DB와 API에서 동일하게 적용한다.
- TripCandidate 저장은 Trip version을 변경하지 않고, TripItem 배치·수정·교체만 version을 올린다.
- OptimizationDecision은 append-only 이벤트이며 같은 idempotency_key를 중복 처리하지 않는다. 현재 상태는 가장 마지막 유효 sequence로 계산한다.
- destination_locked TripItem은 **시스템이 자동 생성하는** DESTINATION_REPLACE OptimizationProposal의 대상이 될 수 없다. date_locked·time_locked 항목은 해당 차원의 변경을 생성할 수 없다.
- 사용자가 명시적으로 요청한 교체(`USER_INITIATED_SWAP`)는 destination_locked 항목에도 허용하되, **잠금 해제 확인 단계를 거쳐야 하며** 해당 Decision에 `unlock_confirmed=true`를 기록한다.
- TripItem의 start_at은 end_at보다 빨라야 하며 Trip timezone과 여행 기간 안에 있어야 한다.
- OptimizationRun은 생성 당시 Trip version, scope, 후보 포함 여부, 잠금 요약을 가져야 한다.
- OptimizationProposal은 normalization_version·source_snapshot_ids와 생성 당시 Trip version을 Run을 통해 고정한다.
- 시간축 Proposal은 `generated_forecast_issue_id`가 필수이며 적용 전 최신 발행 회차에서 개선 방향을 재검증한다. `DATA_CHANGED`이면 TripItem을 변경할 수 없다.
- DAY·TRIP Proposal이 이동시간·경로 개선을 표시하려면 유효한 RouteMatrixSnapshot version이 필수다.
- CrowdSnapshot은 target_type, scope, source, source_state, 기준시각, normalization_version, comparison_group_id 없이 저장할 수 없다.
- 서울 Live 공간 비교 Snapshot은 `source_ordinal`, `collector_run_id`, `snapshot_set_id`, `observed_at_skew_seconds`가 필수다.
- `source_state=REPLAY`인 CrowdSnapshot은 원 관측시각·재사용 근거·만료일이 있어야 하며 현재 Live 집계에 포함할 수 없다.
- CandidateRelation은 0.5km 최소 이격·중첩 Gate·운영·접근성·민감지역·수용력·교차 검수 상태를 모두 가져야 자동 노출될 수 있다.
- LocalizedPOI는 match_confidence가 낮으면 자동 노출하지 않는다.
- 사용자가 확인하지 않은 InterestSignal과 Must Visit 후보는 확정 Trip 값으로 저장하지 않는다.
- TripInterest는 evidence 또는 사용자가 직접 추가했다는 source를 가져야 한다.
- EmptySlot은 Trip 기간 밖이거나 확정 TripItem과 겹칠 수 없다.
- 사용자 원시 위치 좌표와 일정 원문은 UserActionEvent metadata에 저장하지 않는다.
- 모든 Trip·TripCandidate·TripItem·Optimization API는 인증 사용자 또는 DemoSession의 소유권을 검사한다.

---

# 12. 시스템·API 설계

## 12.1 권장 아키텍처

| 컴포넌트 | 책임 |
|---|---|
| PWA | 여행 게시글 Feed, 일정 입력·추출 결과 확인, Live, TripCandidate, S07 보기·편집, S09 지도·타임라인 Preview, 선택적 기기 내 위치 계산, i18n-ready 반응형 UI |
| Spring Boot API | 인증, 일정 구조화 orchestration, SNS, TripCandidate·TripItem, POI, Live, Optimization, 외부 API orchestration, 룰·경로 최적화 |
| PostgreSQL | 사용자 도메인 데이터, Canonical POI 식별자·검수 매핑, 제안 이력·로그; 관광공사 응답 전체의 무승인 영속 미러 제외 |
| Redis | 다중 인스턴스 운영 시 Feed 후보, 혼잡·날씨 캐시, rate limit, Feature Flag |
| Object Storage/CDN | UGC 이미지 - P1 |
| Scheduler/Worker | 허용된 범위의 코드·매핑 갱신, Live 권역 수집, 예측 후보은행·행사·예보 cache refresh, 쿼터 예산·재현성 경보 |
| FastAPI ML Service | 학습 랭킹이 실제 필요할 때만 P2로 분리 |

P0는 Spring Boot 단일 백엔드의 결정적 한국어 파서·구조화 파이프라인과 룰 기반 ITEM 최적화로 완성하며 외부 AI를 호출하지 않는다. P1은 route matrix와 시간창 optimizer를 추가하고, LLM을 사용하더라도 선호 해석·근거 설명에 제한한다. 단일 인스턴스 데모는 애플리케이션 캐시·DB로 시작할 수 있으며 Redis를 선행 조건으로 두지 않는다.

## 12.2 프론트엔드 핵심 상태

- currentUser / locale
- itineraryInput / parseResult / reviewEdits / confirmState
- selectedTrip / tripItems / tripVersion
- tripCandidates / candidateMatches / candidatePanelState
- editMode(VIEW/MANUAL_EDIT/AI_OPTIMIZE) / tripEditDraft / dirtyFields
- lockDraft(destination/date/time/reservation) / unlockConfirmation
- tripInterests / emptySlots
- feed pages / active filters
- POI detail / localized content
- crowdForecast(3구간) / source state
- optimizationScope(ITEM/DAY/TRIP) / includeTripCandidates(default=false)
- optimizationRun / optimizationProposal / optimizationDecision / routePreview
- applyState(PREVIEWING/APPLYING/APPLIED/STALE/FAILED) / undoWindow
- liveScope / optionalDeviceLocationPermission / selectedAnchor / radius / liveFilters / relativeFilterEnabled / dataMode / claimMode / ctaPolicy / livePlaces
- API error / fallback / lastUpdatedAt
- featureFlags: english, tripWideOptimization, liveReplan, weather, search, createPost

서버 상태는 쿼리 캐시를 사용하되, 후보 저장 후에는 `TripCandidate`·match 캐시만, 일정 편집·교체·최적화 적용 후에는 Trip·TripItem·CandidateTripMatch·Optimization·혼잡 예보 캐시를 함께 무효화한다. Preview를 연 상태에서 `tripVersion`이 바뀌면 적용 버튼을 비활성화하고 새로 계산한다. 사용자가 보지 않은 일정이나 후보를 클라이언트가 임의로 Optimization 입력에 추가하지 않는다.

## 12.3 내부 API 초안

| Method | Endpoint | 기능 | P0 |
|---|---|---|---|
| POST | `/api/demo/session` | 복원 가능한 데모 세션 생성 | 필수 |
| GET | `/api/demo/readiness` | 7일 운영·28일 경향, 시간 후보, 앵커 분산 CandidateRelation, REPLAY 신선도·E2E, 쿼터 상태의 비밀값 없는 준비도 | 필수 운영 |
| POST | `/api/auth/login` | 테스트·일반 로그인 | 선택 |
| POST | `/api/trips` | 여행 생성 | 필수 |
| POST | `/api/trips/import/parse` | 붙여넣기·TXT 또는 CSV·XLSX 파일을 받아 Trip 초안과 필요 시 열 매핑 상태 반환 | 필수 |
| POST | `/api/trips/import/remap` | CSV·XLSX의 사용자 확정 열 매핑으로 같은 초안을 재생성 | 필수 |
| POST | `/api/trips/import/confirm` | 사용자가 수정·확정한 초안으로 Trip 생성 | 필수 |
| GET | `/api/trips/:tripId` | 여행·일정 조회 | 필수 |
| PATCH | `/api/trips/:tripId/interests` | 관심사·근거·빈 시간 확인값 수정 | 필수 |
| PATCH | `/api/trips/:tripId/items/:itemId` | 일정 항목·Must Visit·날짜 고정·시간 고정 수정; `If-Match` Trip version 검사 | 필수 |
| DELETE | `/api/trips/:tripId/items/:itemId` | 일정 항목 삭제 | 필수 |
| GET | `/api/feed` | Context 기반 Feed | 필수 |
| POST | `/api/feed/feedback` | P0 관심 없음·숨김·저장, P1 UGC 신고 등 Feed 행동 | 필수 최소 |
| GET | `/api/pois/:poiId` | P0 국문 POI 상세; P1 locale 확장 | 필수 |
| GET | `/api/trips/:tripId/candidates` | 여행별 담아둔 장소와 EXACT/SIMILAR/NONE 상태 조회 | 필수 |
| POST | `/api/trips/:tripId/candidates` | Feed·검색·Live 장소를 날짜 미정 후보로 담기; 중복이면 기존 후보 반환 | 필수 |
| DELETE | `/api/trips/:tripId/candidates/:candidateId` | 담아둔 장소에서 제거 | 필수 |
| GET | `/api/trips/:tripId/candidates/:candidateId/matches` | 현재 Trip version 기준 동일·유사 일정과 근거 조회 | 필수 |
| POST | `/api/trips/:tripId/items` | 직접 입력 또는 후보를 날짜·시간에 배치; `candidateId` 선택 입력 | 필수 |
| POST | `/api/trips/:tripId/items/:itemId/replace` | 사용자가 고른 후보로 항목 교체; 잠금 해제 확인·version 검사 | 필수 |
| GET | `/api/pois/:poiId/crowd-forecast` | 장소별 3구간 혼잡 예보 (오늘·주간·예측 범위 밖 정성 Context) | 필수 |
| POST | `/api/trips/:tripId/optimizations` | 현재 Trip version과 `scope`, 화면 범위, 잠금, `includeTripCandidates`로 Preview 계산 | ITEM 필수, DAY/TRIP P1 |
| GET | `/api/optimizations/:runId` | 실행 상태와 지도·타임라인·지표 변경안 조회 | 필수 |
| POST | `/api/optimizations/:runId/decisions` | 변경안 전체 적용 또는 유지; 최신 version·예측·경로 재검증 | 필수 |
| POST | `/api/optimizations/:runId/revert` | 적용된 change set 원자적 되돌리기 | 필수 |
| GET | `/api/pois/:poiId/related` | 연관·주변 후보 | 필수 권장 |
| GET | `/api/live/areas` | 지원 Live 권역과 직접 선택 장소 목록 | 필수 |
| GET | `/api/live/nearby` | 공개 기준 장소·반경의 주변 혼잡 장소 목록 | 필수 |
| GET | `/api/live/places` | 지역·카테고리·혼잡·상태별 다른 관광지 목록 | 필수 |
| GET | `/api/live/places/:areaOrPoiId` | 선택 장소 Live·Planning Context | 필수 |
| POST | `/api/live/replan` | Live 악화 Context로 `scope=DAY` OptimizationRun 생성; 직접 일정 변경 금지 | P1 |
| POST | `/api/events` | 프론트 분석 이벤트 | 필수 |

Optimization API 공통 계약:

- 요청에는 `tripVersion`, `scope`, `visibleItemIds` 또는 `targetDate`, `includeTripCandidates`, `objectiveWeights`를 보낸다. TRIP 범위는 전체 Trip을 뜻하므로 `visibleItemIds` 대신 모든 날짜를 서버가 소유권·version 기준으로 확정한다.
- `includeTripCandidates` 기본값은 `false`다. 사용자가 `담아둔 대안도 포함`을 켠 실행에서만 ACTIVE 후보를 목적지 교체 탐색에 포함한다.
- 응답 `changeSet`은 이동·교체·추가·삭제를 항목별로 구분하고, `metricBefore/After`, `reasonCodes`, `lockSummary`, `routePolyline`, `routeMatrixVersion`, `sourceSnapshotIds`, `expiresAt`을 제공한다.
- `APPLY`는 change set 전체가 재검증을 통과할 때만 한 트랜잭션으로 반영한다. 하나라도 실패하면 어떤 TripItem도 바꾸지 않고 `TRIP_CHANGED`, `DATA_CHANGED`, `ROUTE_UNAVAILABLE`, `LOCK_CONFLICT` 중 하나를 반환한다.
- `KEEP`은 일정을 바꾸지 않으며 Decision 이력만 기록한다. `REVERT`도 별도 append-only Decision으로 기록한다.

Live API 주요 Query 계약:

- `/api/live/nearby`: P0는 `anchorType=PLACE|NEXT_TRIP_ITEM`, `anchorId`, `radiusKm=1|3|5`, `sort`, `category`; GPS 좌표 파라미터 없음
- `/api/live/places`: `region`, `category`, `sourceState`, `liveOnly`, `anchorId`, `radiusKm`, `sort`, `relativeFilter`, `cursor`. 일반 사용자의 `relativeFilter=true`는 `다른 관광지` 탭의 직접 조작에서만 보내며 서버는 `ctaPolicy=ON_DEMAND_ONLY`로 기록한다.
- 서버는 `candidate.sourceOrdinal < anchor.sourceOrdinal`, 같은 `comparisonGroupId`·`snapshotSetId`, 10분 편차, CandidateRelation Gate를 강제한다. 일반 클라이언트가 `dataMode=REPLAY`나 snapshot ID를 임의 선택하지 못하게 한다.
- REPLAY Snapshot은 DemoSession에 서버가 고정하고 `sourceState=REPLAY` 안에서만 비교한다.
- P1 서버 전송형 위치 API가 필요하면 별도 POST 계약과 위치정보 신고·동의·전체 로그 비수집 검증을 먼저 완료한다. P0 GET URL에는 좌표를 넣지 않는다.
- 응답 장소마다 anchor 기준 distance, sourceValue, sourceOrdinal, normalizedLevel, sourceState, comparisonAxis, comparisonEligible, comparisonReasonCode, dataMode, selectorMode, claimMode, ctaPolicy, comparisonGroupId, collectorRunId, snapshotSetId, observedAtSkewSeconds, candidateRelationId, overlapRatio, isCurrent, source, scope, scopeLabel, observedAt/targetAt, lastUpdatedAt, normalizationVersion, confidence를 반환한다.

`/api/demo/readiness`의 공간 판정은 다음과 같다.

```text
spatialDemoReady =
  sourceRegistryValid
  AND curatedRelationBankValid
  AND replayReady
```

- `currentLiveReady`는 현재 Live를 먼저 보여줄 수 있는 보너스 상태이며 REPLAY를 대신하지 않는다.
- 최근 7일 서로 다른 날짜 7개, 평일·주말/공휴일 표본, 하루 2개 점검 창을 초기 운영 Gate로 둔다. 첫 주 운영 후 6/7 완화 여부를 검토하되 정책 버전 없이 변경하지 않는다.
- 검수 앵커 3개 이상, CandidateRelation 5개 이상, 5개 관계가 최소 2개 앵커에 분산돼야 한다.
- REPLAY는 7일 이내·Source Registry 유효·전체 E2E 통과·최소 2개 앵커 분산이어야 한다.
- rolling 28일은 경향 보고용이며 readiness 통과를 직접 결정하지 않는다.

## 12.4 API 공통 응답 메타데이터

혼잡·날씨·관광정보 응답에는 가능하면 아래를 포함한다.

```json
{
  "data": {},
  "meta": {
    "source": "KTO",
    "sourceState": "FORECAST",
    "observedAt": null,
    "targetAt": "<계산된 ISO 8601 시각>",
    "lastUpdatedAt": "<실제 갱신 ISO 8601 시각>",
    "confidence": "MEDIUM",
    "normalizationVersion": "kto-poi-relative-v1",
    "forecastIssueId": "<발행 회차 ID>",
    "comparisonAxis": "TEMPORAL",
    "comparisonEligible": true,
    "comparisonReasonCode": "SAME_POI_SAME_ISSUE",
    "dataMode": null,
    "selectorMode": null,
    "claimMode": "NONE",
    "ctaPolicy": null,
    "comparisonGroupId": "KTO:POI:<poiId>:v1",
    "collectorRunId": null,
    "snapshotSetId": null,
    "sourceOrdinal": null,
    "observedAtSkewSeconds": null,
    "scope": "AREA",
    "scopeLabel": "경복궁 일대",
    "mappingType": "AREA_CONTAINS_POI",
    "fallbackUsed": false
  }
}
```

프론트엔드는 원시 API별 문구를 자체 추론하지 않고 meta를 이용해 동일한 상태 라벨을 보여준다. 목록 응답은 항목마다 독립 meta를 가지며 `comparisonGroupId`가 다른 항목을 혼잡 순으로 섞지 않는다. `comparisonEligible=false`이면 병렬 전후 상태 컴포넌트를 렌더하지 않는다.

## 12.5 외부 API Adapter

각 제공처를 별도 Adapter로 분리한다.

- KtoTourAdapter
- KtoCrowdForecastAdapter
- KtoRelatedPoiAdapter
- SeoulLiveAdapter
- KmaShortForecastAdapter
- KmaMidForecastAdapter
- MapRouteAdapter

Adapter는 요청·응답 DTO, 오류 매핑, rate limit, retry, cache key를 소스별로 관리한다. 외부 응답을 프론트에 그대로 노출하지 않는다.

---

# 13. 장애·보안·개인정보·운영

## 13.1 Fallback 표

| 실패 | Fallback | 사용자 표시 |
|---|---|---|
| 일정 분석 | 원문 유지 → 직접 입력·수정 | `일부 일정을 읽지 못했어요. 확인해 주세요.` |
| 장소 매핑 | 후보 선택 또는 사용자 직접 검색 | `장소 확인 필요` |
| 선택적 기기 내 위치 권한 거절·GPS 실패 | 장소·권역 직접 선택 | `현재 장소를 직접 선택해 주세요.` |
| 집중률 예측 | 유효 캐시, 없으면 수치 생성 중단 | `공식 혼잡 예측 범위 밖` 또는 `업데이트 지연` |
| 집중률 응답은 있으나 날짜 Level이 평평함 | 일반 사용자는 현재 일정 유지; DemoSession은 다른 `DEMO_READY` POI 선택 | 일반: `현재 기간에는 더 여유로운 날짜 차이가 없어요.` / 데모 후보 5개 미만: 운영 `SUBMISSION_BLOCKED` |
| 서울 Live | 같은 원천의 유효 짧은 TTL 캐시, 없으면 Live 상태 제거 | Planning은 별도 Forecast 그룹으로만 표시 |
| 현재 Live ABSOLUTE 비교쌍 없음 | 일반 사용자는 능동 CTA 없음; 사용자가 직접 RELATIVE 필터를 켤 수 있음 | 유효 상대쌍이 없으면 `비교 가능한 후보 없음` |
| REPLAY D-2 | 최근 7일 후보 세트 수동 검토·교체 | 운영 `REPLAY_EXPIRING` 경보; 사용자 화면 영향 없음 |
| REPLAY 만료·연속 갱신 실패 | `replayReady=false`, 수동 세트 교체와 전체 E2E 재검증 | 운영 `SUBMISSION_BLOCKED`; 일반 Live 탐색은 유지 |
| 과밀지역 우회 후보 | 동일 비교그룹·수집 배치·CandidateRelation Gate 후보 없음 | `현재 조건에서 비교 가능한 우회 후보가 없어요.`; 일반 장소 탐색은 유지 |
| API quota 80% 이상·한도 초과 | 비핵심 배치 중단, request coalescing, TTL 연장, 축소 프로필 | 핵심 심사 흐름은 예비량·유효 캐시로 유지하고 업데이트 지연 표시 |
| 날씨 | 유효 캐시 → 날씨 Context·P1 WeatherFit 제거 | `현재 날씨를 불러오지 못했어요` |
| 관광정보 | 유효한 단기 캐시 | 마지막 호출·업데이트 시각 |
| 연관 관광지 | 사전 검수 후보; 없으면 추천 생략 | `검수된 주변 경험 없음` |
| 지도 | 직선거리만 표시 또는 기능 비활성 | `정확한 이동시간 확인 불가` |
| 후보 유사성 계산 | EXACT만 우선 판정, SIMILAR 자동 교체 금지 | `비슷한 일정 여부를 확인하지 못했어요. 직접 비교해 주세요.` |
| DAY/TRIP 경로 계산 | 확정 일정 목록과 텍스트 변경 비교 유지, 경로 개선 주장·적용 비활성 | `경로를 확인하지 못해 전체 최적화안을 적용할 수 없어요.` |
| 최적화안 적용 | 변경 전 상태 유지 | 최신 발행 회차에서 개선이 뒤집히면 `데이터가 바뀌어 다시 계산이 필요해요`; 그 외 충돌 원인과 재계산 안내 |

## 13.2 안정성

- 제한된 retry와 exponential backoff
- 소스별 Circuit Breaker
- API별 승인량·60/80/90% 사용 경보·오류율·지연·운영계정 상태 모니터링
- 외부 API Timeout을 설정하고 전체 페이지 렌더링을 막지 않음
- 후보 저장과 Optimization Decision은 idempotency key로 중복 생성·적용 방지
- 데모 데이터 초기화와 복원 스크립트 마련
- 공개 데모·일정 분석·이벤트 수집 API에 요청 크기 제한과 사용자·세션·IP 기반 rate limit 적용

## 13.3 인증키·보안

- 모든 외부 API 키는 서버 Secret으로 관리
- 로그에는 키·비밀번호·민감 파라미터 마스킹
- CORS를 제출 도메인 중심으로 제한
- 쿠키 세션을 쓰면 CSRF 방어와 SameSite·Secure·HttpOnly를 적용
- Trip·TripCandidate·TripItem·OptimizationRun·Proposal·Decision마다 인증 사용자 또는 DemoSession 소유권을 검사해 IDOR/BOLA를 차단
- 업로드 파일 형식·크기·악성 콘텐츠 검증 - P1
- 사용자 입력 출력 시 XSS 방지
- 테스트 계정은 개인 계정과 분리
- 테스트 계정은 최소 권한·격리된 데모 데이터만 가지며 반복 사용 후 초기화 가능

## 13.4 위치정보 처리

Live 탭의 P0 기본 UX는 장소 직접 선택 또는 현재 여행의 다음 일정 기준이다. 원시 GPS 좌표를 서버 또는 지도 사업자에 전송하면 저장 여부와 무관하게 위치기반서비스 신고 검토 대상이 될 수 있으므로 P0에서는 해당 경로를 제공하지 않는다.

1. P0 서버 Live API는 공개 장소·권역 ID만 받는다.
2. 선택적 GPS는 권한 요청 직전 `기기 안에서 주변 관광지와 직선거리를 계산하는 용도`임을 고지한다.
3. 좌표는 브라우저 메모리 안에서만 사용하고 서버·외부 지도·URL·분석 SDK로 전송하지 않는다.
4. 권한 거절·철회·GPS 실패 시 동일한 장소 직접 선택 Fallback을 제공한다.
5. 서버 전송형 GPS를 P1에서 열려면 위치정보지원센터 등 공식 상담, 신고·약관·동의, 전송처, 보존, 삭제, CDN·프록시·APM 로그 흐름을 먼저 확정한다.
6. 개인정보처리방침과 실제 코드 경로가 일치해야 하며 Feature Flag가 꺼진 경로의 좌표 파라미터도 서버가 거부한다.

법률 판단을 자체 가정하지 않고 공식 상담 결과를 기록한다.

## 13.5 일정 원문·관심사 처리

- P0 기본 정책은 일정 원문을 분석 요청 처리 중에만 사용하고 서버 DB에 저장하지 않는 것이다.
- 웹서버·프록시·APM의 요청 본문 캡처와 디버그 로그에서도 일정 원문을 제외한다.
- 브라우저 임시 초안은 확인 완료·취소·세션 만료 시 삭제한다.
- 비동기 분석 때문에 임시 저장이 불가피하면 암호화 저장, 접근 통제, 24시간 이하 TTL과 별도 고지를 적용한다.
- P0는 외부 AI를 호출하지 않는다. P1에서 외부 AI를 도입할 때 이름, 연락처, 예약번호, 숙소 상세 주소 등 불필요한 문자열을 전송·로그 전에 제거한다.
- 확정 후에는 구조화된 Trip, TripItem, InterestSignal과 사용자 수정 이력만 저장한다.
- 사용자는 Trip별 관심사 근거를 조회·수정·삭제할 수 있다.
- Trip 신호를 계정의 장기 취향에 합칠 때는 별도 동의를 받고 철회 경로를 제공한다.

## 13.6 운영 관측성

- 외부 API별 호출 성공률·P95 지연·quota
- `DEMO_READY` 후보 수, curated 앵커·후보쌍·서로 다른 앵커 수, Snapshot 편차, REPLAY age·D-2·만료, `/api/demo/readiness` 상태
- API·키별 승인량 대비 사용률, 최근 7일 peak, 예상 p95, 심사 예비량, 운영계정 신청 상태
- Feed → TripCandidate 저장 전환과 후보 → 일정 배치 전환을 분리 측정
- 일정 분석 성공률, 확인 단계 수정률, 분석→Feed 전환 - 원문·개인식별정보 제외
- Live 진입, 위치 권한 결과, 직접 선택 비율, 주변→POI·TripCandidate 전환 - 원시 좌표 제외
- OptimizationRun 생성·Preview 노출·적용·유지·만료·되돌리기, scope·includeTripCandidates
- 최신 발행 회차 재검증·`DATA_CHANGED`, Trip version 충돌·`TRIP_CHANGED`, 경로 실패·`ROUTE_UNAVAILABLE` 비율
- 적용 전후 CrowdLevel·총 이동시간·거리와 바뀐 항목 수
- `RelativeProactiveCtaViolationCount` 목표 0
- 오류 상태와 Fallback 사용률
- 데모 계정 로그인·핵심 시나리오 성공 여부

---

# 14. Seed 콘텐츠·데모·성과 측정

## 14.1 Seed 콘텐츠

### 개발 목표

- 최종 운영 목표: 서울 POI 200~300개 공식 Seed 카드
- 공모전 안정 데모 최소치: 대표 POI 20개 이상, 핵심 경로 검증 POI 5개 이상
- 집중률 예측 후보은행: 실제 대상 ID와 horizon 응답을 확인한 POI 10개 이상
- 시간 분산 데모 Gate: 여행 기간 안에 실제 `LevelDelta >= 1`인 날짜쌍과 운영·잠금·이동 제약을 모두 통과한 `DEMO_READY` POI 5개 이상
- S10 P0 Gate: 위 POI 중 핵심 유명 관광지 1곳 이상에 운영·거리·민감 지역을 검수한 연관 후보 1개 연결
- 유명 관광지 3곳 이상에 각각 검수 연관 후보를 제공하는 범위는 P1 확장 목표

후보은행 10개·`DEMO_READY` 5개는 공고나 제공기관이 보장한 수치가 아니라 심사 시점 데이터 변동을 흡수하기 위한 **Nullnull 내부 Submission Gate**다. 기능설명서에서는 API 전체 커버리지나 공식 요구사항처럼 표현하지 않는다.

### 카드 생성 규칙

- 한국관광공사 상세·소개·이미지 기반
- `공식 관광정보 기반` 등 Seed 배지 표시
- UGC 작성자처럼 위장하지 않음
- 이미지 이용조건·출처 metadata 저장
- P0 한국어 POI가 검증된 카드 우선; 영문 LocalizedPOI는 P1
- 운영시간·행사기간 등 변동 정보는 기준시각 표시

### 콜드스타트

- 사용자가 확인한 TripInterest + 근거 장소 + 여행 지역·기간 + 빈 시간 + 다양성 믹싱으로 Feed 구성
- 일정에서 관심사가 추출되지 않으면 직접 선택 Chips를 제안하고, 선택 전에는 지역·기간 기반 Seed Feed를 제공
- UGC 표본이 적을 때 Social 점수와 InternalDemandBoost를 비활성화
- UGC가 늘면 Seed 노출 비중을 점진적으로 낮춤

## 14.2 심사 데모 데이터

데모 세션에는 아래가 준비돼 있어야 한다.

- `D0` 실행일 기준 `D+7~D+10`으로 계산되는 서울 일정 원문 1개와 기대 추출 결과
- 추출 관심사 4개, Must Visit 후보 1개, Flexible 후보 2개, 빈 시간 1개
- 서울 3박 4일 여행
- Must Visit 유명 관광지 1~2개
- 날짜 미정 후보 2개
- 혼잡 차이가 명확한 날짜 제안 1개
- 사전 검수된 연관 경험 카드 1개
- 적용 전 상태로 복원 가능한 ITEM OptimizationRun·Proposal·Rollback payload
- P1 시연용 DAY/TRIP OptimizationRun 1개와 route matrix·지도/타임라인 비교 Fixture
- 한국어 핵심 POI와 i18n-ready UI key
- CandidateRelation 검수 앵커 3개 이상·후보 관계 5개 이상이며 관계가 최소 2개 앵커에 분산된 목록
- 최근 7일 이내 대표 REPLAY SnapshotSet 1개와 Header→후보→상세→`담아두기` 전체 E2E 검증 이력
- Live·Replay·Forecast·정성 Context의 sourceState별 그룹 목록
- 위치 권한 거절 시 사용할 직접 선택 장소

### `DemoScenarioFactory` 시간 분산 계약

1. Scheduler가 세션 요청과 분리된 사전 점검 작업으로 후보은행 10개 이상을 API별 호출 예산 안에서 갱신한다.
2. 각 후보는 공식 horizon 안의 `D+7~D+10` 날짜, 유효 응답, 같은 POI·source·normalizationVersion·`forecastIssueId`, `LevelDelta >= 1`, 운영·예약·일정 충돌·접근성 Gate를 모두 만족할 때만 `DEMO_READY`가 된다.
3. `DemoScenarioFactory`는 세션 생성 시 최신 유효 캐시에서 `DEMO_READY` 후보를 선택하고 선택 POI 한 건을 실제 API 또는 request-coalesced 유효 응답으로 재검증한다.
4. 선택 우선순위는 `유효기간 여유 → LevelDelta → 제약 통과 확실성 → 최근 데모 미사용`이며 특정 POI·날짜·`5 → 2`를 하드코딩하지 않는다.
5. `DEMO_READY`가 5개 미만이면 운영 경보와 `SUBMISSION_BLOCKED`를 기록한다. 일반 사용자에게는 정직하게 `현재 일정 유지`를 보여주되, 대표 기능이 재현되지 않는 상태를 제출 가능으로 판정하지 않는다.
6. 세션에는 선택 근거, 실제 before/after Level, 응답시각, target dates, source, normalizationVersion을 저장해 시연과 테스트가 같은 Snapshot을 사용하게 한다.

### `DemoScenarioFactory` 공간 분산 계약

1. 현재 서울 Live에서 같은 비교그룹·수집 배치·10분 편차·CandidateRelation Gate를 통과한 `ORDINAL_LOWER` 비교쌍을 찾는다. 현재 Live가 유효하면 심사 화면에서 우선 사용할 수 있다.
2. `anchor.sourceOrdinal >= absoluteClaimMinOrdinal`이면 `claimMode=ABSOLUTE`, 그보다 낮고 2 이상이면 `claimMode=RELATIVE`로 기록한다. DemoSession은 둘 다 재현할 수 있지만 일반 사용자 능동 CTA는 ABSOLUTE에만 허용한다.
3. 공간 데모 readiness는 현재 Live 존재와 무관하게 Source Registry·CandidateRelation·전체 E2E·7일 신선도를 통과한 대표 REPLAY를 필수로 한다.
4. 대표 세트는 서로 다른 유효 앵커 수, 검수 후보쌍 수, 후보 `sourceOrdinal<=2` 쌍 수, 최근성 순으로 선택하며 최소 주 1회 갱신한다.
5. REPLAY가 D-2이면 경보·수동 교체를 시작하고, 만료·연속 갱신 실패이면 `spatialDemoReady=false`와 `SUBMISSION_BLOCKED`를 기록한다.
6. Forecast·정성 Context·합성 값을 섞어 성공 시나리오를 만들지 않는다.

데모 초기화는 사용자별 임시 세션 복제 방식이 가장 안전하다. 공용 한 계정의 동일 row를 여러 심사위원이 수정하게 하지 않는다.

## 14.3 KPI

| KPI | 정의 |
|---|---|
| Itinerary Parse Completion | 일정 분석 시작 대비 결과 확인 도달 비율 |
| Signal Correction Rate | 추출된 관심사·Must Visit·일정 중 사용자가 수정한 비율 |
| Itinerary-to-Feed Conversion | 일정 확인 완료 대비 개인화 Feed 도달 비율 |
| Candidate Save Rate | Feed·검색·Live 노출 대비 TripCandidate 저장 비율 |
| Candidate-to-Schedule Rate | 저장 후보 중 수동 배치·교체 또는 승인된 최적화로 TripItem에 반영된 비율 |
| Optimization Apply Rate | 유효 Preview 노출 중 사용자가 적용한 비율; ITEM/DAY/TRIP 분리 |
| Date Shift Rate | 날짜 변경안 적용 비율 |
| Time Shift Rate (P1) | 시간 변경안 적용 비율 |
| POI Shift Rate (P1) | opt-in 주변·대체 POI 변경안 적용 비율 |
| Route Shift Rate (P1) | 경유·순서 변경안 적용 비율 |
| Temporal CrowdLevel Improvement | 같은 POI·source·normalization version·`forecastIssueId`의 적용 전후 날짜 Level 차이 |
| Spatial Ordinal Improvement | 같은 `comparisonGroupId`·`snapshotSetId`·관측시각 편차 Gate의 서울 Live `sourceOrdinal` 차이 |
| Planned Peak Exposure Reduction (proxy) | 최적화 적용 전후 계획에서 Level 4~5에 배치된 일정 비율의 감소; 실제 방문 감소가 아님 |
| Recommendation Exposure Concentration | POI·권역·시간대별 Feed 노출의 HHI 또는 상위 10% 점유율 |
| Alternative Saturation Rate (P1) | 시간창 ExposureBudget·혼잡 악화 때문에 분산 후보가 보류된 비율 |
| Feed-to-Candidate Conversion | Feed 노출·클릭 대비 TripCandidate 저장 비율 |
| Live-to-POI Conversion | Live 장소 노출 대비 POI 상세 진입 비율 |
| Live-to-Candidate Conversion | Live 장소 노출 대비 TripCandidate 저장 비율 |
| Spatial Detour Completion (proxy) | 유효 `LIVE` ORDINAL_LOWER 후보 세트 조회 대비 후보 저장 비율; ABSOLUTE·RELATIVE는 분해 보고, 실제 방문 우회가 아님 |
| Replay Demo Completion (ops) | `REPLAY` 비교 모드에서 후보 상세·저장 E2E가 완료된 비율; 현재 혼잡 효과 KPI와 분리 |
| Detour No-Candidate Rate | 현재 기준 장소에서 동일 비교그룹·수집 배치·CandidateRelation Gate를 통과한 후보가 없었던 비율; REPLAY는 별도 운영 지표 |
| Comparison Evidence Coverage | 교체·우회 노출 중 유효 비교 근거가 있었던 비율 |
| Comparison-Ineligible Rate | 비교그룹·배치·소스 불일치로 병렬 비교를 제거한 비율 |
| Curated Pair Distinct Anchor Count | 검수 후보쌍이 분산된 서로 다른 앵커 수 |
| Replay Snapshot Age | 대표 REPLAY 세트의 관측 후 경과일 |
| Relative Proactive CTA Violation Count | RELATIVE인데 일반 사용자 능동 CTA가 노출된 건수, 목표 0 |
| Optimization Revalidation Expiry Rate | 최신 예측 회차에서 개선이 뒤집혀 `DATA_CHANGED` 만료된 비율 |
| Optimization Conflict Rate | 적용 시 `TRIP_CHANGED`·`LOCK_CONFLICT`로 재계산한 비율 |
| Route Preview Coverage (P1) | DAY/TRIP Preview 중 유효 route matrix·지도·거리·시간을 모두 제시한 비율 |
| Crowd Data Coverage Top10 | Feed 상위 10장 중 유효 혼잡 근거가 있는 카드 비율 |
| Crowd Unknown Exposure Share | 혼잡 미지원 후보의 Feed 노출 점유율 |
| Top10 Replacement Rate | 일정 등록 전후 상위 10장 교체 비율, 초기 체감 Gate 30% 이상 |
| Evidence Card Count Top10 | 상위 10장 중 일정 근거를 표시한 카드 수, 초기 Gate 4장 이상 |
| Direct Anchor Completion | 장소 직접 선택 또는 다음 일정 기준으로 Live 탐색을 완료한 비율 |
| Unique Area Discovery | 사용자별 새 지역 탐색 수 |
| Must-Visit Retention | Must Visit 목적지 유지 비율 (자동 제안에 의한 변경은 0이어야 함) |
| User-Initiated Swap Rate | My Trip 편집에서 사용자가 직접 교체를 선택한 비율 |
| Optimization Fatigue | 연속 유지·닫기·이탈 지표 |
| Verified Visit Shift (공모전 이후) | 별도 동의한 체크인·방문 확인 표본에서 변경안 적용 후 실제 날짜·시간·권역이 바뀐 비율 |

`dataMode=REPLAY`는 현재 혼잡 기반 Spatial Detour KPI의 분자·분모에서 제외하고 `Replay Demo Completion`에만 집계한다. `Top10ReplacementRate`는 관련성 저하로도 상승할 수 있으므로 `EvidenceCardCountTop10`과 상위 3장 중 최소 1장 변화 조건을 함께 통과해야 한다.

## 14.4 P1 실험

- engagement 중심 Feed vs crowd-aware diversified Feed
- 혼잡 경고 문구 vs 긍정적 여유 경험 문구
- 날짜 제안 1개 vs 상위 3개
- CrowdLevel 숫자만 vs 전후 변화·이유 포함

공모전 기능설명서에는 실제 사용자 표본이 없는 KPI를 성과처럼 쓰지 않는다. 측정 구조와 향후 검증 계획으로 표현한다.

---

# 15. 심사 시연 계획

## 15.1 3분 핵심 시연

| 시간 | 화면 | 시연 내용 | 심사 포인트 |
|---:|---|---|---|
| 0:00~0:15 | Home Feed | 여행 게시글이 메인인 SNS First 경험과 일반 Feed | 핵심 정의 |
| 0:15~0:40 | 일정 분석 | P0 지원 형식 롤링 일정 붙여넣기, 관심사·Must Visit·빈 시간 초안과 수정 | 개인화 근거·사용자 통제 |
| 0:40~1:05 | 개인화 Feed | 등록 전후 Feed 차이, 일정 기반 추천 이유, `+`로 여행별 후보 저장; 일정은 그대로임을 확인 | SNS·일정·관광데이터 연결 |
| 1:05~1:35 | My Trip 편집 | 담아둔 장소 패널, EXACT/SIMILAR, 추가·교체·유지, Must Visit·날짜·시간 독립 잠금 | 가벼운 발견과 사용자 통제 |
| 1:35~2:00 | ITEM 최적화 | 같은 POI·같은 발행 회차의 날짜별 Level, 변경 전후, 승인 후 원자적 일정 갱신·되돌리기 | 시간 분산·완결성 |
| 2:00~2:35 | Live·공간 우회 | dataMode·claimMode 확인 → ABSOLUTE CTA 또는 사용자 직접 RELATIVE 필터 → 동일 비교그룹·수집 배치의 검수 후보 → 담아두기 | 공간 분산·데이터 정직성 |
| 2:35~2:50 | P1 전체 최적화 | DAY/TRIP 지도·타임라인·거리·시간·혼잡 비교와 `담아둔 대안도 포함` OFF 상태 | 공간·시간 통합 최적화 |
| 2:50~3:00 | 출처 | 기준시각·범위·출처 | 투명성 |

3분 시연에서는 `예시 일정 채우기`로 입력 시간을 줄이되, 미리 저장한 분석 결과로 건너뛰지 않고 실제 parse → 확인 응답을 보여준다.

## 15.2 5분 확장 시연

P1 공모전 확장 E2E가 통과한 항목을 아래 순서로 추가한다.

- 전체 TRIP 지도·타임라인 전후 비교와 변경 항목 펼쳐보기
- `담아둔 대안도 포함` OFF/ON에 따른 결과 차이와 사용자 승인
- 영어 전환과 영문 POI
- 날씨 범위별 표시
- 선택적 기기 내 GPS 허용·거절과 직접 선택 경로 - 서버 좌표 전송 없이 구현된 경우만
- 서울 지원 장소 Live Re-plan
- 오류·Fallback 예시
- Optimization Decision·Rollback 이력과 KPI 이벤트

## 15.3 시연 안정성

- 핵심 API 응답을 사전 확인하되, 심사 중 실제 OpenAPI 호출 경로 유지
- 외부 장애 시 사용할 유효 캐시와 상태 문구 준비
- 데모 시작 전 자동 초기화
- 데모 세션 생성 시 실행일 기준 미래 날짜·예측 horizon·실제 Level 차이를 재검증
- `/api/demo/readiness`에서 시간 `DEMO_READY>=5`, 앵커 분산 CandidateRelation, `replayReady`, 쿼터 예비량을 확인하고 하나라도 실패하면 제출·시연을 중단
- REPLAY D-2 경보와 수동 세트 교체 상태를 일 2회 확인하고 만료·연속 갱신 실패이면 `SUBMISSION_BLOCKED`를 해제하지 않음
- Live는 dataMode·claimMode·ctaPolicy와 관측시각이 시연 중 유지되는지 확인
- 제출 후 1차 기능심사 종료까지 일 2회 readiness와 쿼터 예비량을 확인하고, 심사 빌드는 긴급 복구 외 변경을 동결
- 심사위원이 순서를 벗어나도 Home·My Trip에서 복귀 가능
- 모바일·데스크톱 모두 같은 핵심 기능 제공

## 15.4 10월 최종 PT 대비

공개 공고상 1차·최종심사는 10월 중이며 정확한 날짜·발표시간은 대상자별 별도 안내다. 1차 통과 통보를 받으면 `D_pt`와 발표 규칙을 근거대장에 등록하고 아래를 실행한다.

- 공식 발표 제한시간에 맞춘 본편과 30초 축약 시연을 각각 준비
- `서비스 적정성 30 / 완성도 30 / 실용성 25 / 발표 15` 순서의 Q&A 근거표 작성
- 라이브 URL 장애에 대비한 동일 빌드 화면녹화·정적 캡처·API 호출 증거 준비
- 공간 분산 `과밀지역 우회`, 시간 분산 ITEM 변경, 통합 DAY/TRIP 최적화를 각각 한 문장·한 화면으로 설명
- 데이터 한계, 교차 소스 비교 금지, 실제 방문효과 미검증에 대한 방어 답변 준비
- 발표자·데모 조작자 역할, 질의응답 담당, 장비·네트워크 체크리스트 확정

---

# 16. 기능설명서 PDF에 들어갈 내용

기능설명서는 공식 PPT 양식의 필드와 순서를 유지한다. 미구현 기능이나 계획 중인 API를 구현된 것처럼 넣지 않는다. 기능 흐름 슬라이드는 해시태그 수에 맞게 복제하고, 최종 사이트의 실제 캡처만 사용한다. 내부 문서의 Instagram·X 비교는 제품 문법을 설명하기 위한 참고일 뿐이며, 사이트·PDF·제출 시스템의 대외 문구는 `세로형 소셜 피드`로 통일하고 타사 로고·화면 복제·제휴 암시를 사용하지 않는다.

## 16.1 권장 최종 슬라이드 구성

| 슬라이드 | 공식 항목 | 들어갈 내용 | 넣지 않을 내용 |
|---:|---|---|---|
| 1 | 표지 | 등록 팀명, 공식 제출명 `Nullnull — 내 일정을 읽는 분산형 여행 SNS` | 기술 스택 장문 |
| 2 | 서비스 소개 | 유형, 타깃, 한 문장 개요, 지정과제 2번, 선택 이유 | 전체 문제 보고서 |
| 3 | 과제 기반 기획 방향 | SNS 집중의 원인 → 후보 저장 → 수동 편집/AI 최적화 → 승인 → 행동 분산 | 세부 알고리즘 수식 |
| 4 | 해시태그·핵심 기능 | 해시태그, 연결 기능, 상세 내용 표 | 미구현 해시태그 |
| 5 | 흐름 1 | #SNS여행발견 | 소스코드 |
| 6 | 흐름 2 | #혼잡도분석 | DB 스키마 |
| 7 | 흐름 3 | #AI일정최적화 · ITEM에서 DAY/TRIP까지 | API 키 |
| 8 | 흐름 4 | #과밀지역우회 | 지원 범위를 숨긴 전국 실시간 주장·교차 소스 순위 |
| 9 | 서비스 이미지 | 대표 1장 + 상세 3~5장 | 와이어프레임·가상 화면 |
| 10 | 관광공사 데이터 | 실제 사용 OpenAPI와 기능 매핑 | 신청만 하고 미사용한 API |
| 11 | 기타 데이터 | 서울시·기상청·지도 중 실제 사용분 | 미사용 시 슬라이드 삭제 |
| 12 | 차별성·발전계획 | Must Visit 보호, 정직한 데이터 상태, 단계별 확장 | 검증되지 않은 성과 수치 |

공식 양식에서 한 항목이 한 장으로 고정돼 있지 않고 복제 안내가 있는 경우에만 기능 흐름 장을 복제한다. 필수 필드의 이름과 구조는 임의로 삭제·변경하지 않는다.

## 16.2 슬라이드별 초안 문구

### 슬라이드 1 - 표지

- 팀명: 참가 신청 시 등록된 변경 불가 팀명 확인
- 서비스명: `Nullnull — 내 일정을 읽는 분산형 여행 SNS`
- 부제: `내 일정을 읽어 취향과 실시간 여행 선택으로 연결하는 SNS 여행플래너`
- 네이밍 의도 초안: `Nullnull은 정해진 여행의 정답을 비워 두고, 나의 일정과 취향으로 채운다는 의미입니다.` — 팀이 실제 의도와 일치하는지 승인한 뒤 표지 또는 서비스 소개에 1회 사용

### 슬라이드 2 - 서비스 소개

- 서비스 유형: `웹(PWA)`
- 서비스 타깃: `방한 외국인 대상 설계, 현 단계 한국어 검증 버전`
- 동시 지원: `국내 자유여행객`
- 구현 단계: `한국어 UI 완성 → 영어 UI 추가 → 영문 관광정보 상세(P1)`
- 한 문장 개요: `여행 게시글을 탐색하는 SNS형 Home을 중심으로, 등록 일정에서 읽은 관심사와 빈 시간으로 Feed를 개인화하고 여행 중에는 실시간 혼잡·날씨·거리·일정 Context를 함께 확인해 더 나은 선택으로 연결하는 여행플래너`
- 선택 지정과제: `2번 - 유명 관광지 중심의 관광객 쏠림 현상으로 인한 특정 명소의 오버투어리즘(과밀화) 문제`
- 선택 이유: `SNS의 반복 노출이 같은 장소·시간으로 관광 선택을 동기화한다는 문제에서 출발해, 사용자의 목적지를 유지하면서 작은 행동 변화를 만드는 방식으로 피크 집중을 완화하고자 했다.`
- 데이터 한계: `서울 Live는 외국인 관광객 수가 아니라 권역 전체 인구 추정 기반의 혼잡 Context입니다.`

### 슬라이드 3 - 기획 방향

```text
여행 게시글 중심의 일반 Home Feed 탐색
        ↓
사용자가 이미 만든 여행 일정 입력
        ↓
관심사·Must Visit·빈 시간 추출과 사용자 확인
        ↓
개인화 SNS Feed에서 여행 콘텐츠 발견
        ↓
인기 장소·시간으로 선택이 집중될 가능성
        ↓
관광공사 관광정보·혼잡 예측과 서울 Live로 현재 상황 비교
        ↓
단일 서수 Selector와 CandidateRelation으로 ABSOLUTE·사용자 직접 RELATIVE·과거 관측 재생을 구분
        ↓
발견한 장소를 여행별 후보로 저장하고 기존 일정과 동일·유사 관계 안내
        ↓
직접 편집 또는 Must Visit·날짜·시간 잠금을 지키는 AI 최적화안 생성
        ↓
지도·타임라인·지표 전후 비교 후 사용자 승인
        ↓
피크 노출 감소와 주변 지역 발견
```

핵심 설명: `Feed의 +는 장소를 가볍게 담아두고, My Trip에서 Must Visit은 지키면서 직접 편집하거나 검증된 AI 최적화안을 승인해 날짜·시간·동선을 바꾼다.`

### 슬라이드 4 - 해시태그와 핵심 기능

| 해시태그 | 연결 기능 | 상세 내용 |
|---|---|---|
| #SNS여행발견 | 일정 분석 + For You Feed + TripCandidate | 기존 일정에서 관심사를 추출해 관광정보 기반 콘텐츠를 개인화하고 여행별 담아둔 장소로 저장 |
| #혼잡도분석 | S11 혼잡 예보 + S07 인라인 | 같은 POI·같은 예측 발행 회차의 날짜별 CrowdLevel과 범위 밖 정성 Context 구분 |
| #과밀지역우회 | Live 공간 분산 | ABSOLUTE 능동 CTA, 사용자 직접 RELATIVE 필터, REPLAY를 구분하고 동일 비교그룹·수집 배치의 CandidateRelation 후보를 POI 상세·담아두기로 연결 |
| #AI일정최적화 | 승인형 일정 최적화 | P0 ITEM 날짜 변경, P1 DAY/TRIP 시간·순서·경로와 사용자 opt-in 후보 교체를 지도·타임라인으로 비교 후 적용 |

공고 예시의 `#숨은명소` 접점은 S10이다. 핵심 유명 관광지와 연관성이 있고 거리·운영·민감 지역 검수를 통과한 후보 1개를 주변 체류 선택지로 연결하되, S11의 실시간 공간 우회와는 구분한다.

`#실시간리플랜`은 P1 E2E까지 통과한 경우에만 구현 기능으로 추가한다. 주변·숨은 명소는 Live와 TripCandidate·AI 최적화의 후보 전략 안에서 설명한다.

### 슬라이드 5 - #SNS여행발견 흐름

1. 사용자가 여행 게시글 중심의 일반 Home Feed를 탐색한다.
2. P0 지원 형식으로 작성한 서울 일정을 붙여 넣는다.
3. 장소·날짜·관심사·Must Visit 후보·빈 시간을 추출받고 수정·확정한다.
4. 관광공사 관광정보 기반 Feed가 일정에 맞게 달라진 것을 확인한다.
5. 게시글의 추천 이유를 확인하고 `+`로 대상 여행의 `담아둔 장소`에 저장한다. 이때 확정 일정은 바뀌지 않는다.

캡처: 일정 추출 결과와 일정 기반 Feed 카드.

### 슬라이드 6 - #혼잡도분석 흐름

1. Live 또는 Feed에서 장소를 선택해 상세를 연다.
2. **혼잡 예보 3구간**(오늘 시간대별 / 이번 주 / 다음 달)을 확인한다.
3. 구간별로 `실시간 관측` · `공식 혼잡 예측` · `공식 혼잡 예측 범위 밖`이 분리 표시되고 범위 밖에는 숫자 막대가 없다.
4. My Trip 카드의 인라인 액션에서 `이 항목 최적화`를 선택한다.
5. ITEM Optimization Preview에서 더 여유로운 날짜를 확인한다.

캡처: S11 상세 혼잡 예보 3구간과 출처, My Trip 카드 인라인 제안.

### 슬라이드 7 - #AI일정최적화 흐름

1. My Trip에서 `AI로 일정 최적화`와 ITEM/DAY/TRIP 범위를 선택한다.
2. Must Visit·날짜·시간·예약 잠금과 `담아둔 대안도 포함` OFF를 확인한다.
3. 변경 전·후 지도, 타임라인, 총 이동시간·거리·혼잡 지표와 항목별 이유를 비교한다.
4. 사용자가 `변경 n개 적용` 또는 `현재 일정 유지`를 선택한다.
5. 승인 후에만 My Trip이 원자적으로 갱신되고 `되돌리기`가 제공된다.

캡처: ITEM Preview, P1 DAY/TRIP 지도·타임라인 Preview, 적용 후 My Trip.

### 슬라이드 8 - #과밀지역우회 흐름

1. Live 탭에서 기준 장소를 직접 선택하거나 현재 여행의 다음 일정을 사용한다.
2. `현재 여행지`에서 CrowdLevel·현재 날씨·다음 일정까지 남은 시간·기준시각을 확인한다.
3. `claimMode=ABSOLUTE`이면 `비슷한데 덜 붐비는 곳` 능동 CTA를 확인한다. RELATIVE는 능동 CTA가 없고 사용자가 `다른 관광지` 탭의 `지금 더 여유로운 곳` 필터를 직접 켠다.
4. 같은 서울 Live `comparisonGroupId`·`snapshotSetId`와 CandidateRelation Gate 안에서 더 낮은 `sourceOrdinal` 후보를 확인한다.
5. Forecast·정성 Context 장소는 별도 그룹으로 보고 우회 순위에 섞지 않는다. REPLAY는 `과거 관측·실시간 아님`과 원 관측시각을 고정 표시한다.
6. 후보의 거리·범위·출처를 확인해 장소 상세로 이동하거나 여행의 `담아둔 장소`에 저장한다.

캡처: 비교 모드·관측시각, 기준 장소, 더 여유로운 동일 비교그룹 후보, sourceState별 별도 그룹. 재생 화면이면 `실시간 아님`이 잘리지 않아야 한다.

### 슬라이드 9 - 서비스 이미지

- 대표 이미지 1장: 여행 게시글 중심 Home과 일정 등록 후 개인화 상태가 함께 보이는 화면
- 상세 이미지 1: Feed의 관광정보 카드·추천 이유·여행 후보 저장
- 상세 이미지 2: Live의 현재 여행지와 다른 관광지 상태 비교
- 상세 이미지 3: My Trip 편집·후보 유사성·Must Visit/날짜/시간 잠금
- 상세 이미지 4: S11 날짜별 혼잡 예보 + ITEM 최적화 진입
- 상세 이미지 5: DAY/TRIP 지도·타임라인 전후 비교와 승인 후 변경 일정

모든 이미지는 최종 배포 URL의 실제 화면을 캡처한다.

### 슬라이드 10 - 한국관광공사 데이터 활용

최종 구현된 항목만 남긴다.

| 실제 사용 API | 활용 내용 |
|---|---|
| KorService2 | 국문 POI, 이미지, 상세, 위치·검색·행사 |
| 관광지 집중률 방문자 추이 예측 정보 | 같은 발행 회차의 S11 날짜별 혼잡 예보와 ITEM 최적화 근거 |
| 관광지별 연관 관광지 정보 | 주변·경유 POI 후보 |
| 빅데이터 지역별 방문자수_GW | 실제 구현한 경우 P1 지역 분석 보조; 혼잡 수치 생성에는 사용하지 않음 |

### 슬라이드 11 - 기타 데이터

실제 구현된 항목만 기재한다.

- 서울 실시간 도시데이터: 서울 지원 장소의 Live Crowd Context
- 기상청 단기·중기예보: 여행일 접근 단계별 날씨 위험과 실내·외 제안
- 지도 API: P1 경로 기능까지 실제 구현한 경우에만 연관 후보의 이동거리·시간 검증

기타 API를 최종 서비스에서 사용하지 않았다면 공식 안내에 맞춰 해당 슬라이드를 삭제한다.

### 슬라이드 12 - 차별성·발전계획

차별성:

1. 목적지 대체보다 사용자 욕구를 보존하는 최소 변경을 우선한다.
2. 이미 만든 일정에서 취향을 읽어 SNS 발견을 실제 Trip 행동으로 연결한다.
3. 현재 여행지와 다른 관광지를 한 Live 화면에서 확인하되 Live·Replay·Forecast·정성 Context를 구분하고 비교 가능한 그룹만 정렬한다.
4. ABSOLUTE 능동 제안·사용자 직접 RELATIVE 비교·과거 관측 재생을 구분하면서 동일 비교그룹·수집 배치의 검수 후보를 POI 상세·여행별 후보 저장까지 연결한다.
5. 추출 근거와 추천 이유를 사용자가 확인·수정할 수 있다.
6. 최적화 Preview·적용·유지·되돌리기와 변경 후 CrowdLevel·이동 지표를 저장해 효과 측정이 가능하다.

차별성 방어 문장:

> Nullnull은 AI가 일정을 몰래 바꾸는 서비스가 아니라, 사용자가 담은 장소와 고정 조건을 바탕으로 검증 가능한 변경안을 보여주고 승인받는 여행플래너입니다.

> P0는 커뮤니티 규모를 증명하는 단계가 아니라, SNS형 발견이 일정 선택으로 이어지는 지점에 분산 제어를 삽입하는 제품 루프를 검증합니다.

| 유형 | 잘하는 것 | 한계 | Nullnull의 차이 |
|---|---|---|---|
| SNS형 여행 피드 | 장소 발견·관심 형성 | 일정 제약과 혼잡 행동 전환이 분리됨 | 발견한 장소를 여행별 후보로 저장해 편집·최적화로 연결 |
| 일정 플래너·지도 | 일정 정리·경로 탐색 | 반복 노출과 혼잡 분산 개입이 약함 | 기존 일정을 읽고 잠금을 지키는 전후 비교·승인형 최적화 제공 |
| 혼잡 대시보드 | 현재 상태 관측 | 실제 일정 변경까지 이어지지 않음 | 혼잡 Context→후보→Preview→승인→되돌리기를 연결 |

발전계획:

- 1단계(P0 MVP): 서울 핵심 경로, TripCandidate, 수동 편집, ITEM 최적화와 한국어 서비스 안정화
- 2단계(P1 공모전 확장): route matrix 기반 DAY/TRIP 최적화·Live Re-plan·영어/EngService2·검색·알림·UGC 중 실제 E2E를 통과한 기능을 최종 배포
- 3단계(P2): 전국·일본어·중국어 확장, RTO·DMO 분산 캠페인 연계

## 16.3 기능설명서에 넣지 않을 내용

- API 인코딩·디코딩 인증키
- 테스트 계정 비밀번호
- 세부 DB 모델·캐시 TTL·Adapter 코드
- 미구현 SNS 기능
- 사용하지 않은 OpenAPI
- 가상의 KPI 성과 수치
- 불안정한 전국 실시간 혼잡 주장
- Instagram·X 등 타사 실명·로고·화면 모사·제휴를 암시하는 표현; 제출물에서는 `세로형 소셜 피드` 사용
- 팀이 승인하지 않은 `Nullnull` 네이밍 의도 문구

---

# 17. 콘텐츠랩 제출 시스템에 입력할 내용

## 17.1 서비스 기본정보

- 참가 신청 시 등록된 팀명 - 변경 불가 여부 확인
- 서비스명: `Nullnull — 내 일정을 읽는 분산형 여행 SNS`
- 서비스 유형: `웹`
- 출품 부문: `②-2 웹·앱 구현 부문`
- 선택 지정과제: `2번 - 유명 관광지 중심의 관광객 쏠림 현상으로 인한 특정 명소의 오버투어리즘(과밀화) 문제`
- 서비스 한 문장 개요
- 실제 접속 가능한 HTTPS URL

## 17.2 테스트 정보

공개 공고문에는 1차 심사용 테스트 계정의 ID·비밀번호 형식이 명시돼 있지 않다. 따라서 아래 중 실제 배포 방식과 **별도 안내의 최신 제출 필드**에 맞는 하나를 선택한다.

- 로그인 불필요
- SNS 연동 로그인
- 테스트 전용 계정

권장안은 **로그인 없는 독립 DemoSession**이다. 별도 계정이 요구되면 개인 계정을 쓰지 않고 팀이 실제 수신한 안내의 형식을 따른다.

- ID 또는 이메일: `<최신 제출 안내에서 요구한 값>`
- Password: `<Secret Manager에 저장한 테스트 전용 값>`
- 근거: `EVID-SUBMIT-ACCOUNT`에 안내 원문·수신일·URL 또는 캡처·확인자를 기록

과거의 추정 자격증명 예시는 공식 형식으로 단정하거나 문서·Git·PDF에 남기지 않는다. 테스트 자격증명은 제출 시스템의 비공개 필드에만 입력하고 핵심 기능이 로그인 실패로 막히지 않게 한다.

## 17.3 OpenAPI 검증정보

- 활용한 한국관광공사 OpenAPI 신청자별 인코딩 인증키
- 디코딩 인증키
- 최종 활용 API 목록
- 대표 API 운영계정 신청 여부
- API·키별 현재 승인량, 운영계정/증설 신청번호·상태, 예상 일 p95 호출량
- 필요 시 팀원별 신청자 정보

이 값은 제출 시스템에만 넣고 사이트, 기능설명서, Git에 복사하지 않는다.

## 17.4 최종 업로드·입력 항목

- 개발 URL
- 공식 양식으로 작성한 기능설명서 PDF
- 서비스명·개요·팀 정보
- 테스트 방식과, 별도 안내에서 요구하는 경우에만 테스트 계정
- 활용 OpenAPI와 인증키
- 운영계정 관련 입력

## 17.5 제출 전 공식 확인사항

- 1차 심사자료 제출 마감 `D_submit`: **공개 공고에는 미기재·대상자별 별도 안내 대기**
- 공개 공고에서 확인된 일정: 서비스 개발 `7월 말~9월`, 1차 심사 `10월 중`, 최종 PT `10월 중`, 시상식 `11월 중`
- `D_submit`은 운영 사무국 메일·콘텐츠랩 공지·접수 시스템 원문 중 하나를 팀이 실제 수신하고 근거대장에 등록한 뒤에만 확정
- 제출 경로: 한국관광콘텐츠랩 공모전 제출 시스템
- 최종 제출 직전 공식 안내와 양식 버전을 다시 확인
- 공식 PPT 양식의 필수 항목을 삭제·임의 변경하지 않음
- 기능설명서 PDF 변환 후 글꼴·이미지·페이지 잘림 확인

### 제출 근거대장

| 근거 ID | 확인 항목 | 현재 상태 | 인정 근거 | 필수 기록 |
|---|---|---|---|---|
| EVID-PUBLIC-NOTICE | 부문·지정과제·심사기준·공개 일정·참가 제한 | 확인 완료 | 한국관광공사 공식 투어라즈 공고와 첨부 공고문 | URL, 문서명, 공고일, 확인일, 확인자 |
| EVID-SUBMIT-DEADLINE | 1차 심사자료 마감 `D_submit` | 미확인 | 대상자별 운영 사무국 메일·콘텐츠랩 공지·접수 시스템 | 원문 파일/캡처, 수신일, 마감 timezone, 확인자 2인 |
| EVID-SUBMIT-ACCOUNT | 테스트 방식·계정 필드·형식 | 미확인 | 1차 제출 안내 또는 실제 제출 화면 | 필드 캡처, 요구 형식, 비공개 보관 위치, 확인자 |
| EVID-PT | 최종 PT 일시·제한시간·제출물 | 1차 통과 후 확인 | 대상자별 최종심사 안내 | 원문, `D_pt`, 발표시간, 장비·파일 규칙 |
| EVID-KTO-QUOTA | API별 승인량·운영계정/증설 신청 | 공식 기본 한도 확인·개별 키 승인량과 신청 상태 미확인 | 공공데이터포털 공식 상세 페이지와 My Page | API ID, key alias, 개발/운영 단계, 승인량, 신청번호·일시·상태, 예상 p95, 캡처, 확인자 |
| EVID-LIVE-REPLAY | 과거 관측 보관·재사용 가능 범위 | 확인 전 REPLAY 비활성 | 서울 데이터 이용조건·제공기관 서면 답변 | 허용 필드, 보관기간, 표시 조건, 원 관측시각, 근거 URL/답변, 확인자 |

근거가 서로 충돌하면 가장 최신의 한국관광공사·운영 사무국 원문을 우선하고, 전화·구두 안내는 담당자·통화일시·후속 확인 메일까지 남긴다.

## 17.6 참가 적격성·중복 출품 Gate

아래는 기능 QA와 별개인 **심사 자격 Gate**다. 하나라도 확인되지 않으면 제출 완료로 표시하지 않는다.

- 동일 결과물이 `① 웹·앱 개발 부문`에 동시 출품되지 않았는지 서비스명·URL·저장소·핵심 산출물 기준으로 팀원 전원 확인
- 종료된 `②-1 생성형 AI 활용 관광 프롬프톤 부문` 제출 서비스와 동일 결과물이 아닌지 확인; 아이디어 연계가 불가피하거나 경계가 모호하면 운영 사무국에 서면 질의
- 동일 서비스로 과거 공사 관광데이터 활용 공모전 수상 또는 공사 주관 서비스 개발·구축 지원사업 수혜 이력이 없는지 대표·팀원 서약과 공개 이력 검색으로 확인
- 1차 심사자료 제출 전에 최종 팀원 전원이 한국관광콘텐츠랩 회원가입을 완료하고 접수 시스템에서 조회·입력 가능한지 확인
- 최종 팀원 명단·역할·연락처와 참가 신청 정보가 일치하는지 2인 교차검수
- 확인 결과, 검색일, 검색어·대상 사업명, 서약자, 운영 사무국 질의·답변을 `Eligibility Register`에 보관

공고는 **동일 서비스·동일 결과물의 중복 제출**을 금지한다. 팀원이 다른 부문에서 전혀 다른 서비스를 제출한 경우까지 자동으로 금지된다고 확대 해석하지 않고, 경계가 불분명하면 운영 사무국의 서면 답변을 따른다.

---

# 18. 개발 일정

공개 공고에는 1차 심사자료의 정확한 마감 `D_submit`이 없다. 아래 일정은 제출 운영 우선순위를 위한 참고선이며 2인 팀의 개발 중단 기준이 아니다. 제출 뒤에도 readiness·안정성·제품 개선을 계속한다. 대상자별 안내를 받으면 1영업일 안에 표를 재기준화한다.

| 기간 | 목표 | 산출물 |
|---|---|---|
| 8/24~8/26 | 최우선 데이터·쿼터 Gate | KTO 운영계정/증설 신청, 승인량·호출예산, 예측 후보 POI 10개, 한국어 파서 Fixture 8종, 서울 Live 비교쌍·REPLAY 이용조건, P0 GPS 서버 전송 제거 |
| 8/27~9/03 | P0 기반·롤링 데모 | PWA 골격, 여행 게시글 Home, 직접 입력·제한 파싱·확인, Trip, 개인화 Feed, POI, 여행별 TripCandidate, `DEMO_READY` 후보은행·readiness API, 직접 선택 Live 목록 |
| 9/04~9/10 | P0 핵심 차별 기능 | My Trip 수동 편집·독립 잠금·후보 EXACT/SIMILAR, ITEM Optimization Preview·승인·되돌리기, 시간 분산 `DEMO_READY` 5개, 단일 서수 Selector·REPLAY 필수 계약 |
| 9/11~9/15 | P0 Gate·통합 안정화 | 쿼터 부하·Fallback, 데모 모드 라벨, 분석 로그, 모바일·데스크톱 QA, P1 Feature Flag 격리 검수 |
| 9/16~`D_internal` | 제출물 완성 | 최종 배포, 실제 화면 캡처, 기능설명서 PDF, 제출정보 정리 |
| `D_submit-2영업일`~`D_submit-1영업일` | 최종 리허설 | 익명창·다기기·외부망 테스트, 3분 시연, 오탈자·출처·Eligibility 검수 |
| `D_submit-1영업일` | 내부 제출 권장 | 업로드·입력·제출 완료와 접수증·화면 캡처 보관 |
| P0 Gate 통과~`D_pt-7영업일` | P1 공모전 확장 | route provider 확정, RouteMatrixSnapshot, DAY/TRIP 최적화, 변경 전후 지도·타임라인·지표, opt-in 후보 교체, Live Re-plan, P1 E2E |
| `D_submit`~1차 기능심사 종료 | 심사 운영 유지 + P1 격리 개발 | P0 readiness 일 2회 점검·배포 동결 원칙 유지, P1은 Feature Flag·별도 환경에서 개발하고 P0 회귀 Gate 통과 후만 병합 |
| 10월 중 `D_pt` | 1차 통과 시 최종 PT | P0+실제 통과한 P1 기능이 포함된 라이브·백업 시연, 공식 제한시간 반영 발표자료, Q&A 리허설 |

## 18.1 개발 티켓 묶음

### 데이터·백엔드

- DATA-00 한국어 지원 Fixture 8종·직접 입력 Fallback·추출 스키마·정답셋·원문 비영속 규칙
- DATA-01 집중률 예측 명세·horizon·후보은행 10개·`DEMO_READY` 5개 검증
- DATA-02 KTO POI·서울 Live 권역 Canonical 매핑; 한영 매핑은 P1
- DATA-03 Planning 5단계·서울 Live 원천 4단계 sourceOrdinal·범위·최신성 검증
- DATA-03A 같은 POI·같은 forecastIssueId 날짜 비교, 같은 비교그룹·수집 배치 공간 비교, 교차 소스 정렬 금지 계약
- DATA-03B 단일 `ORDINAL_LOWER` Selector, dataMode·claimMode·ctaPolicy, 0.5km 최소 이격·중첩 3단계·CandidateRelation 계약
- DATA-04 연관 관광지+선택 POI 기준 직선거리·P0 후보 검수
- BE-01 롤링 DemoScenarioFactory·앵커 분산 후보은행·7일 이내 필수 REPLAY readiness·D-2 경보·세션 Snapshot 격리·초기화
- BE-02 일정 text/file parse·column remap·confirm·관심사 수정 API와 Trip·TripItem 저장
- BE-02A TripCandidate 저장·중복 방지·CandidateTripMatch(EXACT/SIMILAR/NONE)·후보 배치/교체 API
- BE-03 KTO Adapter·캐시·호출 로그·API별 quota budget/Circuit Breaker
- BE-04 장소별 3구간 혼잡 예보 API
- BE-05 ITEM OptimizationRun·Proposal 생성, 최신 forecastIssueId 적용 재검증, Decision·Rollback 트랜잭션
- BE-06 Fallback·Circuit Breaker·모니터링
- BE-07 anchorId 기반 Live API, RELATIVE 사용자 직접 필터, REPLAY 서버 고정 권한, comparison meta와 GPS 파라미터 거부 정책
- BE-08 P1 RouteMatrixSnapshot·시간창 optimizer·DAY/TRIP change set·Live Re-plan·후보 opt-in 계약
- OPS-00 API 운영계정/증설 신청·후보은행 조밀 수집/전수 일 1~2회 이원화·심사 예비량·60/80/90% 경보

### 프론트엔드

- FE-01 전역 레이아웃·반응형·한국어 리소스·i18n key 구조
- FE-02 Home Feed Shell·일정 붙여넣기·TXT/CSV/XLSX 업로드·열 매핑·추출 결과 확인
- FE-03 Feed·POI·여행 선택형 `+ → 담아둔 장소`·저장 완료 상태
- FE-04 My Trip 보기/수동 편집/AI 최적화 모드, 후보 패널, 독립 잠금, dirty-state 이탈 방지
- FE-05 S11 상세 혼잡 예보 3구간 그래프·S07 항목별 최적화 진입
- FE-06 ITEM Optimization Preview·적용/유지/되돌리기·stale 상태
- FE-07 Live 기준 장소·다른 관광지 sourceState 그룹·ABSOLUTE 능동 CTA·RELATIVE 사용자 직접 필터·REPLAY 고정 라벨·비교 불가 DOM 제거
- FE-08 검수된 연관 후보 1개
- FE-09 데이터 라벨·출처·오류 상태
- FE-10 접근성·브라우저 QA
- FE-11 P1 DAY/TRIP 지도·타임라인·지표 비교, `담아둔 대안도 포함` opt-in, Live Re-plan

### 디자인·콘텐츠

- UX-01 UI 상태·CrowdLevel·Live 범위·sourceState 그룹 디자인 토큰
- UX-02 Figma 핵심 상태 High-fidelity: S06 후보 저장, S07 보기/편집/AI 진입, S09 ITEM/DAY/TRIP Preview
- UX-03 일정 추출 근거·독립 잠금·후보 유사성·적용 확인/되돌리기 UX와 선택적 기기 내 위치 사전 안내
- CONTENT-01 Seed 카드와 출처 검수
- CONTENT-02 한국어 UI 문구 작성과 번역 키 누락 검사 → 안정화 후 영어 문구 추가; 영문 관광정보 상세는 P1
- CONTENT-03 데이터 설명·한계 문구

### 제출

- SUB-00A `D_submit`·테스트 방식·PT 안내 제출 근거대장 갱신
- SUB-00B 중복 출품·동일 서비스 수상/지원 수혜 이력 Eligibility Register와 팀원 서약
- SUB-00C 최종 팀원 전원 한국관광콘텐츠랩 가입·접수 시스템 조회 확인
- SUB-01 URL·로그인 없는 DemoSession 또는 공식 안내 형식의 데모 계정 검증
- SUB-02 최종 화면 캡처
- SUB-03 공식 기능설명서 작성·PDF QA
- SUB-04 OpenAPI 키·활용 목록 취합
- SUB-04A API별 운영계정/승인량·예상 p95·심사 예비량·최근 7일 peak 증거 취합
- SUB-04B 시간 `DEMO_READY >= 5`·앵커 분산 CandidateRelation·필수 REPLAY·readiness 캡처
- SUB-05 최종 제출 리허설·제출
- SUB-05A 제출 후 1차 기능심사 종료까지 readiness 일 2회 점검·on-call·변경 동결
- SUB-06 제출물 `세로형 소셜 피드` 용어·타사 실명/로고·Nullnull 네이밍 문구 승인 검수
- SUB-07 1차 통과 시 공식 PT 기준 30·30·25·15 발표자료·백업 시연·Q&A 준비

---

# 19. 테스트·완료 기준

## 19.1 핵심 E2E 테스트

1. 지원 형식 한국어 일정 입력 → 분석 결과 로드; 계약 밖 문장 → 직접 입력 보정
1-B. 일정 없이 지역·날짜·관심사만 선택 → Trip 생성 → 개인화 Feed 도달
2. 관심사·Must Visit·Flexible·빈 시간 수정 → 확정
3. 개인화 Feed에서 일정 기반 추천 이유 확인
4. Feed·POI 상세 `+` → 대상 여행 선택 → TripCandidate 생성 → My Trip `담아둔 장소` 확인; TripItem과 Trip version은 변하지 않음
4-B. 같은 POI를 다시 담으면 중복 후보가 생기지 않고, 기존 일정의 EXACT/SIMILAR/NONE 관계와 근거가 표시됨
4-C. My Trip `일정 편집` → 후보를 빈 시간에 추가하거나 유사 항목과 교체/유지 → Must Visit·날짜 고정·시간 고정 독립 수정 → 저장
5. Demo readiness에서 시간 `DEMO_READY>=5`, 앵커 분산 CandidateRelation, `replayReady=true` 확인
6. Live 장소 직접 선택 또는 다음 일정 기준 → ABSOLUTE CTA 확인; 별도 경로에서 `다른 관광지` 탭의 RELATIVE 필터를 사용자가 직접 켜 목록 확인
7. 선택적 기기 내 GPS 허용·거절 시 좌표 서버 전송 없이 동일 Fallback 확인
8. Live·Replay·Forecast·정성 Context 상태와 기준시각 구분
9. S11 상세 혼잡 예보에서 날짜 비교
10. ITEM Optimization Preview 조회 → 적용 전 일정 불변 → 변경 전후 확인 → 승인 → 일정 날짜 변경
11. P1 DAY/TRIP 최적화 → 지도·타임라인·거리·이동시간·혼잡 전후 확인 → opt-in을 끄면 TripCandidate 미사용 → 승인 후 원자적 반영
12. Preview 중 다른 탭에서 일정 변경 → `TRIP_CHANGED` 적용 차단 → 재계산
13. 새로고침 → 변경 상태 유지 → 되돌리기 → 이전 change set 복원
14. 데모 초기화 → 기준 상태와 같은 Snapshot 복원

## 19.2 데이터·API 테스트

- 한국관광공사 실제 API 호출 성공 로그
- 한국어 지원 Fixture 8종과 TXT·CSV·XLSX import Fixture의 회귀 테스트, 열 자동·수동 매핑, 부분 성공·빈 입력·악성 파일 계약 테스트
- 장소명 중복·오탈자·날짜 누락은 확인 필요·직접 보정으로 연결하고, 상대 날짜·영문 입력은 P1 지원 범위라고 안내하는지 테스트
- P0 일정 분석 경로에서 NER·LLM·외부 AI 네트워크 호출이 발생하지 않는지 테스트
- TripCandidate 저장이 TripItem·startAt·endAt을 암묵적으로 만들거나 Trip version을 올리지 않는지 테스트
- 후보 동일 POI 중복 저장의 idempotency와 CandidateTripMatch의 Trip version 만료·재계산 테스트
- 일정 편집 PATCH에서 destination/date/time/reservation 잠금이 독립 저장되고 예약 잠금 해제가 추가 확인 없이 처리되지 않는지 테스트
- 사용자가 확정하기 전 TripItem·장기 preference가 저장되지 않는지 확인
- 인코딩·디코딩 키 각각 검증
- 정상, 빈 응답, quota, timeout, 잘못된 파라미터 계약 테스트
- 집중률 horizon 경계일 테스트
- 예측 후보은행 10개 이상과 `DEMO_READY` 5개 이상이 유지되고, flat·누락·제약 실패 POI가 제외되는지 테스트
- 데모 생성일이 항상 실행일 이후·공식 horizon 안이며 실제 `LevelDelta >= 1`인지, 고정 POI·날짜·Level이 없는지 테스트
- Planning percentile 경계값 테스트
- 다른 POI의 Planning Level과 Live·Replay·Forecast·정성 Context를 단일 혼잡 순위로 정렬하지 않는지 테스트
- 서울 Live 원천 4단계가 sourceOrdinal 1~4로 정확히 사상되고 내부 Level 2가 창작되지 않는지 테스트
- Live freshness 경계와 AREA_CONTAINS_POI 범위 라벨 테스트
- 주변 반경·거리 정렬과 다른 관광지 필터 테스트
- 다른 `collectorRunId`를 같은 SnapshotSet으로 비교하지 않고 배치 내 최대-최소 관측시각 편차 10분을 넘으면 비교 세트에서 제외하는지 테스트
- 모든 Live 후보가 단일 `ORDINAL_LOWER` Selector와 같은 comparisonGroupId·snapshotSetId를 사용하는지 테스트
- `absoluteClaimMinOrdinal`을 3에서 4로 바꿔도 코드 변경 없이 claim·CTA 경계가 함께 바뀌는지 테스트
- RELATIVE는 일반 사용자 능동 CTA를 만들지 않고 사용자가 `지금 더 여유로운 곳` 필터를 직접 켠 경우에만 열리는지 테스트
- hard 조건 통과 후보 중 `candidate.sourceOrdinal<=2`가 ordinal 3 후보보다 우선되는지 테스트
- 중심점 0.5km 미만은 제외되고 중첩률 30% 초과는 AUTO_REJECT, 10~30%는 MANUAL_REVIEW 전 자동 노출되지 않는지 테스트
- `REPLAY`가 Source Registry·동일 SnapshotSet·`실시간 아님`·전체 E2E·7일 만료·주 1회 갱신 규칙을 지키는지 테스트
- `replayReady=false`이면 `currentLiveReady=true`여도 `spatialDemoReady=false`인지 테스트
- CandidateRelation 5개가 한 앵커에 몰리면 readiness가 실패하고 최소 2개 앵커에 분산돼야 통과하는지 테스트
- Forecast·정성 Context·다른 scope·normalizationVersion 후보가 우회 순위에 섞이지 않고 후보 없음 상태가 동작하는지 테스트
- P0 서버가 `lat`·`lng`·GPS anchorType 요청을 거부하고 URL·프록시·APM에 좌표가 없는지 테스트
- P0 KTO POI·서울 Live 권역 Canonical 매칭 테스트; 한영 High/Medium/Low는 P1 테스트로 분리
- P0 날씨가 CrowdLevel을 변경하지 않는지, P1 WeatherFit Feature Flag가 꺼져 있는지 테스트
- 서울 Live 지원 장소 코드 증감·1장소 1호출·캐시 fan-out 계약 테스트
- 검수 후보은행 점검 창 조밀 수집과 121개 전수 일 1~2회 수집이 분리되고 승인량 확인 전 5분 전수 수집이 실행되지 않는지 테스트
- POI 운영시간·예약 잠금·접근성·민감 지역 constraint 누락·만료 테스트
- API·키별 승인량 로드, 60/80/90% 경보, 배치 중단, request coalescing, `429`·한도 초과·운영계정 미승인 축소 프로필 테스트
- OptimizationRun 입력의 Trip version·scope·visibleItemIds·includeTripCandidates가 고정되고 권한 없는 Item/Candidate가 거부되는지 테스트
- DAY/TRIP 지도 지표는 유효한 RouteMatrixSnapshot 없이는 표시되지 않고 `ROUTE_UNAVAILABLE` 목록 Fallback으로 전환되는지 테스트

## 19.3 추천 규칙 테스트

- Must Visit에는 **자동** 목적지 대체가 생성되지 않음
- 날짜 고정 항목은 날짜 이동, 시간 고정 항목은 시간 이동, 예약 잠금 항목은 예약 차원의 변경이 각각 생성되지 않음
- 사용자가 요청한 교체는 Must Visit에도 동작하되 잠금 해제 확인을 거침
- 날짜 개선이 가능하면 더 큰 변경보다 우선
- 시간 근거가 없는 구간에는 시간 변경 적극 제안 없음
- 같은 제안 거절 후 즉시 반복하지 않음
- 만료 변경안 적용 차단·재계산
- 수락 실패 시 TripItem이 부분 변경되지 않음
- `includeTripCandidates=false`가 기본이며 사용자가 켜지 않은 후보가 자동 변경안에 포함되지 않음
- EXACT 후보는 중복 추가 CTA가 없고, SIMILAR 후보는 근거와 교체/그대로 두기를 제공하며 NONE은 추가만 제공
- DAY/TRIP change set의 일부 항목만 Gate를 실패해도 전체 적용되지 않음
- 닫힘·숨김·신고·이미 본 후보가 점수 계산 전에 제거됨
- 상위 Feed에서 동일 POI·권역·작성자 연속 노출 상한이 지켜짐
- P0 한 응답의 POI·권역 슬롯 상한이 지켜지고, P1 ExposureBudget은 Feature Flag가 꺼져 있음
- 운영시간·예약·일정 시각 충돌·접근성 Gate를 통과하지 못한 제안은 노출되지 않고 경로 API가 없을 때 이동시간 검증 완료 문구가 생성되지 않음
- 수락 직전 최신 `forecastIssueId`에서 개선이 뒤집히면 `DATA_CHANGED`로 만료되고 TripItem이 변경되지 않음
- 개선은 유지되지만 Level이 바뀌면 최신 Preview 확인 전 적용되지 않음
- 혼잡 결측이 0으로 저장되지 않고 CrowdPenalty·DistributionBonus·혼잡 근거가 함께 비활성화됨
- FeedScore component가 `[0,1]`을 벗어나지 않고 선택적 조정이 핵심 관련성 점수를 압도하지 않음
- 등록 전후 Top10ReplacementRate 30%·EvidenceCardCountTop10 4장·상위 3장 최소 1개 변화 체감 Gate를 Fixture별로 측정
- 적용 후 되돌리기와 동시 수정 충돌이 원자적으로 처리됨
- LLM 출력이 POI 존재·운영·혼잡·경로 가능성을 단독 확정하지 못하고, 결정적 검증 결과가 없는 변경은 Proposal에 포함되지 않음

## 19.4 UI·접근성 테스트

- 360px, 768px, 1280px 이상
- Chrome, Safari 최소 확인
- 키보드 탐색과 포커스
- 색상 없이 CrowdLevel 이해 가능
- 긴 한국어·오류 문구에서 레이아웃 유지, 번역 키 누락 없음
- **영어 리소스 추가 전에도** 대표 영어 문자열을 주입한 레이아웃 회귀 테스트를 통과 (배지·버튼·탭 잘림 없음)
- 영어 리소스 추가 후 전환 시 모든 핵심 화면의 레이아웃이 유지됨
- 이미지 없음·데이터 없음·느린 API 상태
- 일정 분석 중·일부 인식 실패·확인 필요·분석 실패 상태
- 장소 직접 선택·다음 일정 기준·지원 권역 밖 상태
- ABSOLUTE 능동 CTA, RELATIVE 사용자 직접 필터·능동 CTA 부재, REPLAY Header·카드·상세·`담아두기` 라벨, 후보 없음 상태
- 비교 불가 교차 POI에서 화살표·병렬 Level이 DOM에 생성되지 않고 자체 상태 칩만 표시되는지 테스트
- 공식 예측 범위 밖에 숫자 막대·CrowdLevel이 생성되지 않고 검증된 주말·공휴일 배지만 표시되는지 테스트
- 선택적 기기 내 위치 권한 요청 전·허용·거절·철회·GPS 실패와 좌표 비전송 상태
- P1 지도 Feature Flag가 꺼져도 목록 Fallback과 Live 지연 시 실시간 배지 제거
- S07 보기/편집/AI 모드가 제목·버튼·포커스로 구분되고, 후보 패널과 잠금 토글을 키보드·스크린리더로 조작 가능
- S09 지도만으로 변경을 전달하지 않고 타임라인·텍스트 변경 목록·지표를 함께 제공하며 적용 버튼에 변경 항목 수를 표시

## 19.5 제출 전 외부 검증

- 익명창에서 URL 접속
- 로그인 또는 데모 시작
- 외부 네트워크에서 HTTPS·인증서 확인
- 로그인 없는 DemoSession 또는 `EVID-SUBMIT-ACCOUNT`에서 확인한 테스트 방식 검증
- 계정 방식이 요구된 경우 심사 계정이 여러 번 사용돼도 동작
- 핵심 시나리오 3분 안에 완료
- 기능설명서 캡처와 최종 배포 UI 일치
- 사이트·PDF·제출 시스템의 서비스명과 API 목록 일치
- `D_submit` 원문·timezone·양식 버전이 근거대장에 등록되고 2인이 교차확인
- 중복 출품·동일 서비스 수상/지원 수혜 이력 Eligibility Register와 팀원 전원 서약 완료
- 최종 팀원 전원이 한국관광콘텐츠랩 회원이며 접수 시스템에서 조회 가능
- 사이트·PDF·제출 시스템에 타사 실명·로고·제휴 암시가 없고 `세로형 소셜 피드`로 통일
- `Nullnull` 네이밍 의도 문구가 팀 승인본과 일치하거나 미승인 시 제출물에서 제외
- 화면별 출처 표시
- 키가 프론트 번들·Git·PDF에 없는지 확인
- `DEMO_READY>=5`, 앵커 분산 CandidateRelation, 7일 이내 `replayReady`, `Q_projected_p95 <= 0.5 × Q_approved`, 심사 예비량 30% 이상이 readiness와 근거대장에서 일치

---

# 20. P0 검증 리스크와 의사결정

| 항목 | 리스크 | 완료 기준 | 실패 시 결정 |
|---|---|---|---|
| 일정 이해 정확도 | 범용 자유문장·상대 날짜를 지원하려다 품질과 일정이 무너짐 | 한국어 지원 Fixture 8종 회귀 통과, 계약 밖 입력은 직접 보정 | 파서 범위 추가 금지, 직접 입력을 기본 경로로 승격 |
| 일정 원문·업로드 파일 개인정보 | 예약번호·연락처·주소가 파일·로그에 남음 | P0 외부 AI 미호출, 원문·원본 파일 비영속, 파일명 최소화, 로그 검사 | 파일 원본을 폐기하고 인식된 항목의 직접 수정 중심 Fallback |
| 집중률 API horizon | 공개 설명은 향후 30일이나 실제 operation의 대상일·누락 규칙이 다를 수 있음 | 실응답으로 H·경계·누락 확정 | H 밖은 `공식 혼잡 예측 범위 밖`과 정성 달력 Context만 표시 |
| 예측 커버리지·평평한 분포 | 응답은 있지만 여행 기간 내 Level 차이가 없어 대표 기능이 무동작 | 후보은행 10개 이상, `LevelDelta >= 1`·제약 통과 `DEMO_READY` 5개 이상 | 5개 미만이면 `SUBMISSION_BLOCKED`; 다른 POI 자동 선택, 값 조작 금지 |
| 고정 데모 날짜 | 문서의 특정 날짜가 실행 시점에는 과거이거나 horizon 밖임 | `DemoScenarioFactory`가 D+7~D+10·실제 Level 차이를 매 세션 선택 | POI·날짜·Level 하드코딩 제거, readiness 실패 시 제출 중단 |
| 영문 관광정보 범위 누출 | EngService2·한영 매핑·이중 데이터 검수가 핵심 E2E를 지연 | P0 API 목록에 EngService2 없음. 영어 UI에서도 POI 원문은 국문 + `Korean only` 표시 | 영문 상세 연결을 P1 티켓으로 유지하고 UI 영어화만 P0에서 완성 |
| 서울 Live 매핑 | 권역과 개별 POI 혼동 | 최신 코드·검수 앵커 3개, CandidateRelation 5개·최소 2개 앵커 분산 | 개별 POI 실시간 표현 제거, 검수된 권역만 Live 표시 |
| 심사 시각 Live 평탄화 | 현재 절대 혼잡 권역이나 더 낮은 후보가 없어 공간 E2E 재현 실패 | 7일 이내 대표 REPLAY의 전체 E2E를 P0 필수로 유지 | REPLAY 없으면 현재 Live가 살아 있어도 `SUBMISSION_BLOCKED`; 재생을 현재로 위장 금지 |
| REPLAY 만료·연속 갱신 실패 | 비·행사·평탄화로 7일 안에 대표 세트를 교체하지 못함 | D-2 경보, 일 2회 점검, 최근 후보 수동 교체·전체 E2E 재검증 | `replayReady=false`, `spatialDemoReady=false`, 제출·시연 중단 |
| 지도 사업자 | 외국인 UX·쿼터·라이선스 | P1 도입 전 1개 사업자 계약 확정 | P0 목록형·기준 장소 선택 유지, 지도·경로 제안 비활성 |
| 날씨 의미 혼합 | 날씨로 관측 혼잡값을 보정해 허위 정밀도를 만듦 | P0 현재 날씨는 별도 Context로 표시, CrowdLevel 불변 테스트 | WeatherFit·날씨 기반 변경은 P1 Feature Flag 뒤로 이동 |
| 출처·이미지 권리 | 잘못된 표기·이용조건 | 화면·Seed metadata 검수 | 문제 이미지 제거 |
| OpenAPI 호출·저장 | 장기 로컬 미러만 사용해 실제 호출 이력이 없거나 이용 범위를 넘음 | 심사 경로 실제 호출 로그, 짧은 운영 캐시, 저장정책 서면 확인 | 미승인 영속 미러 제거, API 직접 호출+유효 캐시로 전환 |
| OpenAPI 쿼터 고갈 | 후보은행·Seed 갱신·심사 동시접속이 개발계정 승인량을 소진 | 운영계정/증설 신청번호, `Q_projected_p95 <= 50%`, 심사 예비량 30%, 60/80/90% 경보 | 비핵심 배치 중단·TTL 연장·축소 프로필; 핵심 실제 호출 예비량 보호 |
| 위치정보 | 좌표를 서버·지도 사업자에 전송하면 위치기반서비스 신고·약관 검토가 필요 | P0 API가 좌표를 받지 않고 장소 직접 선택·다음 일정 기준으로 완료 | 서버 GPS 기능 제거; P1은 공식 검토·승인 뒤 별도 출시 |
| 제출 마감 오기 | 공개 공고에 없는 `2026-09-21 16:00`을 공식 마감으로 믿어 제출 실패 | `EVID-SUBMIT-DEADLINE`에 별도 안내 원문·timezone·2인 확인 | `D_submit` 미확정 상태 유지, 운영 사무국 서면 문의 |
| 테스트 계정 형식 오기 | 추정 ID·비밀번호를 공식 형식으로 제출하거나 문서에 노출 | 실제 제출 화면·별도 안내를 `EVID-SUBMIT-ACCOUNT`에 등록 | 로그인 없는 DemoSession 유지, 형식 확정 전 계정값 문서화 금지 |
| 지정과제 연계기능 약함 | 시간·공간 분산 E2E가 외부 데이터 상태 때문에 심사 화면에서 사라짐 | 시간 `DEMO_READY`, 앵커 분산 CandidateRelation, 필수 REPLAY를 readiness로 보장하고 상세·TripCandidate·Optimization Preview까지 완료 | 해당 E2E가 없으면 과제 연계기능 완결을 주장하지 않고 제출 중단 |
| 참가 결격 | 동일 결과물 중복 출품·동일 서비스 수혜 이력·팀원 미가입 | Eligibility Register, 팀원 전원 서약·회원가입·2인 검수 | 제출 중단 후 운영 사무국 서면 확인 |
| 기준일 노후화 | API 범위·쿼터·공고 내용이 바뀌었는데 8/24 상태를 최신으로 오인 | owner·verified_at·next_review_at·evidence, 주 1회와 `D_submit-2영업일` 검증 | overdue 소스의 확정 수치·문구 제거 또는 재검증 전 비노출 |
| 기능 과다 | P0 불안정 | Submission Gate E2E 전부 통과 | P1·P2 메뉴·API·약속 숨김 |
| SNS 자기강화 | 인기·TripCandidate 신호가 새 집중을 생성 | P0 응답별 POI·권역 슬롯 상한과 다양성 테스트 | Social 가중치 0, 공식 Seed 균등 믹싱; 지속형 노출 예산은 P1 |
| 대안지 역혼잡 | 숨은 명소 추천이 새 핫스폿·주민 피해 유발 | 포화·민감 지역·수용력 Gate | 해당 후보 자동 추천 중단, 운영 검수 |
| 최적화 실행 불가 | 휴무·예약·일정 충돌을 무시하거나 경로 없이 이동시간 검증을 주장 | 확인 가능한 constraint Gate·route matrix·직선거리 Fallback 계약 테스트 | ITEM 날짜 변경만 제한 노출하거나 현재 일정 유지, `이동시간 확인 불가` 표시 |
| 소스 간 허위 비교 | KTO 상대 집중률과 서울 Live 권역 상태를 한 순위로 정렬 | `comparisonGroupId`·snapshotSetId·정규화 버전·금지 테스트 | 소스별 섹션 분리, 비교 문구 제거 |
| 데이터 대표성 | 이동통신 기반 상대값이나 권역 생활인구를 외국인 관광객 절대수로 오해 | 범위·단위·기준시각·한계 문구 노출 | 절대 인원·대기시간 표현 제거 |
| X 공개 코드 오용 | 현재 Apache 저장소와 과거 AGPL 저장소 혼동 | 저장소·commit·license 기록 | 코드 복사 없이 구조만 참고 |

## 20.1 즉시 확정해야 할 결정

결정이 끝난 항목을 다시 논의하지 않고, 미정 항목만 담당자와 기한을 붙여 닫는다. 아래 `담당자 실명 입력`은 제출팀이 실제 이름으로 교체한다.

| 결정 항목 | 상태·결정 | 담당 | 기한 |
|---|---|---|---|
| 공식 서비스명 | **확정:** `Nullnull — 내 일정을 읽는 분산형 여행 SNS` | 기획 | 완료 |
| 제출 팀명 | **확인 필요:** 접수 시스템에 등록한 팀명과 PDF·사이트를 동일하게 맞춤 | 담당자 실명 입력 | 2026-08-24 |
| 프론트엔드·배포 | **미정:** PWA·HTTPS·외부망 데모 조건으로 1개 조합 확정 | 담당자 실명 입력 | 2026-08-25 |
| 백엔드 운영환경 | **미정:** Spring Boot·PostgreSQL·Redis의 실제 배포 가능 조합 확정 | 담당자 실명 입력 | 2026-08-25 |
| P0 로그인 | **확정:** 무로그인 DemoSession, 저장·동기화는 P1 로그인 | 기획·개발 | 완료 |
| 1차 제출 마감 | **미확정:** 공개 공고에는 없음; `EVID-SUBMIT-DEADLINE` 원문 등록 후 `D_submit` 확정 | 담당자 실명 입력 + 교차확인자 | 안내 수신 즉시 |
| 테스트 방식·계정 형식 | **미확정:** 로그인 없는 DemoSession 기본, 별도 안내가 계정을 요구할 때만 형식 확정 | 담당자 실명 입력 | `D_submit-5영업일` |
| 일정 분석 | **확정:** P0 한국어 지원 Fixture 8종·규칙+장소 사전·직접 보정; 상대 날짜·영문·NER/LLM은 P1 | 기획·개발 | 완료 |
| 영어 서비스 | **확정(v5.7 개정):** 1차 타깃은 방한 외국인. 구현은 **한국어 P0 완성 → 안정화 후 영어 추가** 2단계. 레이아웃은 1단계부터 영어 길이 전제. EngService2·한영 매핑은 P1 | 기획·개발 | 완료 |
| 시간 분산 데모 | **확정/목록 필요:** D+7~D+10, 예측 후보은행 10개 이상, `DEMO_READY` 5개 이상 | 데이터 담당 | 2026-08-26 이후 매일 |
| 공간 분산 데모 | **확정:** 단일 ORDINAL_LOWER Selector, ABSOLUTE 능동 CTA·RELATIVE on-demand, 7일 이내 REPLAY 필수 | 데이터·프론트 | 지속 점검 |
| P0 위치 | **확정:** 장소 직접 선택·다음 일정 기준, 서버 GPS 좌표 미수신 | 기획·개발 | 완료 |
| 지도 사업자 | **P1 비차단:** 도입 전 계약·쿼터·위치정보 검토 후 1개 확정 | 담당자 실명 입력 | P1 착수 전 |
| OpenAPI 저장정책 | **확인 필요:** 실제 호출+짧은 캐시 원칙, 영속 미러 필요 시 제공기관 서면 질의 | 담당자 실명 입력 | 2026-08-25 |
| OpenAPI 운영계정·쿼터 | **즉시 실행:** API별 운영계정/증설 신청, 승인량·신청번호·예상 p95·심사 예비량 등록 | 대표·백엔드 | 2026-08-25 |
| Live REPLAY 보관 | **Source Registry 검증 Gate:** 공식 이용조건 원문 캡처·hash·출처표시·보존기간·전체 E2E·7일 만료를 확인 | 데이터 담당 | 주 1회·D-2 점검 |
| 과제 연계 해시태그 | **확정:** `#과밀지역우회`; Live의 동일 비교그룹 공간 분산 E2E와 연결 | 기획·개발 | 완료 |
| 참가 적격성 | **확인 필요:** 중복 출품·동일 서비스 수상/지원 수혜·최종 팀원 가입 | 대표 + 팀원 전원 | `D_submit-5영업일` |
| Nullnull 네이밍 설명 | **팀 승인 필요:** `정해진 여행의 정답을 비우고 내 일정과 취향으로 채운다` 초안 | 대표·기획 | PDF 동결 전 |
| 제출물 타사 실명 | **확정:** 대외 문구는 `세로형 소셜 피드`; Instagram·X 실명·로고·제휴 암시 제외 | 기획·디자인 | PDF 동결 전 |
| UI 디자인 반영 | **확정:** 재량·계약·개정 3층 구분(§6.8), UI 계약 8개 항목은 시안과 무관하게 유지, 시안 확정 시 §6.8 절차로 기획안 동기화 | 기획·디자인 | 시안 확정 시 |

---

# 21. 최종 Definition of Done

아래가 모두 충족돼야 개발 완료로 본다.

## 제품

- [ ] 비로그인·일정 미등록 사용자는 공식 Seed 여행 게시글 중심의 일반 Feed를 바로 볼 수 있다.
- [ ] 한국어 지원 Fixture 8종, 직접 입력, **또는 일정 없는 최소 입력** → 결과 수정·확인 → 개인화 Feed가 끊기지 않는다.
- [ ] 계약 밖·상대 날짜·영문 일정은 지원된다고 오인시키지 않고 직접 입력 보정 또는 P1 안내로 연결된다.
- [ ] 일정 등록 전·등록 후·여행 중의 세 Feed 상태가 구분되고, 여행 중에는 현재 날씨·Live 상태·거리·남은 시간이 별도 Context로 보인다.
- [ ] Feed·검색·Live의 `+`는 대상 여행의 TripCandidate만 만들고, 사용자 배치·교체 또는 최적화 승인 전에는 TripItem을 바꾸지 않는다.
- [ ] My Trip에서 `일정 편집`과 `AI로 일정 최적화`가 명확히 분리되고, 담아둔 장소 패널에서 추가·교체·유지를 완료할 수 있다.
- [ ] 담아둔 장소마다 기존 일정과 EXACT/SIMILAR/NONE 여부와 이해 가능한 근거가 표시되며 중복 항목이 조용히 생성되지 않는다.
- [ ] Feed 또는 Live 카드에서 30초 안에 후보 저장과 My Trip 편집/최적화 진입까지 도달한다.
- [ ] Live에서 장소 직접 선택 또는 다음 일정 기준으로 현재 여행지와 검수된 다른 관광지를 탐색하고 POI 상세·여행 후보 저장으로 연결할 수 있다.
- [ ] Live 후보는 단일 `ORDINAL_LOWER` Selector를 사용하고 같은 comparisonGroupId·snapshotSetId·CandidateRelation Gate의 더 낮은 sourceOrdinal 후보 → POI 상세 → 후보 저장 흐름을 완료한다.
- [ ] 일반 사용자 능동 CTA는 ABSOLUTE에만 노출되고 RELATIVE는 `다른 관광지` 탭의 사용자 직접 필터에서만 진입한다.
- [ ] REPLAY는 모든 관련 화면에 `과거 관측·실시간 아님`과 원 관측시각이 표시되고 7일 이내·주 1회 갱신·전체 E2E를 통과한다.
- [ ] 교차 POI 비교 불가 상태에서는 화살표·병렬 Level이 완전히 제거된다.
- [ ] 비교 가능한 우회 후보가 없으면 다른 소스·scope를 섞지 않고 정직한 후보 없음 상태를 보여준다.
- [ ] Must Visit 목적지는 유지된다.
- [ ] P0 ITEM 최적화는 동일 POI의 더 나은 날짜를 완결하고, P1은 공모전 최종 전 실제 route matrix로 DAY/TRIP의 시간·순서·경로와 사용자가 허용한 후보 교체를 완결한다.
- [ ] 추출 관심사와 추천 근거를 사용자가 조회·수정할 수 있다.
- [ ] 시간축은 동일 POI·동일 소스·동일 정규화 버전·같은 forecastIssueId에서만 변경 전후 CrowdLevel과 이유가 보인다.
- [ ] 날씨·거리·남은 시간·일정 제약은 관측 혼잡값과 합산되지 않고 각각의 근거로 표시된다.
- [ ] 최적화안이 운영시간·예약 잠금·일정 시각 충돌·접근성 Gate를 통과하고 경로 API 없이 이동시간 검증 완료를 주장하지 않는다.
- [ ] 시간축 OptimizationProposal은 적용 직전 최신 forecastIssueId에서 개선 방향을 재검증하고 뒤집혔으면 적용하지 않는다.
- [ ] 목적지·날짜·시간 잠금이 분리되어 저장되고 각 잠금이 변경안 생성·적용 시 지켜진다.
- [ ] AI Preview는 변경 전후 지도·타임라인·거리·이동시간·혼잡 지표와 변경 목록을 제공하고 승인 전에는 확정 일정이 바뀌지 않는다.
- [ ] `담아둔 대안도 포함`은 기본 OFF이며 사용자가 켠 실행에서만 TripCandidate가 목적지 교체안에 들어간다.
- [ ] change set은 전부 또는 전혀 적용되지 않으며, 적용 직후 되돌리기가 가능하고 다른 탭 수정과 충돌하면 안전하게 중단된다.
- [ ] **한국어 핵심 흐름이 동작하고** UI 문자열은 번역 키로 관리되며, 레이아웃이 영어 문자열 길이에서도 깨지지 않는다. 영어 리소스는 안정화 후 추가하고, 영문 관광정보 상세(EngService2)는 P1 Gate 전 비노출이다.
- [ ] 모바일·데스크톱에서 사용할 수 있다.

## 데이터

- [ ] 일정 원문은 기본 비영속이며 확정된 구조화 정보만 저장된다.
- [ ] InterestSignal에 근거·confidence·사용자 확인 상태가 있다.
- [ ] 한국관광공사 OpenAPI를 실제 호출한다.
- [ ] 미승인 영속 전체 미러 없이 실제 호출과 짧은 유효 캐시로 동작하며, 예외 저장은 서면 확인을 보유한다.
- [ ] 실제 사용 API만 문서·제출 시스템에 기재한다.
- [ ] Live, Replay, Forecast, 공식 예측 범위 밖 정성 Context 상태가 구분된다.
- [ ] 서울 Live 원천 4단계와 sourceOrdinal 1~4가 보존되고 공간 개선폭에 창작 5단계 차이를 쓰지 않는다.
- [ ] 한 수집 배치가 하나의 snapshotSetId이며 관측시각 최대-최소 편차 10분 Gate를 통과한다.
- [ ] 공간 CandidateRelation 5개 이상이 최소 2개 앵커에 분산되고 0.5km 최소 이격·중첩 3단계·검수 Gate를 통과한다.
- [ ] Live 권역 범위·mapping type·기준시각이 화면에 표시된다.
- [ ] 서로 다른 POI·제공처·범위의 혼잡값을 하나의 절대 순위로 정렬하지 않는다.
- [ ] 데이터 시점·신뢰도·Fallback이 표시된다.
- [ ] 모든 한국관광공사 데이터 화면에 올바른 텍스트 출처가 있다.
- [ ] 공식 이미지의 이용조건·출처 metadata가 카드까지 보존된다.
- [ ] P0 응답별 POI·권역·작성자 슬롯 상한이 적용되고, 지속형 ExposureBudget은 P1 Feature Flag 뒤에 있다.
- [ ] 집중률 예측 후보은행이 10개 이상이고 실제 `LevelDelta >= 1`·제약 통과 `DEMO_READY`가 5개 이상이다.
- [ ] DemoScenarioFactory가 실행일 기준 날짜와 실제 응답 Level을 선택하며 고정 POI·날짜·개선 수치를 사용하지 않는다.
- [ ] Feed 혼잡 결측은 0으로 대체되지 않고 CrowdPenalty·DistributionBonus·혼잡 근거가 함께 비활성화된다.
- [ ] 개인화 Fixture가 Top10ReplacementRate 30%·EvidenceCardCountTop10 4장·상위 3장 최소 1개 변화 초기 Gate를 통과한다.
- [ ] API별 운영계정/증설 신청번호·승인량·최근 peak·예상 p95가 `EVID-KTO-QUOTA`에 있고, 정상 예상량 50% 이하·심사 예비량 30% 이상이다.
- [ ] Source Registry의 공고·API 항목마다 owner, verified_at, next_review_at, evidence가 있고 주 1회 및 `D_submit-2영업일` 검증이 완료됐다.

## 안정성

- [ ] 일정 분석 실패 시 원문을 잃지 않고 직접 입력·수정으로 복구된다.
- [ ] 장소 직접 선택·다음 일정 기준만으로 Live 핵심 흐름을 완료할 수 있다.
- [ ] P0 서버·URL·프록시·APM·분석 로그가 GPS 좌표를 받거나 기록하지 않는다.
- [ ] 선택적 기기 내 위치 권한 거절·GPS 실패·P1 지도 비활성·Live API 실패가 Live 화면 전체 실패로 이어지지 않는다.
- [ ] 외부 API 실패 시 핵심 일정 기능이 깨지지 않는다.
- [ ] 후보 저장·Optimization Decision이 중복 처리되거나 부분 적용되지 않는다.
- [ ] DemoSession·Trip·TripCandidate·TripItem·Optimization 소유권과 CSRF·IDOR/BOLA 방어 테스트가 통과한다.
- [ ] 데모 상태를 복원할 수 있다.
- [ ] API 키와 테스트 비밀번호가 프론트·PDF·Git에 노출되지 않는다.
- [ ] 오류·빈 상태·로딩 상태가 구현돼 있다.
- [ ] 문서에 명시된 외부 API마다 키·쿼터·캐시·Fallback·계약 테스트 담당이 정해져 있다.
- [ ] API 호출량 60/80/90% 경보, 비핵심 배치 중단, TTL 연장, request coalescing과 `429` Fallback이 동작한다.
- [ ] 제출일부터 1차 기능심사 종료일까지 readiness 일 2회 점검·on-call·변경 동결 담당과 대응 절차가 정해져 있다.
- [ ] REPLAY D-2 경보·수동 대표 세트 교체·만료 시 `SUBMISSION_BLOCKED` 절차가 동작한다.

## 제출

- [ ] 최종 HTTPS URL이 익명창과 외부망에서 열린다.
- [ ] 테스트 방식과 계정이 검증됐다.
- [ ] `D_submit`과 테스트 계정 요구 형식이 각각 `EVID-SUBMIT-DEADLINE`, `EVID-SUBMIT-ACCOUNT` 원문으로 확인됐으며 추정값이 없다.
- [ ] 공식 기능설명서 양식에 실제 캡처가 들어갔다.
- [ ] 공식 분류 `②-2 웹·앱 구현 부문`, 공식 서비스명, 접수 팀명이 사이트·PDF·제출 시스템에서 일치한다.
- [ ] 사이트·PDF·제출 시스템의 명칭·기능·API 목록이 일치한다.
- [ ] P0 제출물·API 목록에 **EngService2를 구현 기능으로 기재하지 않는다.** 영어 UI는 실제 구현된 범위(화면 문구)만 기재하고 영문 관광정보 제공으로 오인시키지 않는다.
- [ ] `/api/demo/readiness`와 운영 근거에서 시간 `DEMO_READY>=5`, sourceRegistryValid, curatedRelationBankValid, `replayReady`, 쿼터 Gate가 모두 통과한다.
- [ ] 동일 결과물 중복 출품과 동일 서비스 공사 수상·지원 수혜 이력이 없다는 Eligibility Register·팀원 전원 서약이 완료됐다.
- [ ] 최종 팀원 전원이 한국관광콘텐츠랩 회원가입을 마치고 접수 시스템에서 조회·입력 가능하다.
- [ ] 사이트·PDF·제출 시스템은 `세로형 소셜 피드`를 사용하며 타사 실명·로고·화면 모사·제휴 암시가 없다.
- [ ] `Nullnull` 네이밍 설명은 팀 승인본만 사용하고, 미승인 상태면 제출물에서 제외한다.
- [ ] PDF 렌더링과 오탈자를 확인했다.
- [ ] 근거대장으로 확정한 `D_submit`보다 최소 1영업일 전에 제출하고 접수증·완료 화면을 보관했다.
- [ ] 1차 통과 시 `D_pt`·공식 발표시간을 등록하고 최종심사 30·30·25·15 기준의 PT·백업 시연·Q&A 리허설을 완료했다.

---

# 22. 기능별 구현 감사

## 22.1 전체 판정

핵심 방향은 맞다. 메인은 세로형 여행 게시글이 연속 노출되는 Feed이고, `일정 미등록 일반 Feed → 일정 등록 개인화 Feed → 여행 중 맥락형 Feed·Live`로 진화한다. v6.0의 기본 루프는 `일정에서 취향 추출 → 여행 게시글 발견 → 여행별 후보 저장 → My Trip 수동 편집 또는 AI 최적화 → 지도·시간 비교 → 승인`이다. 후보와 확정 일정을 분리했기 때문에 Feed 탐색의 가벼움과 일정 변경의 통제권을 동시에 유지한다.

공식 1차 심사의 `서비스 구현성`은 과제 연계기능 완결성을 직접 평가한다. 따라서 P0 화면에서 `ABSOLUTE CTA 또는 사용자 직접 RELATIVE 필터 → 같은 비교그룹·수집 배치의 검수 후보 → POI 상세 → TripCandidate` 공간 분산 흐름과 `같은 발행 회차의 실제 편차가 있는 동일 POI ITEM 최적화` 시간 분산 흐름을 각각 완결해야 한다. 이후 P1은 같은 모델 위에서 route matrix 기반 DAY/TRIP 리밸런싱을 공모전 최종 범위로 닫는다.

v5.9는 가장 불확실했던 공간 비교를 서울 Live 원천 4단계·단일 Selector·CandidateRelation·필수 REPLAY 계약으로 닫았다. v6.0은 S09를 ITEM/DAY/TRIP Optimization Preview로 확장한다. P1의 DAY/TRIP·검색·알림·UGC·영어·Live Re-plan은 Feature Flag 뒤에서 개발하되, 실제 통과한 기능은 공모전 최종 배포에 포함한다.

현재 날씨, 거리, 남은 시간, 일정 제약은 사용자의 판단과 추천 이유를 만드는 **독립 Context**다. 서울 Live의 관측 혼잡이나 KTO의 예측 집중률을 이 요소로 임의 보정해 하나의 `실시간 종합 혼잡도`라고 부르지 않는다.

## 22.2 S00~S15 감사표

| 화면 | 핵심 기능 | 주요 구현 문제 | 기획안 반영 보완 | 통과 조건 | 판정 |
|---|---|---|---|---|---|
| S00 | 랜딩·일반 Feed·데모 진입 | 일정 입력 전 빈 화면이면 SNS 첫인상이 약함 | 공식 Seed 여행 게시글 Feed를 첫 화면으로 두고 `내 일정을 읽는 여행 SNS`를 1순위, Live를 보조 탭으로 고정 | 5초 사용자 테스트에서 여행 게시글 Feed와 일정 개인화 가치가 함께 이해됨 | P0 진행 |
| S01 | 로그인 | 첫 가치 경험 전 로그인은 이탈과 데모 상태 충돌 유발 | 무로그인 데모·게스트 초안을 기본, 저장 시 로그인 선택 | 새 세션마다 독립 데모 복제, 로그인 실패가 데모를 막지 않음 | P0 최소 |
| S02 | 일정 입력·분석 | 범용 자유문장·다국어·상대 날짜는 공수와 오인식 위험이 큼. **1차 타깃(외국인)은 일정 미보유 비율이 높아 파싱 전제가 성립하지 않을 수 있음** | P0 한국어 Fixture 8종·TXT·CSV·XLSX 가져오기·결정적 규칙·직접 입력 보정·Step 2 사용자 확정; **일정 없는 최소 입력 경로를 동등한 P0 경로로 추가**; 외부 AI 미호출 | 텍스트·표 형식 Fixture 회귀 통과, 계약 밖 입력도 직접 보정해 흐름 완료, **최소 입력만으로도 Feed 도달** | P0 제한·중위험 |
| S03 | Social For You | UGC 없는 콜드스타트, 인기 신호 자기강화, 같은 권역 반복 | 공식 Seed임을 표시하고 일반·일정 개인화·여행 중 세 상태, P0 응답별 슬롯 상한으로 랭킹 | 추천 근거 수정 가능, 동일 POI·권역 연속 노출 상한 준수 | P0 핵심·고위험 |
| S04 | 검색·탐색 | 한·영 ID 불일치, 검색어 품질, 지도 사업자 의존 | Canonical POI High만 자동 연결, Live 내부 검색을 먼저 완성 | 대표 POI 한·영 매핑 계약 테스트 통과 | P1 유지 |
| ~~S05~~ | **삭제 · S11로 통합** | 별도 화면을 두면 Live 상세와 정보가 중복되고 유지 지점이 둘로 갈라짐 | S11 시트가 선택 시 장소 상세로 전면 교체. 기준시각·출처·이미지 권리 표시는 그대로 유지 | S11 상세에 필수 요소가 모두 표시됨 | **삭제 (v5.8)** |
| S06 | 여행 후보 저장·선택적 배치 | 후보와 확정 일정 혼동, 여행 선택, 동일 POI 중복 | `담아둔 장소` 저장을 기본으로 하고 즉시 배치는 명시적 2단계로 분리; 저장만으로 Trip version 불변 | Feed `+` 후 후보가 보이고 일정은 그대로이며 중복 저장은 기존 후보 반환 | P0 핵심 |
| S07 | My Trip 보기·수동 편집·AI 진입 | 모드 혼동, 동시 수정, 잠금, 후보 유사성 | 세 모드와 Trip version, 독립 잠금, CandidateTripMatch, dirty-state 이탈 방지 | 새로고침·다른 탭 수정에도 부분 적용 없고 교체/유지 선택이 명시됨 | P0 핵심 |
| ~~S08~~ | **삭제 · S11+S07로 분산** | 달력 화면을 따로 두면 사용자가 찾아가야 하고, 정작 결정은 일정 화면에서 일어남 | 혼잡 예보는 **S11 상세의 3구간 그래프**로, 날짜 변경 제안은 **S07 카드 인라인**으로 이동. 후보은행 10개·`DEMO_READY` 5개 Gate는 그대로 유지 | 세션마다 실제 Level 차이 POI 선택, 5개 미만이면 Submission 차단 | **삭제 (v5.8)** |
| S09 | AI 일정 최적화 Preview | 덜 붐벼도 휴무·예약·일정·접근성·경로 때문에 적용할 수 없거나 최신 데이터에서 개선이 뒤집힐 수 있음 | P0 ITEM 날짜 변경, P1 DAY/TRIP 지도·타임라인; 잠금·최신 회차·route matrix 재검증, opt-in 후보, 원자적 처리·되돌리기 | `DATA_CHANGED`·`TRIP_CHANGED`·`ROUTE_UNAVAILABLE` 미적용과 잠금 충돌 테스트 통과 | P0/P1 핵심·최고위험 |
| S10 | 검수된 주변 경험 | 연관성≠거리, 새 핫스폿·주민 피해를 만들 수 있음 | P0는 사전 검수한 관련 후보 1개만 보조 노출, 민감 지역·수용력 Gate; 공간 분산 주 E2E는 S11, 일반 주변 지도는 P1 | 후보가 부적합·포화면 추천 생략 | P0 보조 |
| S11 | 실시간 혼잡·과밀지역 우회 | 심사 시각 Live 평탄화, 교차 POI 허위 비교, RELATIVE 진입 경로 소실 | 단일 Selector, ABSOLUTE 능동 CTA·RELATIVE 사용자 직접 필터, 같은 comparisonGroupId·snapshotSetId, 7일 이내 필수 REPLAY | 앵커 분산 Gate와 후보→상세→담아두기, RELATIVE 능동 CTA 0, 재생 실시간 오인 없음 | P0 핵심·최고위험 |
| S12 | 알림센터 | PWA 백그라운드·iOS 권한·예보 갱신 신뢰성이 낮음 | P0에서 자동 알림 약속 금지, P1 인앱 알림부터 검증 | 권한 거절·백그라운드 제한에도 핵심 기능 유지 | P1 유지 |
| S13 | 게시물 작성 | 저작권·스팸·위험장소·신고·운영 인력이 필요 | UGC 작성 전 모더레이션·신고·숨김·삭제·권리처리 완성 | 미처리 신고 SLA와 운영자가 준비된 뒤 노출 | P1 유지 |
| S14 | Profile·설정 | Trip 관심사를 장기 프로필로 과잉 축적할 위험 | Trip 단위 기본, 장기 취향은 별도 동의·삭제·철회 | 위치·관심사·원문 삭제 경로 검증 | P0 최소 |
| S15 | 데이터 안내 | 출처가 많아지면 상태 문구·이용조건이 불일치 | Source Registry에서 UI 라벨·출처·범위·신선도를 생성 | 실제 호출 API와 화면·PDF·제출 목록이 일치 | P0 필수 |

## 22.3 Flow 감사 결과

현재 기본 루프는 `Feed → +로 여행 후보 저장 → My Trip 일정 편집 또는 AI 최적화 → Preview → 승인`으로 단축한다. 구 Calendar를 경유하지 않는다.

아래 분기는 반드시 화면과 API 모두에서 닫혀야 한다.

- 일정이 없음: 일반 Seed 여행 게시글 Feed 탐색 → `+` 시 여행 생성 또는 일정 등록 안내
- 일정 분석 일부 실패: 인식된 항목은 유지 → 확인 필요 항목만 직접 수정
- 혼잡 데이터 없음: 장소 발견·후보 저장·수동 편집은 유지 → 혼잡 최적화 근거만 생략
- 비교 가능한 공간 우회 후보 없음: 일반 Live 탐색 유지 → 다른 소스 값을 섞지 않고 후보 없음 표시
- 최적화 실행 불가: 낮은 혼잡 점수를 이유로 강행하지 않고 현재 일정 유지
- 선택적 기기 위치 거절·지원 권역 밖: 장소 직접 선택 또는 다음 일정 기준 → 소스별 Live/Forecast/정성 Context 표시
- 외부 API 전체 장애: 복원 가능한 Seed 데모와 출처가 있는 유효 캐시

## 22.4 개발 순서

1. 국문 관광정보·집중률 API의 운영계정/증설을 신청하고 API별 호출 예산·심사 예비량·경보를 먼저 고정한다.
2. S02 구조화 스키마·한국어 Fixture 8종·TXT/CSV/XLSX Import Fixture·열 매핑·직접 수정 UI와 P0 Canonical POI 5개를 고정한다.
3. 집중률 후보은행 10개와 `DEMO_READY` 5개를 확보하고 readiness API·롤링 선택을 계약 테스트한다.
4. S11 검수 앵커 3개·CandidateRelation 5개·최소 2개 앵커 분산과 단일 Selector·CTA/claim 분리·7일 이내 REPLAY 필수 계약을 닫는다.
5. S00·S03 공식 Seed Feed를 파이프라인 인터페이스로 만들고, 일반·일정 개인화·여행 중 세 상태를 룰 기반으로 실행한다.
6. S06 TripCandidate·CandidateTripMatch와 S07 보기/편집/AI 모드·독립 잠금·버전 일관성, S09 ITEM 트랜잭션·되돌리기를 완성해 P0 Gate를 닫는다.
7. S10은 사전 검수 후보 1개의 안전장치를 통과시킨 뒤 연결한다.
8. P0 Gate가 한국어로 안정된 뒤 P1 route matrix·DAY/TRIP 최적화·opt-in 후보 교체·Live Re-plan을 같은 Preview/Decision 계약으로 구현한다. 이어서 영어 UI, EngService2·한영 매핑, S04·S12·S13을 실제 검증 범위만 공모전 최종 배포에 연다.

---

# 23. 사용 기능·API·외부 의존성 전수 명세

## 23.1 기능 모듈 목록

| 모듈 | 하는 일 | 우선순위 | 구현 방식 |
|---|---|---|---|
| Itinerary Normalizer | P0 지원 한국어 날짜·시간·줄바꿈 표현 정규화 | P0 | 결정적 백엔드 규칙 |
| Itinerary File Importer | TXT를 텍스트 입력으로 변환하고 CSV·XLSX 시트·헤더·표시값을 공통 행으로 정규화 | P0 | MIME 검증·크기/행 제한·자동/수동 열 매핑; 수식·매크로 미실행 |
| Itinerary Parser | Fixture 8종에서 일정 항목·예약 잠금·Must Visit 후보 추출 | P0 | 규칙+장소 사전; 외부 AI 없음 |
| Interest Extractor | 장소 카테고리·명시 선호·반복 유형에서 관심사 생성 | P0 | 설명 가능한 룰, 사용자 확정 |
| Canonical POI Resolver | P0 KTO·서울 Live 외부 ID를 내부 UUID로 연결; 한영 연결은 P1 | P0/P1 | 좌표·명칭·주소·검수 |
| Empty Slot Calculator | 일정·이동 버퍼를 제외한 빈 시간 계산 | P0 | 결정적 룰 |
| Feed Query Hydrator | 여행·관심사·최근 행동·숨김·권역 Context 구성 | P0 | 서버 orchestration |
| Candidate Source | 공식 Seed·지역·행사·P0 검수 연관 후보, P1 일반 주변·소셜 후보 수집 | P0/P1 | Adapter 병렬 호출 |
| Candidate Hydrator | POI·혼잡·거리·운영·출처를 후보에 결합 | P0 | batch/cache |
| Eligibility Filter | 중복·닫힘·숨김·신고·본 항목·민감·포화 후보 제거 | P0 | hard gate |
| Feed Scorer | 관심사·여행 적합·신선도·분산 점수 계산 | P0 | 설정형 룰 |
| Diversity Reranker | 동일 POI·권역·작성자 연속 노출 억제 | P0 | MMR/슬롯 믹싱 |
| Response Diversity Cap | 한 응답의 동일 POI·권역·작성자 슬롯 상한 | P0 | 요청 단위 슬롯 믹싱 |
| Exposure Budget | 세션·시간창을 넘는 대안 장소·권역 추천 집중 상한 | P1 | 시간창별 카운터·운영 중단 |
| TripCandidate Manager | 여행별 후보 저장·중복 방지·출처 연결·상태 전이 | P0 | idempotent 트랜잭션; 저장만으로 Trip version 불변 |
| Candidate Match Engine | 후보와 확정 일정의 EXACT/SIMILAR/NONE 판정·근거 생성 | P0/P1 | 동일 Canonical ID + 검수된 관계·카테고리·거리 룰; versioned |
| Crowd Normalizer | Planning 5단계와 서울 Live 원천 4단계 sourceOrdinal을 분리하고 `comparisonGroupId` 부여 | P0 | 소스별 Adapter; 교차 소스 절대 비교 금지 |
| Crowd Forecast Presenter | 같은 POI·같은 발행 회차의 예측과 범위 밖 정성 Context 표시 | P0 | S11 혼잡 예보·S07 인라인, 설정 horizon |
| Demo Forecast Bank | 후보 POI 10개 이상을 점검해 실제 편차·제약을 통과한 `DEMO_READY` 5개 이상 유지 | P0 | Scheduler+유효 캐시+readiness Gate |
| OpenAPI Quota Manager | API·키별 승인량·예상량·심사 예비량·경보·배치 중단 관리 | P0 | 운영계정 증거+60/80/90% 정책 |
| Constraint Engine | 운영·예약·일정 충돌·접근성·민감 지역·수용력 검증; 경로 없이는 이동시간 미검증 | P0 | hard gate+직선거리 보수 필터 |
| Optimization Engine | P0 ITEM 날짜 변경, P1 DAY/TRIP 시간·순서·경로·opt-in 후보 교체 탐색 | P0/P1 | 결정적 제약·목적함수·route matrix; LLM은 설명 보조 |
| Optimization Decision & Rollback | 적용·유지·만료·충돌·change set 되돌리기 | P0/P1 | version+idempotency+원자적 처리 |
| Live Aggregator | 후보은행 점검 창 조밀 수집·121개 일 1~2회 발굴, 수집 배치·10분 편차·권역 매핑·캐시 | P0 | anchorId 기반 서버 Adapter |
| Live Detour Selector | 동일 comparisonGroupId·snapshotSetId에서 낮은 sourceOrdinal과 CandidateRelation Gate 적용 | P0 | 단일 ORDINAL_LOWER+claim/CTA 분리; 후보 없음 허용 |
| Replay Readiness Manager | 대표 REPLAY 선택·주 1회 갱신·D-2 경보·만료 차단 | P0 | Source Registry+E2E+7일 신선도+SUBMISSION_BLOCKED |
| Anchor Resolver | 장소 직접 선택·다음 TripItem을 Live 기준 장소로 변환 | P0 | Canonical POI·검수 권역 mapping |
| Device Location Helper | 사용자 동작 시 기기 안에서 가까운 기준 장소 후보를 찾고 placeId만 확정 | P0 선택 | Browser Geolocation; 좌표 서버 전송 없음 |
| Source Registry | 출처·범위·상태·기준시각·이용조건·검증 근거와 갱신 책임 UI 계약 | P0 | owner·verified_at·next_review_at·evidence 포함 서버 metadata |
| Analytics | 노출·행동·제안·대체 포화·Fallback 측정 | P0 | 자체 event API |
| Weather Fit | 유효 예보 구간의 실내외·시간 보조 | P1 | KMA Adapter |
| UGC & Moderation | 게시·신고·숨김·차단·권리 관리 | P1 | 운영 도구 포함 |
| Notification | 예측 갱신·Live 악화 인앱 알림 | P1 | 인앱 우선, Web Push 조건부 |
| ML Retrieval/Ranker | 임베딩 후보 탐색·다중 행동 확률 | P2 | 데이터 충분 시 별도 서비스 |

## 23.2 확정 외부 API — P0

초기 검증 기준일은 **2026-08-24**이며 영구 유효한 사실을 뜻하지 않는다. API 경로·쿼터·장소 수는 코드에 박지 않고 Adapter 설정과 계약 테스트로 관리하며, Source Registry 담당자가 주 1회와 `D_submit-2영업일`에 공식 문서·실응답을 다시 확인한다. `next_review_at`이 지난 값은 문서·UI의 확정 수치로 사용하지 않는다.

### A. 한국관광공사 국문 관광정보 서비스_GW

- 공식 문서: [공공데이터포털 KorService2](https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15101578)
- Base URL: `https://apis.data.go.kr/B551011/KorService2`
- 인증: 공공데이터포털 `serviceKey`, 서버 전용
- Nullnull 사용 operation:
  - `areaCode2`: 지역코드
  - `lDongRegnCd2`, `lDongSignguCd2`: 법정동 코드 — 최신 Swagger에 존재할 때 사용
  - `lclsSystmCode2`: 신분류체계 코드 — 최신 Swagger에 존재할 때 사용
  - `areaBasedList2`: 지역 기반 POI·Seed 동기화
  - `locationBasedList2`: 선택된 POI 기준 P0 검수 후보 확인; P1 일반 주변 탐색
  - `searchKeyword2`: 장소 매핑·검색
  - `searchFestival2`: 여행 기간 행사 후보
  - `detailCommon2`: 명칭·주소·좌표·개요·홈페이지
  - `detailIntro2`: 콘텐츠 유형별 운영·이용 정보
  - `detailInfo2`: 반복 상세 정보
  - `detailImage2`: 카드·상세 이미지 metadata
- 공통 요청: `MobileOS=ETC`, `MobileApp=Nullnull`, `_type=json`, 페이지·행수, contentTypeId·areaCode·좌표 등 operation별 필수값
- 주요 저장값: external contentId, contentTypeId, title, address, mapX/mapY, category, modifiedTime, image URL, copyright/type metadata
- 캐시·Fallback: 심사 경로에서 실제 OpenAPI를 호출하고 응답별 짧은 TTL 캐시를 사용한다. 실패 시 유효 캐시와 기준시각을 표시하며, 전체 데이터의 영속 로컬 미러는 기본값으로 두지 않는다.
- 쿼터: 공식 페이지 기준 개발계정 1,000건, 운영은 활용사례 등록 후 증설 신청; 화면 카드별 중복 호출은 서버 batch·단기 캐시로 줄인다. 증분 동기화·영속 저장이 꼭 필요하면 최신 계약과 제공기관 서면 답변을 먼저 Source Registry에 보관한다.
- 주의: 공식 설명상 자유 활용 가능한 콘텐츠만 제공되더라도 이미지별 저작권·이용조건 metadata를 보존하고 CI·BI 용도로 전용하지 않는다.

### B. 한국관광공사 관광지 집중률 방문자 추이 예측 정보

- 공식 데이터셋: [한국관광공사 관광지 집중률 방문자 추이 예측 정보](https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15128555)
- 데이터셋 메타 ID: `15128555`; 내부 Adapter는 `KtoCrowdForecastAdapter`로 명명하고 실제 Base URL·operation은 활용신청 후 최신 Swagger 값을 계약 테스트에 고정한다. 검증 전 통칭으로 알려진 service명을 운영 경로에 하드코딩하지 않는다.
- 사용처: S03 DateFit, **S11 상세 혼잡 예보 3구간**, S06 인라인 인사이트, **S07 카드 인라인 제안**, S09 날짜 제안
- 데이터 의미: 조회 시점 기준 향후 30일, 2018년 이후 이동통신 기반 패턴, 가장 붐비는 시기를 100으로 둔 **상대 집중률**
- 금지: 실제 현장 인원·대기시간·개별 방문자의 확정 행동처럼 표현하거나 다른 원시 소스와 직접 합산
- 캐시·Fallback: 짧은 TTL과 targetAt·`forecastIssueId` 저장 → 유효 캐시 → H 밖은 `공식 혼잡 예측 범위 밖` 정성 Context → 모두 없으면 제안 생략
- 개발 Gate: 후보 POI 10개 이상의 대상 ID·30일 경계·빈/flat 응답을 실응답으로 검증하고 `LevelDelta >= 1`·제약 통과 `DEMO_READY` 5개 이상 유지
- 쿼터: 공식 페이지 기준 개발계정 1,000건, 운영계정은 활용사례 등록 후 증설 신청 가능; 즉시 신청번호를 근거대장에 등록하고 Scheduler·캐시 없이 카드별 직접 호출 금지

### C. 한국관광공사 관광지별 연관 관광지 정보

- 공공 서비스 통칭: `TarRlteTarService1`; 내부 Adapter는 `KtoRelatedPoiAdapter`로 명명한다. Base URL·operation은 활용신청 후 제공되는 최신 Swagger를 source of truth로 고정하며, 검증 전 예상 경로를 운영 계약으로 쓰지 않는다.
- 사용처: **S07 인라인 대체 관광지**, S09 주변·경유 제안, S10 후보 소싱
- 제공 기능: 지역 기반 연관 관광지 목록과 키워드 검색 연관 관광지 목록. 공식 활용사례 설명 기준 전체·관광지·음식·숙박 유형별 최대 각 50위 후보를 제공한다.
- 의미: T map 차량 내비게이션 이용 행태 기반 연결성으로, 물리적 도보거리·실제 연계 방문자 수·영업·수용력을 보장하지 않는다.
- 공식 활용사례에 안내된 데이터 기간이 2024-05~2025-04이므로 실시간 Trending처럼 표시하지 않는다. 실제 응답의 기간·유형 metadata를 보존하고 좌표·지도 경로·운영 Constraint를 통과시킨다.
- Fallback: 사용자가 선택한 Canonical POI를 기준으로 KorService2 `locationBasedList2` + 카테고리·거리 기반 후보를 만들고, P0에는 사전 검수한 1개만 노출

### D. 서울특별시 실시간 도시데이터

- 공식 문서: [서울시 실시간 도시데이터 OA-21285](https://data.seoul.go.kr/dataList/OA-21285/A/1/datasetView.do)
- 호출 형식: `openapi.seoul.go.kr:8088/{KEY}/{TYPE}/citydata/{START_INDEX}/{END_INDEX}/{AREA_NM 또는 AREA_CD}` — 실제 HTTPS 지원과 인코딩은 서버 PoC로 확정
- 인증: 서울 열린데이터광장 KEY, 서버 전용
- 사용 필드: `AREA_NM`, `AREA_CD`, `LIVE_PPLTN_STTS`, `AREA_CONGEST_LVL`, `AREA_CONGEST_MSG`, `AREA_PPLTN_MIN`, `AREA_PPLTN_MAX`, `PPLTN_TIME`; 원천 상태는 sourceOrdinal 1~4로 보존한다. P0 현재 날씨는 별도 Weather Context로 파싱하고 혼잡값과 합산하지 않는다.
- 공식 현재 범위: 문서 기준 주요 121장소지만 개수는 변할 수 있으므로 장소명·코드 파일을 동기화하고 코드에 `121`을 고정하지 않는다.
- 호출 제약: 한 번에 1개 장소. 브라우저에서 N개 요청하지 않는다. 승인량 확인 전 5분 전수 수집을 금지하고 후보은행 점검 창 조밀 수집과 121개 전수 일 1~2회 발굴을 분리한다.
- 사용처: S11 `현재 여행지/다음 일정/검수된 다른 관광지`; S09 Live Re-plan은 P1
- 범위 표시: `AREA_CONTAINS_POI`이면 `경복궁 일대 기준`처럼 권역 데이터임을 표시
- Fallback: 같은 원천의 유효 Live 캐시 → Live 상태 제거. KTO Planning Crowd는 별도 Forecast 그룹이며 현재 관측 Fallback으로 위장하지 않는다.
- 라이선스: 공식 페이지에 표시된 공공누리 제1유형 출처표시 조건을 따른다.
- 재생 사용: 데이터셋 상세·이용약관·API 발급조건·최신성/재가공 안내·출처표시 원문을 캡처하고 hash·검토자·next_review_at을 Source Registry에 등록한다. 검증된 세트만 7일 동안 사용하고 최소 주 1회 교체하며 `과거 관측·실시간 아님`·원 관측시각·서울특별시 출처를 고정 표시한다.
- 공간 비교: 한 수집 사이클을 `collectorRunId=snapshotSetId`로 묶고 원 관측시각 최대-최소 편차 10분 이하에서만 같은 comparisonGroupId의 sourceOrdinal을 비교한다.

### E. Browser Geolocation API

- 표준 기능: 사용자가 `현재 위치로 기준 장소 찾기`를 누른 경우에만 `navigator.geolocation.getCurrentPosition()`을 선택적으로 호출
- 사용처: 미리 내려받은 검수 기준 장소 중 가까운 후보를 **브라우저 안에서만** 계산하고, 사용자가 확인한 `placeId`를 S11 기준 장소로 설정
- 요구조건: HTTPS secure context, 명시적 사용자 동작 후 권한 요청, timeout·maximumAge 설정, 기능 미사용·거절을 기본 경로와 동등하게 지원
- 보관·전송: 원시 좌표는 서버·지도 사업자·URL·분석 SDK·오류 로그로 전송하거나 저장하지 않는다. 서버 API는 좌표 파라미터와 `GPS` anchorType을 거부한다.
- Fallback: `장소 직접 선택` 또는 `다음 일정 기준`; 위치 권한이 없어도 Live 핵심 기능 완료
- 성격: 제3자 상용 API가 아니라 브라우저 표준 기능이다.

P0의 `실시간 여행 Context`는 아래처럼 조합하되 값의 의미는 분리한다.

```text
Observed Crowd = 서울 Live가 관측한 해당 권역 상태와 기준시각
Planning Forecast = KTO가 제공하는 동일 POI의 날짜별 상대 집중률
Current Weather = 유효한 현재 날씨 필드
Trip Context = 선택 장소·다음 일정·거리·남은 시간·사용자 제약
Recommendation = 위 Context를 함께 설명해 만든 행동 제안
```

`Observed Crowd`는 날씨·거리·일정 제약으로 재계산하지 않는다. 서로 다른 source·target scope의 값을 합쳐 `실시간 종합 혼잡도` 또는 관광객 절대 인원처럼 표시하지 않는다.

## 23.3 조건부·P1 API

| API/기능 | Endpoint·기능 | 사용 조건 | 미확정 시 동작 |
|---|---|---|---|
| 한국관광공사 EngService2 | 영문 POI·상세·이미지·검색·행사 | 한영 Canonical 매핑·영문 관광정보 검수가 통과한 P1 | P0 API 호출·제출 목록에서 제외. **영어 UI 추가 후에도 POI 원문은 국문 + `Korean only` 표시로 동작** |
| 기상청 단기예보 | [단기예보 조회서비스](https://www.data.go.kr/data/15084084/openapi.do), `VilageFcstInfoService_2.0`의 `getUltraSrtNcst`, `getUltraSrtFcst`, `getVilageFcst` | D-3~당일 WeatherFit을 실제 구현할 때 | 날씨 가중치·문구 제거 |
| 기상청 중기예보 | [기상청 API허브 중기예보](https://apihub.kma.go.kr/apiList.do?seqApi=10&seqApiSub=287)의 육상·기온 예보 | D-10~D-4 광역 주의를 실제 구현할 때 | 날짜 혼잡 비교만 유지 |
| 지도·경로 API | 지오코딩, 보행·대중교통 거리와 ETA, 지도 SDK | Google Maps·Mapbox·Kakao·Naver 등에서 외국인 UX·PWA·쿼터·라이선스·국내 경로 지원을 비교해 **한 사업자만** 확정 | 직선거리, `이동시간 확인 불가`; 경로 제안 비활성 |
| 외부 AI/LLM API | 자유 일정의 개체·관심사 후보 추출 | 제공자·리전·보존·학습사용·비용·DPA를 확정하고 PII 제거 PoC를 통과할 때 | 규칙 파서+장소 사전+직접 수정으로 P0 완성 |
| Web Push/Push Provider | 예측 갱신·Live 악화 알림 | PWA 권한·iOS 백그라운드·구독 해제·조용한 시간 정책을 검증한 P1 | 인앱 알림만 제공 |

기상청 중기예보는 날짜 4~10일 수준의 광역 예보이고, 단기예보는 격자 기반이다. POI 내부 상태나 30일 뒤 시간별 날씨로 확대 해석하지 않는다.

## 23.4 현재 사용하지 않는 API·기술

| 항목 | 현재 결정 | 이유/재검토 조건 |
|---|---|---|
| X API | 사용 안 함 | X 콘텐츠·팔로우·사용자 데이터를 가져오지 않음; 공개 저장소 구조만 참고 |
| X Phoenix/Thunder/SimClusters 런타임 | P0 사용 안 함 | Nullnull 데이터·규모가 없고 GPU·실시간 인프라가 과도함 |
| 과거 `twitter/the-algorithm` 코드 | 복사 금지 | AGPL-3.0 경계와 현재 X 구조 차이; 별도 법률·아키텍처 검토 없이는 사용하지 않음 |
| KTO 빅데이터 지역별 방문자수 | 미사용 | 장기 패턴을 실제 구현·검증할 때만 제출 목록에 추가 |
| KTO 지역별 관광 다양성·수요 강도·관광 자원 수요 | P2 검증 후보 | 신규·확장 데이터의 정의·대상·계약을 확인한 뒤 분산 공급 신호로 평가 |
| 소셜 로그인 | 미선정 | P0 무로그인 데모 우선 |
| 외부 분석 SDK | 미선정 | P0는 자체 `/api/events`, 원시 위치·일정 원문 제외 |
| 자동 번역 API | 사용 안 함 | 공식 EngService2 우선, UGC 자동 번역은 P2 |

기능설명서와 공모전 제출 시스템에는 위 표의 후보가 아니라 **배포 버전에서 호출 로그가 확인된 API만** 기재한다.

## 23.5 API 운영 체크리스트

각 Adapter는 다음 값을 Source Registry에 등록한다.

- provider, service name, portal URL, base URL, operation, owner
- verified_at, verified_by, next_review_at, evidence URL 또는 원문 파일 hash, 마지막 contract test 결과
- key 발급 계정, 개발·운영 승인 상태, 운영계정/증설 신청번호, API별 `Q_approved`, 예상 p95, 최근 peak, 심사 예비량
- request parameter, response schema, charset, 좌표계, locale
- source scope, observedAt/targetAt/lastUpdatedAt, freshness TTL
- retry·timeout·circuit breaker·cache key·stale 허용시간
- Fallback 순서와 UI 문구
- 이용조건·출처표시·이미지 권리·보존·과거 관측 재생 가능 범위와 최대 기간
- REPLAY 대표 세트 ID·선택 이유·원 관측시각·age·D-2 경보·전체 E2E 검증·교체 이력
- contract test fixture, API 버전·변경 공지, 주 1회와 `D_submit-2영업일` 재검증 상태

공공 API도 점검·장애가 발생하므로 데모는 실시간 호출 성공에만 의존하지 않는다. 단, 심사 검증을 위해 실제 호출 로그와 유효 캐시 여부를 구분해 남긴다.

---

# 24. X 공개 추천 알고리즘의 Nullnull 적용 명세

## 24.1 참고 기준과 표현 원칙

- 현재 참고 저장소: [xai-org/x-algorithm](https://github.com/xai-org/x-algorithm), 초기 검증일 2026-08-24; 실제 코드·라이선스 인용 시 PDF 동결 전에 commit·license 재확인
- 라이선스: [Apache License 2.0](https://github.com/xai-org/x-algorithm/blob/main/LICENSE)
- 과거 저장소: [twitter/the-algorithm](https://github.com/twitter/the-algorithm), AGPL-3.0 — 현재 기준과 혼용하지 않는다.
- 내부 기술 문서에서는 `X의 공개 추천 파이프라인에서 구조적 아이디어를 참고한 Nullnull 추천 시스템`으로 설명할 수 있다.
- 사이트·기능설명서·제출 시스템·PT에서는 타사 실명 대신 `다단계 후보 생성·필터·점수화·다양성 재정렬 구조를 자체 구현`으로 표현한다.
- X의 모델·가중치·프로덕션 데이터·API를 그대로 쓴다는 표현은 금지한다.
- 실제 코드를 복사하면 사용한 파일·commit·license·NOTICE를 기록한다. P0 권장은 **코드 복사 없이 개념만 자체 구현**하는 것이다.

X 공개 구조의 핵심은 단순히 좋아요가 많은 글을 정렬하는 것이 아니다. 요청 Context를 채운 뒤 여러 소스에서 후보를 가져오고, 후보 정보를 보강하고, 부적격 후보를 먼저 제거하고, 여러 행동의 예상확률을 조합해 점수를 만들고, 다양성·가시성 정책을 거친 뒤 노출과 로그를 기록하는 pipeline이다.

공개 저장소의 주요 구성과 Nullnull의 판단은 다음과 같다.

| X 공개 구성 | 공개 역할 | Nullnull 판단 |
|---|---|---|
| `home-mixer/` | For You 요청 경로·stage·score weight orchestration | stage 구조와 설정 분리만 참고 |
| `candidate-pipeline/` | source, hydrator, filter, scorer, selector, side effect의 조합·병렬 실행 | P0 backend interface 설계에 직접 적용 |
| `thunder/` | 팔로우한 계정의 최신 in-network post를 메모리에서 공급 | 팔로우 그래프가 없는 P0에서는 미사용; P1 Following source로 별도 구현 |
| `phoenix/` retrieval | viewer·post embedding 기반 out-of-network 후보 탐색 | P2 two-tower 후보로만 검토 |
| `simclusters/` | 참여 그래프 cluster로 out-of-network 후보 탐색 | 충분한 소셜 행동 그래프 전에는 미사용 |
| `phoenix-rankall/` | Phoenix retrieval index 유지 | P0 KTO Seed 인덱스·DB로 대체 |
| `phoenix/` ranking | transformer로 각 후보의 여러 행동 확률·연속값 예측 | P0 설정형 action utility, P2 multi-task model |
| `RankingScorer` | `Σ(weight × predicted action probability/value)`와 작성자 다양성·OON 감쇠·신규 작성자 boost | 원시 좋아요 수 가중치로 오해하지 않고, Nullnull용 POI·권역·신규 장소 보정으로 재설계 |
| `vm-ranker/` | embedding DPP로 이웃 post 유사도를 낮추는 재정렬 | P0 MMR/슬롯 믹싱, P2 DPP 평가 |
| `grox/`·content safety components | 게시 시점 분류·표현·안전 라벨 생성 | UGC P1에서 별도 모더레이션 설계; X 모델 복제 안 함 |
| `visibility-filtering/` | 랭킹과 별도로 show/drop/interstitial 판단 | 폐업·위험·신고·민감 지역 최종 Visibility Gate로 원칙 적용 |

공개 저장소도 전체 X 프로덕션 환경과 동일한 배포 패키지는 아니다. 일부 Grox prompt·규칙은 공개되지 않았고, 여러 구성에는 내부 service runner·Kafka 등 배포 인프라가 포함되지 않는다. Phoenix의 실행 예시는 synthetic data·소형 환경을 포함하므로 이를 Nullnull용 학습 데이터나 즉시 배포 가능한 모델로 간주하지 않는다.

## 24.2 단계별 대응

| X 공개 pipeline | Nullnull P0 대응 | P1/P2 확장 | 그대로 가져오지 않는 것 |
|---|---|---|---|
| Query Hydration | TripInterest, 근거 POI, EmptySlot, TripItem, locale, 최근 노출·숨김·TripCandidate, 현재 권역 | 장기 선호·팔로우를 별도 동의로 추가 | X 사용자·팔로우 데이터 |
| Candidate Sources | KTO 공식 Seed, 지역·행사, 선택 POI 기준 검수 연관 관광지, Live 지원 POI | 일반 주변·UGC·Following·검수 Creator | Thunder 인메모리 소셜 그래프 |
| Candidate Hydration | POI 상세·이미지·혼잡·거리·운영·출처·수용력·권리 정보 | 임베딩·Creator 품질 | X post/author 구독 metadata |
| Pre-scoring Filters | 중복, 이미 본 항목, 닫힘, 여행 범위 밖, 숨김·신고, 민감 지역, 포화 상한 | 안전 모델·모더레이션 라벨 | X 정책 라벨 그대로 복제 |
| Scoring | `[0,1]` BaseRelevance + `[-0.15,+0.15]` 선택적 조정, 혼잡 결측 비활성 | 다중 행동 확률 모델 | X 가중치와 Phoenix checkpoint |
| Adjustments | 동일 POI·권역·작성자 감쇠, 새 권역 보너스, 과노출 감점 | 개인별 exploration 보정 | X author/OON 계수를 그대로 사용 |
| Selection/Reranking | greedy MMR 또는 슬롯 믹싱으로 인접 다양성 | 데이터·성능 확보 후 DPP 검토 | P0 GPU·대규모 DPP 인프라 |
| Visibility/Safety | 랭킹과 별도로 폐업·위험·신고·차단·민감 지역 최종 검사 | UGC 안전·권리 workflow | 점수만으로 안전 여부 결정 |
| Side Effects | impression·rank·reason·source·dwell·feedback·응답별 슬롯 사용 기록 | P1 지속형 노출 예산·학습 feature·A/B assignment | 개인정보·원시 위치 로그 |

## 24.3 P0 추천 요청 처리

```text
1. Context hydrate
   trip + confirmed interests + empty slots + recent actions + blocks

2. Candidate source
   official seed + area + vetted related POI + active events

3. Candidate hydrate
   canonical POI + crowd + distance + operation + rights + source metadata

4. Hard filter
   duplicate/seen/hidden/reported/closed/out-of-range/sensitive/saturated

5. Rule score
   bounded base relevance + bounded optional adjustment
   crowd evidence missing => crowd penalty/distribution reason disabled

6. Diversity rerank
   enforce per-response POI/area/author slot caps

7. Final visibility check
   source rights + safety + freshness + support scope

8. Return reason codes and log impression
```

추천 이유는 최종 점수의 가장 큰 임의 항목이 아니라, 실제로 사용된 사용자 확인 신호와 유효 데이터만으로 만든다. 예: `경복궁 일정에서 읽은 역사 관심사`, `여행 중 확인된 오후 빈 시간`, `같은 예측 발행 회차에서 더 낮은 날짜 상태`.

## 24.4 다중 행동 신호 설계

X Phoenix는 여러 행동의 확률을 예측하고 그 예측값을 가중 결합한다. Nullnull P0는 학습 확률 대신 이벤트별 설정형 점수를 쓰되 같은 인터페이스를 유지한다.

```text
ActionUtility =
  w_candidate * normalized(candidate_save)
  + w_schedule * normalized(candidate_to_schedule)
  + w_save * normalized(save)
  + w_click * qualified_detail_click
  + w_dwell * qualified_dwell
  + w_apply * optimization_apply
  - w_skip * repeated_short_skip
  - w_not_interested * not_interested
  - w_hide * hide
  - w_report * report
```

- 원시 좋아요·조회수는 후보 규모와 노출량 편향이 커서 그대로 합산하지 않는다.
- TripCandidate 저장도 유명 장소를 자기강화할 수 있으므로 P0 응답별 POI·권역 슬롯 상한과 함께 사용한다. 후보 저장과 실제 일정 반영은 서로 다른 신호로 학습한다. 세션·시간창을 넘는 ExposureBudget은 P1에서 운영 지표와 중단 절차를 갖춘 뒤 적용한다.
- 신고는 단순 선호 점수보다 최종 Visibility·운영 검토를 우선한다.
- 체류는 foreground·최소시간·반복 탭을 정제한 `qualified_dwell`만 사용한다.
- 사용자별 장기 프로필 학습은 별도 동의를 받은 뒤에만 한다.

## 24.5 P2 ML 전환 Gate

아래 조건을 모두 충족하기 전에는 Phoenix형 모델을 도입하지 않는다.

1. 노출·후보·rank·Context·부정행동이 누락 없이 기록된다.
2. 학습·검증 기간 분리와 사용자 단위 누수가 없는 dataset을 만들 수 있다.
3. 강한 긍정·부정 행동의 표본이 안정적으로 쌓이고 콜드스타트 비율을 측정할 수 있다.
4. 룰 baseline 대비 offline relevance, calibration, coverage, diversity, Planned Peak Exposure를 함께 개선한다.
5. A/B에서 후보 저장만 높이고 실제 일정 반영·분산·만족·숨김을 악화시키지 않는다.
6. 모델 장애 시 P0 룰 랭커로 즉시 Fallback할 수 있다.

P2 후보 구조는 `two-tower retrieval → multi-task ranker → diversity reranker`다. X 공개 Phoenix의 transformer·checkpoint는 Nullnull 여행 도메인과 행동 정의가 다르므로 그대로 배포하지 않는다.

## 24.6 추천의 오버투어리즘 안전 목적함수

Nullnull은 engagement만 최대화하지 않는다.

```text
NullnullUtility =
  UserRelevance
  + TripFeasibility
  + DiscoveryValue
  + DistributionValue
  - CrowdRisk
  - TravelCost
  - Repetition
  - SaturationRisk
  - ResidentImpactRisk
```

Hard constraint는 점수로 상쇄할 수 없다. 폐업·예약 잠금·접근 불가·민감 주거지·포화 상한 초과 후보는 관심사 점수가 높아도 제외한다. Must Visit은 Feed에서 없애지 않고 더 나은 날짜·시간·주변 순서를 제안한다.

---

# 25. 오버투어리즘 해결 가능성과 검증 계획

## 25.1 최종 판정

Nullnull은 **오버투어리즘을 완전히 해결하는 시스템**이라고 말하기에는 아직 부족하지만, 공모전에서 검증 가능한 **관광객 선택의 시간·공간 분산 개입 시스템**으로는 충분한 가능성이 있다. 차별성은 혼잡 정보를 보여주는 데 있지 않고, 사용자가 이미 가진 일정과 취향을 보호하면서 SNS 노출·여행 후보 저장·수동 편집/AI Preview·사용자 승인을 하나의 측정 가능한 흐름으로 연결하는 데 있다.

아래 세 조건을 지켜야 이 주장이 성립한다.

1. 피크 회피를 계획 단계에서 실제 수락 가능한 변경으로 연결한다.
2. 대안 장소의 역혼잡과 주민 피해를 P0 응답별 노출 상한·수용력 Gate로 막고, P1에서 지속형 ExposureBudget으로 확장한다.
3. 계획 변화와 실제 방문 변화를 구분해 표현한다.

## 25.2 측정 사다리

| Level | 측정 단위 | 데이터 | 핵심 질문 | P0 가능 |
|---|---|---|---|---|
| 0 노출 | Feed·Live impression | 후보·rank·권역·시간·reason | 추천 자체가 한 곳에 몰리지 않았나 | 가능 |
| 1 관심 | 클릭·북마크·TripCandidate·숨김 | UserActionEvent | 분산 후보가 관련성과 신뢰를 유지하나 | 가능 |
| 2 계획 | 최적화 전후 TripItem | OptimizationDecision·CrowdSnapshot·RouteMatrixSnapshot | 피크 날짜·시간·권역의 계획과 이동 부담이 줄었나 | 가능 |
| 3 행동 | 선택적 체크인·방문 확인 | 동의 표본·제휴 집계 | 승인한 계획을 실제로 따랐나 | 공모전 이후 |
| 4 지역 결과 | 권역·시간별 집계와 대조군 | DMO·통신·현장 집계 | 지역 피크가 의미 있게 완화됐나 | 파트너 검증 |

## 25.3 P0 지표와 산식

```text
PlannedPeakRate =
  analyzable TripItems at CrowdLevel 4~5 / all analyzable TripItems

PlannedPeakExposureReduction =
  (PlannedPeakRate_before - PlannedPeakRate_after)
  / PlannedPeakRate_before

OptimizationApplyRate =
  applied valid optimization proposals / viewed valid optimization proposals

SpatialDetourCompletionRate =
  current-data detour candidates added to a Trip
  / viewed valid LIVE ORDINAL_LOWER candidate sets

RecommendationExposureHHI =
  Σ (impression share of each POI or area)^2

MustVisitRetention =
  retained Must Visit destinations / Must Visit destinations before optimization
```

- 분모에서 혼잡 데이터가 없는 항목을 몰래 제외하지 않고 `analyzable coverage`를 함께 보고한다.
- 시간 Level 개선은 같은 POI·source·normalization version·forecastIssueId, 공간 개선은 같은 comparisonGroupId·snapshotSetId·관측시각 편차 Gate의 sourceOrdinal로만 계산한다.
- ABSOLUTE·RELATIVE는 분해 보고하되 Selector와 KPI 산식을 중복 구현하지 않는다.
- `SpatialDetourCompletionRate`는 화면 내 공간 분산 전환 proxy이며 실제 방문 우회율로 표현하지 않는다.
- `REPLAY` 시연 이벤트는 기능 재현 증거로만 별도 집계하고 현재 혼잡 기반 SpatialDetourCompletionRate·효과 지표의 분자·분모에서 제외한다.
- 계획 감소에는 반드시 `(proxy)`를 붙이고 실제 방문 효과로 표현하지 않는다.
- HHI·상위 10% 노출 점유율을 관심사 적합도·TripCandidate·실제 일정 반영·숨김과 함께 보고해 다양성만 높이고 품질을 잃지 않았는지 본다.

## 25.4 실험 설계

### 공모전 P0 — 기능 검증

- P0 한국어 Fixture 8종과 계약 밖 입력의 직접 보정으로 파싱·Must Visit 보호·빈 시간 계산 검증
- 같은 일정에 `engagement 중심`과 `distribution-aware` 두 랭킹을 오프라인 재생해 POI·권역 노출 집중 비교
- ABSOLUTE 능동 CTA와 사용자 직접 RELATIVE 필터에서 동일 comparisonGroupId·snapshotSetId의 낮은 sourceOrdinal·CandidateRelation 후보만 노출되고 POI 상세·TripCandidate까지 이어지는지 검증
- REPLAY Header→후보→상세→담아두기, 7일 신선도·주 1회 갱신·D-2 경보를 검증
- ITEM 최적화안이 운영·예약·일정 충돌·접근성 Gate를 통과하고 경로 없이 이동시간 검증을 주장하지 않는지 테스트
- 후보 저장이 일정을 변경하지 않고, 수동 편집과 AI 적용이 서로 다른 이벤트·승인 경로를 갖는지 검증
- 심사 데모에서는 실제 사용자 성과가 아니라 전후 계산과 이벤트 기록을 보여줌

### 공모전 이후 — 사용자 행동 검증

- A/B 단위는 사용자 또는 Trip으로 고정해 교차 오염을 막는다.
- Control은 관련성 중심 Feed, Treatment는 관련성+다양성+혼잡 Context Feed로 둔다.
- Primary는 valid optimization apply와 Planned Peak Exposure, Guardrail은 후보 저장·실제 일정 반영·숨김·Must Visit retention·이동시간 증가다.
- 선택적 방문 확인은 별도 동의·철회·보존기간을 두고 원시 이동 이력을 수집하지 않는 방식부터 검증한다.

### 지역 파트너 — 실제 효과 검증

- 캠페인 권역·기간과 유사 대조 권역·기간을 사전에 정한다.
- 노출→계획→확인 방문의 funnel과 권역별 피크 분포를 집계 수준으로 비교한다.
- 이벤트·날씨·교통·휴일 같은 외생 변수를 기록하고 단순 전후 차이를 인과효과라고 주장하지 않는다.

## 25.5 실패·역효과 감시

| 위험 | 선행 지표 | 자동·운영 대응 |
|---|---|---|
| 새 핫스폿 생성 | 대안 POI 노출·TripCandidate·Live Level 급증 | P0 응답별 슬롯 축소·후보 제외 → P1 시간창 노출 감쇠·중단 |
| 주민 생활권 피해 | 신고·운영자 플래그·민감 지역 접근 | 즉시 Visibility 제외, DMO 검수 |
| 지나친 이동 유도 | 제안 이동시간·거리·거절 증가 | 이동비용 가중치 상향, 최대거리 축소 |
| 관련성 저하 | 빠른 넘김·관심 없음·Feed 이탈 | 분산 보너스 상한 조정, 관심사 Gate 강화 |
| 허위 신뢰 | stale·Fallback 비율, 실제와 다름 신고 | 실시간 배지 제거, 문구 하향, 소스 중단 |
| 제안 피로 | 연속 거절·알림 차단 | 동일 유형 cooldown, 사용자 빈도 설정 |

## 25.6 공모전용 권장 표현

- 사용: `Nullnull은 SNS형 추천과 일정 의사결정 사이에 분산 개입을 넣어, 피크 혼잡에 노출되는 여행 계획을 줄이는 것을 검증합니다.`
- 사용: `현재 절대 혼잡에서는 과밀지역 우회 CTA를, 사용자가 직접 연 상대 비교와 명시된 과거 관측 재생에서는 같은 비교그룹·수집 배치의 검수 후보를 제공해 공간 분산 기능을 검증합니다.`
- 사용: `Must Visit은 지키고 날짜·시간·주변 경험부터 작은 변화를 제안합니다.`
- 사용: `공모전 단계에서는 계획 변화 proxy를 측정하고, 실제 방문·지역 효과는 동의 표본과 파트너 데이터로 단계적으로 검증합니다.`
- 금지: `AI가 실시간으로 모든 관광객을 분산합니다.`
- 금지: `혼잡도 5에서 2로 실제 방문객이 감소했습니다.`
- 금지: `숨은 명소를 추천하면 오버투어리즘이 해결됩니다.`

---

# 26. 최종 서비스 정의

Nullnull은 세로형 여행 게시글이 이어지는 Feed를 메인으로 제공하고, 사용자가 한국어 지원 형식 일정, TXT·CSV·XLSX 파일 또는 직접 입력으로 여행을 등록하면 장소·날짜·관심사·Must Visit 후보·빈 시간을 확인해 그 일정에 맞게 Feed를 개인화하는 여행 SNS다. Feed·검색·Live에서 누른 `+`는 선택한 여행의 `담아둔 장소(TripCandidate)`를 만들 뿐 확정 일정을 즉시 바꾸지 않는다. My Trip에서는 `일정 편집`으로 후보 추가·교체와 Must Visit·날짜·시간 잠금을 직접 수정하거나, `AI로 일정 최적화`에서 현재 ITEM/DAY/TRIP 범위의 변경 전·후 지도·타임라인·거리·이동시간·혼잡 지표를 확인하고 승인한다. `담아둔 대안도 포함`은 기본 OFF이며 승인 전에는 일정이 바뀌지 않고 적용·되돌리기는 원자적으로 처리한다.

P0 MVP는 TripCandidate, 수동 편집, 독립 잠금, 동일 POI 날짜 ITEM 최적화와 Live·REPLAY를 먼저 완결한다. P1은 공모전 밖의 발전계획이 아니라 P0 Gate 뒤 이어지는 **공모전 최종 구현 범위**이며, 실제 route matrix 기반 DAY/TRIP 리밸런싱, 사용자 opt-in 후보 교체, Live Re-plan과 검증을 추가한다. 전체 일정 생성·영어·검색·알림·UGC도 실제 E2E를 통과한 항목만 Feature Flag를 열고 제출물에 기재한다.

서비스의 첫인상은 `혼잡 회피 도구`가 아니라 `내 일정을 이해하는 여행 SNS`다. 일정이 없으면 공식 Seed 중심의 일반 여행 Feed, 일정이 있으면 여행지·날짜·관심사 기반 개인화 Feed, 여행 중이면 게시글 카드에 현재 여행지의 날씨·Live 상태·거리·남은 시간을 별도 Context로 보여준다. 혼잡 기능은 개인화된 발견을 후보 저장과 승인 가능한 일정 변화로 옮길 때 작동한다. 장소 직접 선택 또는 다음 일정 기준으로 현재 여행지와 검수된 다른 관광지의 상태를 **같은 소스·범위 안에서만** 비교하고, 결정적 서버 검증이 선호와 잠금을 보호한다. LLM을 쓰더라도 선호 해석과 근거 설명만 담당한다.

따라서 서비스 느낌에 대한 판정은 **맞다**. 다만 `날씨 등 복합 요소를 반영한 실시간 혼잡도`라는 표현보다 `관측 실시간 혼잡도와 날씨·거리·일정 맥락을 함께 보여주는 여행 의사결정 서비스`가 정확하다. 날씨·거리·남은 시간은 실시간 혼잡 관측값을 바꾸지 않고 추천과 설명의 별도 근거로 사용한다.

Nullnull의 공모전 목표는 실제 도시 전체의 혼잡 감소를 단정하는 것이 아니라, 추천 노출이 새로운 집중을 만들지 않도록 통제하면서 **사용자가 직접 승인한 여행 계획의 공간·시간 피크 노출과 불필요한 이동 부담을 줄이는 것**이다. 실제 방문과 지역 수준 효과는 동의 기반 데이터와 파트너 검증으로 확장한다.

---

## 공식 제출 안내

- 한국관광공사 공식 투어라즈 공고: https://touraz.kr/announcementList/pssrpView?curPage=2&pssrpSeqEnc=wY3RgFjvo8lP8yzPYIomsg%3D%3D
- 한국관광콘텐츠랩 접수 페이지: https://api.visitkorea.or.kr/#/cntSearchDetail?no=3
- 공공데이터포털 국문 관광정보 서비스: https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15101578
- 공공데이터포털 관광지 집중률 예측 정보: https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15128555
- 공개 공고에는 1차 심사자료의 정확한 `D_submit`이 없으므로, 대상자별 별도 안내 원문을 근거대장에 등록한다.
- 최종 제출 전 공지, 양식, 마감시각·timezone, 테스트 방식, API 이용조건·운영계정 승인량을 다시 확인한다.
