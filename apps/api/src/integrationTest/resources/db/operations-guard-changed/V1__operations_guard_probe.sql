-- OperationsSchemaGuardIT only: V1 of db/operations-guard with different content, standing for a migration that
-- was edited after it was applied.
CREATE TABLE operations_guard_probe (id bigint PRIMARY KEY);
