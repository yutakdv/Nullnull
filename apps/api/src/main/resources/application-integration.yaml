# Compose integration profile: every value comes from the environment.
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

nullnull:
  ai:
    base-url: ${NULLNULL_AI_BASE_URL}
